# Sanctuary Assurance Matrix

This matrix is the authoritative source for Sanctuary trust claims. Product, release, and marketing copy must trace each trust claim to a `proven` or `partial` row here, and must preserve the platform, gap, and next-proof limits named in that row. As of main at PR #276, the durable critical audit claim is mechanically true on main; the state-envelope and audit-chain rows remain partial where stated below.

Status enum: `proven`, `partial`, `manual`, `not_implemented`, `planned`, `in_flight`, `unknown`.

| Claim | Platform | Status | Evidence | Known Gap | Next Proof Needed |
|---|---|---|---|---|---|
| Agent wrapping (MCP interception) | MCP-compatible harnesses | partial | [server/test/wrap/wrap-empty-config.test.ts](server/test/wrap/wrap-empty-config.test.ts); [server/test/wrap/wrap-claude-code-paths.test.ts](server/test/wrap/wrap-claude-code-paths.test.ts) | Tests prove config rewrite and bootstrap behavior for representative harness paths. They do not prove interception for every listed harness version or non-MCP agent surface. | Harness compatibility drill that launches each supported harness, verifies the Sanctuary MCP entry is the active route, and proves an upstream tool call is observed through the broker. |
| Approval gating (Tier 1 / Tier 2 / Tier 3 policy) | Cross-platform server | proven | [server/test/principal-policy/approval-gate.test.ts](server/test/principal-policy/approval-gate.test.ts); [server/test/principal-policy/gate-tier1-unreachable-approval.test.ts](server/test/principal-policy/gate-tier1-unreachable-approval.test.ts) | The policy engine and fail-closed channel behavior are covered in unit tests. Operator UX timing and every transport-specific approval channel are not covered by one end-to-end drill here. | End-to-end approval drill that exercises Tier 1 deny, Tier 1 approve, Tier 2 anomaly, Tier 3 allow, and unreachable-channel deny through the operator surface. |
| Critical audit durability (appendCritical) | Cross-platform server storage | proven | [PR #271](https://github.com/eriknewton/sanctuary-framework/pull/271); [PR #276](https://github.com/eriknewton/sanctuary-framework/pull/276); [server/test/operational/audit-log-flush.test.ts](server/test/operational/audit-log-flush.test.ts); [server/test/cognitive/audit-durability.test.ts](server/test/cognitive/audit-durability.test.ts); [server/test/audit/critical-followup.test.ts](server/test/audit/critical-followup.test.ts) | Critical Cognitive-layer mutations fail closed on audit write failure in the tested storage adapters. PR #276 migrated 43 additional critical call sites across principal-policy, proxy, Castle Wall blocked-egress, sovereignty-profile, bridge/reputation, broker, and token-issuer. Two synchronous API sites remain flagged for later restructure. | Filesystem-backed durability drill with injected ENOSPC, EIO, partial-write, process-exit races, and coverage for the remaining sync-API restructure sites on the production storage path. |
| Tamper-evident audit chain | Cross-platform server audit log | partial | [PR #274](https://github.com/eriknewton/sanctuary-framework/pull/274); [server/test/operational/audit-log-chain.test.ts](server/test/operational/audit-log-chain.test.ts); [server/src/audit/chain.ts](server/src/audit/chain.ts); [server/src/operational/audit-log.ts](server/src/operational/audit-log.ts); [server/src/cli/audit-chain-export.ts](server/src/cli/audit-chain-export.ts); [server/src/cli/audit-chain-verify.ts](server/src/cli/audit-chain-verify.ts); [server/test/audit/external-verifier-drill.test.ts](server/test/audit/external-verifier-drill.test.ts); [server/test/cli/audit-chain-verify-exit.test.ts](server/test/cli/audit-chain-verify-exit.test.ts); [server/src/audit/checkpoint-identity.ts](server/src/audit/checkpoint-identity.ts); [server/test/audit/checkpoint-signer-wiring.test.ts](server/test/audit/checkpoint-signer-wiring.test.ts) | Audit entries carry monotonic sequence, `prev_hash`, `entry_hash`, root-hash recomputation, legacy-anchor migration, and strict-mode verification defaults. The production checkpoint signer and authenticated key resolver are now derived inside the `AuditLog` constructor from its required arguments, so every production call site signs checkpoints when the fortress holds an identity and verifies them through the identity's signed rotation history; a wired-consumer test drives the real `createSanctuaryServer` boot path. Bounds: a fortress with no identity yet, and the root daemon's split audit store, write honestly `unsigned` checkpoints; `audit-chain verify --no-strict` reports findings with exit code 10; single-file export is one-chain-only when multiple audit chains exist, and standalone verification checks rotation-anchor shape/linkage but not the custody-key MAC. This slice is wiring plus authenticated verification of SIGNED checkpoints only: an `unsigned` checkpoint is accepted as unsigned, so a signed-to-unsigned downgrade is NOT yet detected (register id **IC-05-DG**); detection requires authenticated downgrade evidence and is deferred to a design-first follow-up plus the external-verifier drill. The signed-checkpoint claim stays partial pending drill evidence: **IC-05**. | Design-first follow-up for authenticated downgrade evidence (**IC-05-DG**), then an external-verifier drill over production-signed and deliberately tampered checkpoints, then revisit the remaining bounds. External anchoring to a third-party transparency log remains separate. |
| State encryption | Cross-platform server storage | proven | [server/test/security/tamper-detection.test.ts](server/test/security/tamper-detection.test.ts); [server/test/security/iv-uniqueness.test.ts](server/test/security/iv-uniqueness.test.ts); [server/src/core/encryption.ts](server/src/core/encryption.ts) | Tests cover AES-GCM tamper rejection and IV uniqueness. They do not by themselves prove host keychain security or hardware-backed key custody. | Filesystem storage drill that confirms no plaintext state, unique IVs across persisted writes, and failed decrypt after ciphertext, IV, or tag mutation. |
| State envelope integrity / default verify-on-read | Cross-platform server storage | partial | [PR #275](https://github.com/eriknewton/sanctuary-framework/pull/275); [server/test/cognitive/state-envelope-integrity.test.ts](server/test/cognitive/state-envelope-integrity.test.ts); [server/test/cognitive/state-envelope-migration-drill.test.ts](server/test/cognitive/state-envelope-migration-drill.test.ts); [server/src/cognitive/state-store.ts](server/src/cognitive/state-store.ts); [server/src/cognitive/tools.ts](server/src/cognitive/tools.ts) | Schema-2 envelopes bind namespace, key, version, kid, metadata, integrity hash, and payload metadata, and are signed under the `sanctuary.state-envelope.v1` domain prefix. `state_read` verifies by default and rollback anchors are persisted with MAC authentication. The rollback floor still has an anchor-deletion replay residual; closing that requires boot-anchored or externally attested storage. A separate private key-resolution hardening item tracked under register id M-01 is closed. Representative legacy entries auto-migrate on first verified read when the local writer identity is available; outlier legacy shapes or missing local writer identities still need soak coverage. | Run a long-running upgrade soak with mixed legacy and schema-2 traffic across all production namespace types, including outlier legacy shapes and missing-local-identity cases. |
| Config/profile fail-closed | Cross-platform server startup and profile storage | proven | [PR #269](https://github.com/eriknewton/sanctuary-framework/pull/269); [server/test/unit/core/config.test.ts](server/test/unit/core/config.test.ts); [server/test/sovereignty-profile.test.ts](server/test/sovereignty-profile.test.ts); [server/src/config.ts](server/src/config.ts); [server/src/sovereignty-profile.ts](server/src/sovereignty-profile.ts) | Config and sovereignty profile corruption are classified, quarantined, and fail closed in tested load paths. This does not prove every future config-like store inherits the same behavior automatically. | Registry check for persistent config/profile stores that requires corruption tests and fail-closed load behavior for each new store. |
| Health and attestation evidence-based | Cross-platform server tools | proven | [PR #273](https://github.com/eriknewton/sanctuary-framework/pull/273); [server/test/attestation-evidence.test.ts](server/test/attestation-evidence.test.ts); [server/test/health-evidence.test.ts](server/test/health-evidence.test.ts); [server/src/health/evidence.ts](server/src/health/evidence.ts); [server/src/index.ts](server/src/index.ts) | `exec_attest` and `monitor_health` now use structured local evidence reports instead of unsupported self-claims. This does not prove TEE-backed execution or remote attestation. | Evidence drill that verifies reported checks against independent process, config, storage, and audit observations, plus a separate TEE proof before any hardware-attestation claim. |
| Egress enforcement: Linux (Castle Wall Phase 1) | Linux | not_implemented | [castle-wall-daemon/tests/integration_kernel_binding.rs](castle-wall-daemon/tests/integration_kernel_binding.rs); [castle-wall-daemon/tests/integration_dns_bypass.rs](castle-wall-daemon/tests/integration_dns_bypass.rs); [.github/workflows/castle-wall-linux.yml](.github/workflows/castle-wall-linux.yml) | The Linux modules are proven against a real kernel by integration tests, but the shipped daemon boot path does not install the nftables table, bind NFQUEUE, create cgroup scopes, or call the deny-by-default evaluator; the systemd unit also uses `Type=notify` while the daemon never sends readiness. Open defect: **IC-02, IC-03, IC-04**. | Wire the shipped daemon boot path to the tested kernel-enforcement loop, fix systemd readiness, then run an independent Linux drill on a clean host where a wrapped agent attempts raw IP, DNS, DoH, and DoT exfiltration and receives kernel-level blocks with audit evidence. |
| Egress enforcement: macOS (Castle Wall Phase 1) | macOS | proven | [docs/audit/castle-wall-macos-boot-survival-redrill-2026-06-22.md](docs/audit/castle-wall-macos-boot-survival-redrill-2026-06-22.md); [docs/audit/castle-wall-macos-fullscope-redrill-2026-06-20.md](docs/audit/castle-wall-macos-fullscope-redrill-2026-06-20.md); [docs/audit/castle-wall-macos-fresh-binary-redrill-2026-06-16.md](docs/audit/castle-wall-macos-fresh-binary-redrill-2026-06-16.md); [docs/audit/castle-wall-macos-boot-survival-drill-2026-06-14.md](docs/audit/castle-wall-macos-boot-survival-drill-2026-06-14.md); [docs/audit/castle-wall-macos-allow-deny-drill-2026-06-11.md](docs/audit/castle-wall-macos-allow-deny-drill-2026-06-11.md); [castle-wall-macos/README.md](castle-wall-macos/README.md); [castle-wall-macos/Tests/CastleWallExtensionTests/FilterProviderTests.swift](castle-wall-macos/Tests/CastleWallExtensionTests/FilterProviderTests.swift); [server/docs/castle-wall-macos-deploy.md](server/docs/castle-wall-macos-deploy.md) | The proven claim is exactly: Castle Wall on macOS enforces a signed operator policy with a clean per-uid allow/deny demonstration (an agent uid is blocked on a non-allowlisted destination and reaches its allowlisted one, while the operator uid is unaffected, in the same armed window) that survives attended reboot cycles, proven on a Dev-ID-signed and notarized binary built from the project's mainline source tree. Fresh-binary re-drill 2026-06-16 (Mini1, macOS Tahoe 26.5.1): the binary was built from origin/main's exact tree 053093963dbf, Dev-ID-signed plus notarized and stapled (v807, activated ext sha 26e3c0ce, verified to be the activated binary replacing the prior v774 5fae8f4b); A2 per-uid allow/deny N=3 (agent to non-allowlisted IP 1.1.1.1 blocked 000, agent to allowlisted hostname api.anthropic.com reached 404, operator unaffected 301); A1 attended boot-survival 5 of 5 real reboots with per-uid enforcement holding across every reboot; C6 absent-boot-token fail-closed, C7 launchd KeepAlive recovery after a daemon kill, and a populated boot-audit chain (1052 checkpoints) that verifies clean under the recovered master. The earlier 2026-06-11 drill (system extension v716, N=3) first proved the clean allow/deny demonstration plus graceful disarm 3/3 and dead-man fail-open 3/3 with zero ruleDigestMismatch. Full-scope re-drill 2026-06-20 (Mini1, Tahoe, Erik-present, drill-acceptance applied): re-proven on a fresher signed binary v876 (git 75a48c5794b5; system extension v858 -> v876; Dev-ID-signed plus notarized and stapled, spctl "Notarized Developer ID") that carries the #653 native posture-board embed and the #596 async-init crash fix; per-uid allow/deny to https://www.apple.com was deterministic with operator uid 501 3/3 ALLOWED (HTTP 200) and agent uid 502 3/3 BLOCKED (curl (7) couldn't connect, port 443), armed via the GUI toggle and enforced by the safe-mode boot daemon (global pin c3f22755). Attended boot-survival re-drill 2026-06-22 (Mini1, Tahoe 26.5.1, Erik-present, coordinator-verified over SSH): F1 closed. The 2026-06-20 window installed and certified the F1 LaunchDaemon boot service on signed+notarized v876 but deferred the reboot cycles; this 2026-06-22 drill runs the N=5 acceptance criterion on that same build. Every attended cycle: sysext activated/enabled, safe-mode boot daemon auto-started (fresh PID each reboot), operator uid 501 3/3 ALLOWED (HTTP 200), agent uid 502 3/3 BLOCKED (curl (7) port 443, ~10-22ms), box reachable, enforcement live within approximately 45-60 seconds of boot. System Application Firewall OFF (confirms the block is the NE content filter, not ALF). A control reboot with the filter toggled OFF showed no enforcement, confirming filter state (on or off) persists across reboot. Carried-forward honest bounds (must NOT be claimed beyond): this is NOT unattended reboot survival (Mini2 2026-07-29 proved authrestart-to-login-window can deliver a manifest without an arm lease and fail open until F-SAFEMODE-NO-LEASE and F-LEASE-NO-TTL close); NOT "audited per-rule per-flow" and NOT "tamper-evident per-flow audit" (a per-rule read-out exists, but the unforgeable producer-signed per-flow audit is inert: that is the real C4 gap); the freshest proof attaches to the drilled tree 75a48c5794b5; one host and one OS version (Tahoe 26.5.1); no p99 overhead; the arm-lease TTL expiry leg remains inconclusive (the headless CLI arm path is still wedged on Tahoe, W7-1); and hardware boot-verification of the GUI host-app launch and the #596 async-init crash fix (this drill tested the daemon + per-uid enforcement, NOT the GUI app launch) remains owed. | Unattended-reboot drill with as-uid differential after the remaining safe-mode surfacing and lease-TTL fixes land; sustained-operation drill measured in hours; the TTL-expiry leg through the real CLI enable path once the headless-arm wedge (W7-1) is fixed; a second macOS host or OS version; p99 overhead capture; and hardware boot-verification of the GUI host-app launch and the #596 crash fix on a fresh reboot. |
| Per-agent network stop | macOS | partial | [server/test/castle-wall/provision/agent-stop.test.ts](server/test/castle-wall/provision/agent-stop.test.ts); [server/test/hub/fortress-tier1.test.ts](server/test/hub/fortress-tier1.test.ts); [server/test/dashboard/v1_1/templates.test.ts](server/test/dashboard/v1_1/templates.test.ts) | Tests prove the dashboard stop path refuses without verified-green enforcement, revokes agent-matchable allow rules from the signing source, preserves the habeas lane, reports partial when reload is unconfirmed, and no longer writes a fortress engaged record for zero effect. This is not yet a captured on-hardware stop drill. | macOS drill on an armed disposable fortress: before/after as-uid probes for every declared endpoint, operator shell unaffected, audit evidence with residual_allow_count 0 and reload_confirmed true, lift restores the rule set byte-for-byte, N>=3 plus one disarmed-wall refusal leg. |
| Dashboard approval flow | Cross-platform local dashboard | partial | [server/test/principal-policy/dashboard.test.ts](server/test/principal-policy/dashboard.test.ts); [server/test/principal-policy/approval-aggregator.test.ts](server/test/principal-policy/approval-aggregator.test.ts); [server/test/cli/inbox-approvals.test.ts](server/test/cli/inbox-approvals.test.ts) | HTTP approve/deny, timeout-deny, SSE stream, aggregator routes, and CLI approval commands are tested. Live dashboard refresh is partial: five of six inbox sources and status changes can go stale until reload, so dashboard copy must not promise real-time delivery. Open defect: **IC-12**. A browser-level operator drill is not represented in this matrix. | Browser drill that opens the dashboard, receives a live Tier 1 request, approves and denies through the UI, verifies visible refresh across all inbox sources without reload, and verifies the gate result plus audit receipt. |
| Export / exit bundle | Cross-platform server and CLI | partial | [server/test/exit/exit-bundle.test.ts](server/test/exit/exit-bundle.test.ts); [server/test/drills/v1.1-exit-drill.test.ts](server/test/drills/v1.1-exit-drill.test.ts); [server/test/exit/exit-bundle-hardening.test.ts](server/test/exit/exit-bundle-hardening.test.ts) | CLI export and verifier coverage exist, but the whole exit guarantee is partial: dashboard export omits the state re-key key, import hides skipped-entry counters, and imported bundles from a fortress that rotated identity keys can silently lose pre-rotation state. Legacy Operational audit receipts are archive-only in the current verifier, and individual legacy audit signatures are not verified as normal query continuity. Open defect: **IC-07, IC-08, IC-09**. | External exit drill across fresh and key-rotated fortress directories using shipped CLI and dashboard paths: export, offline verify, import, re-key, visible skipped-entry refusal or warning, conflict refusal, and source-key cleanup verification. |
| Query anonymity (selective disclosure) | Cross-platform server outbound payloads | proven | [server/test/query-anonymity/header-strip.test.ts](server/test/query-anonymity/header-strip.test.ts); [server/test/query-anonymity/pii-rewrite.test.ts](server/test/query-anonymity/pii-rewrite.test.ts); [server/test/query-anonymity/smart-mode.test.ts](server/test/query-anonymity/smart-mode.test.ts); [server/test/drills/v1.1-privacy-drill.test.ts](server/test/drills/v1.1-privacy-drill.test.ts); [server/test/drills/v1.x-provider-matrix-privacy-drill.test.ts](server/test/drills/v1.x-provider-matrix-privacy-drill.test.ts) | Coverage is bounded by the magic-string corpus and the shipped adapter list at drill time; novel PII shapes not represented in the corpus and provider adapters added after this drill require corpus and matrix extension. | Drill-on-add: any new outbound adapter or PII shape requires a matrix-extension PR before merge. |
| Context-gate single source of truth | Cross-platform server tools | proven | [PR #281](https://github.com/eriknewton/sanctuary-framework/pull/281); [server/test/operational/context-gate.test.ts](server/test/operational/context-gate.test.ts); [server/test/operational/context-gate-enforcer.test.ts](server/test/operational/context-gate-enforcer.test.ts); [server/test/system-prompt-generator-v2.test.ts](server/test/system-prompt-generator-v2.test.ts); [PR #269](https://github.com/eriknewton/sanctuary-framework/pull/269) | The persisted sovereignty profile `features.context_gating` is the source of truth; `ContextGateEnforcer` is initialized from the profile at boot; proxy `contextGateFilter` delegates to the live enforcer instead of returning unchanged args; combined-state status is surfaced through health. This does not prove every future outbound surface automatically registers with the shared enforcer. | Registry guard for new outbound surfaces requiring explicit context-gate routing, plus an end-to-end drill that toggles profile `features.context_gating` and verifies prompt text, proxy filtering, health status, and audit behavior across restart. |
| Proxy isolation: env (PR #267) | Cross-platform stdio MCP upstreams | proven | [PR #267](https://github.com/eriknewton/sanctuary-framework/pull/267); [server/test/proxy/client-manager.test.ts](server/test/proxy/client-manager.test.ts); [server/src/proxy/client-manager.ts](server/src/proxy/client-manager.ts) | Stdio upstreams receive a minimal allowlisted environment with deny filtering. This row does not cover SSE URL SSRF or network isolation. | Regression test that spawns a fixture upstream process and asserts observed environment contains only the allowed keys and explicit safe operator config. |
| Proxy SSE SSRF protection | Cross-platform SSE MCP upstreams | proven | [PR #278](https://github.com/eriknewton/sanctuary-framework/pull/278); [server/src/proxy/client-manager.ts](server/src/proxy/client-manager.ts) | `ssrf-validator.ts` blocks loopback, RFC1918, link-local, multicast, IPv6 loopback/local, and cloud metadata addresses; DNS resolution is validated before connection; the operator escape hatch is audited through `appendCritical`. This does not prove kernel-level network isolation for SSE transports. | Network-level SSE drill that attempts DNS rebinding and redirect chains from a fixture upstream, confirms validator rejection plus critical audit evidence, and separately verifies Castle Wall or host firewall enforcement for configured SSE egress. |
| Dynamic proxy tool discovery | Cross-platform MCP upstreams | proven | [PR #283](https://github.com/eriknewton/sanctuary-framework/pull/283); [server/src/proxy/client-manager.ts](server/src/proxy/client-manager.ts); [server/test/proxy/client-manager.test.ts](server/test/proxy/client-manager.test.ts) | Dynamic tool-list strategy handles `tools/list_changed` notifications; `ClientManager` refreshes the proxy catalog on reconnect; Tier 1 approval gating, context-gate filtering, and critical audit emission are preserved. This does not prove every possible upstream transport emits timely change notifications. | End-to-end upstream fixture drill that mutates its tool list under load, verifies stale tools disappear after reconnect or notification, and records policy wrapping plus audit evidence for added and removed tools. |
| did:key encoding (PR #268, base58btc compliant) | Cross-platform identity | proven | [PR #268](https://github.com/eriknewton/sanctuary-framework/pull/268); [server/test/identity-did-key.test.ts](server/test/identity-did-key.test.ts); [server/src/core/identity.ts](server/src/core/identity.ts) | New identities use base58btc `did:key` encoding and tests include W3C Ed25519 vectors. Legacy base64url DIDs may still exist in historical state and docs until migration cleanup. | Migration drill that loads legacy identities, preserves verification, emits new compliant DIDs for new identities, and confirms public docs no longer describe the legacy encoding as current behavior. |
| Identity signing authority (PR #270, raw identity_sign Tier 1) | Cross-platform identity | proven | [PR #270](https://github.com/eriknewton/sanctuary-framework/pull/270); [server/test/identity-signing-helpers.test.ts](server/test/identity-signing-helpers.test.ts); [server/test/principal-policy/approval-gate.test.ts](server/test/principal-policy/approval-gate.test.ts); [server/test/security/tool-registry-raw-signing-guard.test.ts](server/test/security/tool-registry-raw-signing-guard.test.ts); [server/docs/tool-registry-raw-signing-policy.md](server/docs/tool-registry-raw-signing-policy.md) | Raw `identity_sign` is Tier 1 and typed internal signing helpers are not exposed as MCP tools. Guard enforces classification per registered tool name; non-registered helpers and dynamically constructed tool names remain manual-review surface. | Static analysis pass on every Ed25519.sign() call site that is not behind a domain-separation wrapper. |
| Composition root review surface | Cross-platform server startup | planned | [server/src/index.ts](server/src/index.ts); [server/test/composition/composition-v1.test.ts](server/test/composition/composition-v1.test.ts); Wave 4 deferred to v1.4 | Composition features have dedicated tests, but the main server composition root remains large and is explicitly deferred to Wave 4 review in v1.4. Reviewability and trust-boundary mapping are not yet proven. | Wave 4 composition-root review: split startup wiring into bounded modules, add dependency graph tests, and document trust-boundary ownership per module. |
| Hostile-guest containment: macOS box launcher jails uncooperative plugins (B2 inner-confinement, seccomp-deny-AF_VSOCK) | macOS host, Linux guest (Apple Containerization) | proven | [PR #441](https://github.com/eriknewton/sanctuary-framework/pull/441); [PR #439](https://github.com/eriknewton/sanctuary-framework/pull/439); [castle-wall-vmm/Sources/SanctuaryVMM/SanctuaryGuestJail.swift](castle-wall-vmm/Sources/SanctuaryVMM/SanctuaryGuestJail.swift); [castle-wall-daemon/src/jail.rs](castle-wall-daemon/src/jail.rs); launcher-integration drill on the signing/boot host, 2026-06-09, GREEN 3/3: Phase A baseline (jail off) reproduced the vminitd escape (`B2X-EXPLOIT-CONFIRMED`), Phase B (jail on) shut it (`B2X-REJECTED` + `[JAIL]` marker) on all 3 boots; evidence in the PR #441 body and the coordinator drill runbook | The proven claim is exactly: the trusted launcher applies seccomp-deny-AF_VSOCK inner-confinement to an UNCOOPERATIVE plugin and the vminitd escape confirmed without it is shut with it (N=3/3), with AF_UNIX egress intact, via the python-preamble delivery on a python-capable image (`python:3.12-alpine`). It does NOT yet cover python-less guest images: the static `sanctuary-jail` delivery path (binary authenticated by a required operator-pinned SHA-256 computed on its staged copy, ELF shape checks as secondary sanity only, bind-mounted by the launcher, static linkage enforced in CI) is built and unit/CI-verified but has not run its own box drill. It is also one inner-confinement layer, not full plugin-isolation; do not claim "plugin-isolated on Mac". | Static-binary delivery drill on the signing/boot host (coordinator-orchestrated, Erik present, N≥3): forced static delivery shuts the same exploit probe with the shim's `[JAIL]` marker, plus python-less-image fail-closed checks -- see the static-binary addendum in the B2 launcher-integration drill runbook. |
| Verifiable transparency checkpoint format and offline verifier | Cross-platform server and CLI; signing custody macOS helper-as-signer | proven | [server/src/transparency/checkpoint.ts](server/src/transparency/checkpoint.ts); [server/src/transparency/emitter.ts](server/src/transparency/emitter.ts); [server/src/transparency/verify.ts](server/src/transparency/verify.ts); [server/src/transparency/against-log.ts](server/src/transparency/against-log.ts); [server/test/transparency/emitter.test.ts](server/test/transparency/emitter.test.ts); [server/test/transparency/verify.test.ts](server/test/transparency/verify.test.ts); [server/test/transparency/against-log.test.ts](server/test/transparency/against-log.test.ts); [server/test/transparency/cli-transparency.test.ts](server/test/transparency/cli-transparency.test.ts); [server/docs/transparency-checkpoints.md](server/docs/transparency-checkpoints.md) | Tests prove the format and verifier properties: forged signatures rejected, counter rollback/gap/duplicate and replaced-checkpoint linkage detected, tampered counters detected, fail-closed emission (no unsigned or partial checkpoint on any failure). Offline completeness: a clean PASS / exit 0 means "verified complete from genesis with the authentic genesis sentinel" and nothing else. A bundle whose earliest checkpoint is counter N>1 FAILS by default (withheld-prefix `counter_prefix_missing`); a counter-1 record whose `previous_checkpoint_hash` is not the genesis sentinel (`"GENESIS"`) FAILS as a forged/withheld origin (`genesis_sentinel_mismatch`) and `--allow-partial` cannot launder it; a misplaced genesis sentinel on a non-earliest record FAILS (`genesis_sentinel_misplaced`). A suffix fragment verifies only under explicit `--allow-partial`, which returns the DISTINCT verdict `PARTIAL` and the dedicated exit code `10` (never PASS / exit 0), so no caller gating on verdict or exit code can read incomplete evidence as a complete-from-genesis verification. Host (`--against-log`) detection: tail truncation, content alteration, and non-prefix in-window deletion are detected against the live log; deletion of the LOWEST covered entry (which mimics rotation by raising the live floor) is detected ONLY when the master-key-MAC'd rotation anchor authenticates the cut (requires the fortress passphrase) and otherwise FAILS closed (`rotation_floor_unauthenticated`). A host PASS therefore means "the live log matches the signed root over the covered window, OR a pruned prefix was proven by an authenticated rotation anchor"; a host run that emits only a `merkle root not recomputed` note is NOT evidence the live log matches the signed root. What this row does NOT claim: that the wall was enforcing on the wire (enforcement claims trace to the Castle Wall drill rows, not to this format); the emitting binary hash is self-reported; there is no hosted transparency log and no external anchoring; publishing is a manual operator action. No third-party cross-host verification drill has been captured yet. | Cross-host drill: operator publishes a bundle from the enforcement host, an independent party verifies it offline on a different machine with only the published key, and a deliberately truncated log on the host is caught by --against-log; then external anchoring (e.g., Sigstore Rekor) for operator-independent proof. |
