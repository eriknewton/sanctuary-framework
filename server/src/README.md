# server/src - Module Map

This is the first thing to read before you touch the Sanctuary server. `server/src` is the
TypeScript MCP server: 61 module directories plus a small set of root files. It is the in-process
"fortress" runtime - crypto core, the four sovereignty layers, enforcement surfaces, networking,
identity, and the operator CLI. The native OS-level enforcers (Rust/Swift) live OUTSIDE this tree
(see "Adjacent out-of-scope systems").

This map exists because the codebase is a legibility problem, not a code-removal problem. No dead
code was found; nothing here is a delete candidate. Several names collide or mislead, so most of the
value is in the disambiguation sections below.

## Named-layer vocabulary

Sanctuary has four sovereignty layers. The L1..L4 numbering is being retired in prose, but the
`lN` tokens are still LIVE wire and at-rest contracts (tool names, audit fields, HKDF labels,
`L1Status..L4Status` exports) and must survive byte-for-byte. Use the named layer in writing; never
edit the wire token.

| Layer | Named layer (human / property) | Wire token | Owning module(s) | One line |
|-------|--------------------------------|-----------|------------------|----------|
| L1 | Castle Wall / Cognitive | `l1` / `l1_cognitive` | `cognitive`, `core`, `castle-wall` | Encrypted at-rest state + identity keys; the crypto foundation |
| L2 | Sentinels / Operational Isolation | `l2` / `l2_operational` | `operational`, `sentinel`, `anomaly-detection` | Append-only audit log + behavioral watchers + egress context-gate |
| L3 | Charter / Selective Disclosure | `l3` / `l3_selective_disclosure` | `disclosure`, `sdw`, `query-anonymity` | Commitments, ZK proofs, the secret broker, controlled disclosure |
| L4 | Heralds / Verifiable Reputation | `l4` / `l4_reputation` | `reputation`, `attestation`, `shr` | Portable signed reputation attestations + badge UX |

## How to use this map

1. Find your module in the MODULE INDEX TABLE. The "Distinct from" cell tells you what it is NOT.
2. If a name confused you, jump to "Confusable clusters" - most pain is lexical, not structural.
3. Before renaming or moving anything, read the "Do-not-touch" cell and the "Conventions" section.
   Many directory names double as frozen wire tokens; the rule is "directory names and relative
   import paths move; tool names, routes, HKDF labels, and display strings never do."

---

## MODULE INDEX TABLE (61 modules)

> **IC-16 correction (2026-09-01):** the SDW owner pin is no longer process-local. Production persists a master-MAC-authenticated one-owner-per-fortress record in `_sdw_meta`, atomically claims only a fresh empty store, authenticates it on every SDW route, and uses compare-and-replace for explicit operator transfer. Existing unpinned SDW data refuses until an operator claims it. This closes distinct correctly wrapped cross-process isolation; the cooperative harness-configuration rewrite bound remains. This correction supersedes the older process-local bound still embedded in the historical `sdw` row below.

All 61 module directories under `server/src` are listed. Status legend: **canonical** = a real,
wired subsystem; **thin/utility** = honestly one or two files; **default-off-allocated** = real code,
deliberately unwired or off by default (not dead, not shipped-as-enforcing); **versioned-frozen** =
a versioned wire/route surface that is frozen. "Barrel?" = does the dir expose a thin re-export
`index.ts` (53 of 61 do today).

| Module | Subject area | Status | What it owns | Distinct from | Barrel? | Do-not-touch |
|--------|-------------|--------|--------------|---------------|---------|--------------|
| principal-policy | Cognitive (runtime approval) | canonical | The runtime human-in-the-loop approval gate: Tier 1/2/3 classification, baseline anomaly, approval channels, unified inbox/aggregator. `dashboard.ts` (the daemon `DashboardApprovalChannel`) also hosts the fleet console: it builds the durable federation roster via `fleet-roster.ts` and, PR-B, applies the paid node-count cap (`entitlement/applyFleetCap` over `resolveActivation`) to that roster + serves operator-bearer-gated `POST /api/fleet/activate` (`handleFleetActivate` → `activateFleetLicense`). Add-Machine slice: read-only `GET /api/fleet/capacity` (`handleFleetCapacity` → `computeFleetCapacity` → `entitlement/computeFleetCapacityView`) surfaces active/max/remaining node-count headroom, and operator-bearer-gated `POST /api/fleet/enroll-token` (`handleFleetEnrollToken`) mints a bootstrap token via the SAME shipped ceremony primitive (`v1/federation.ts`'s `JoinCeremony.authorizeInit`) with an advisory at-capacity 409 pre-check (never re-implements the join ceremony or durable roster). Enforcement is fail-closed to the community floor and NEVER gates a node's wall/local-dashboard/policy-push/kill-safety | policy-engine (asks no human); `entitlement/` (the cap/activation/capacity core the daemon roster consumes) | Yes | tool names `principal_policy_view`/`principal_baseline_view`; tier tokens `tier1_always_approve`/`tier2_anomaly`/`tier3_always_allow`; `auto_deny=true` (SEC-002); frozen policy file `principal-policy.yaml`; `normalized_args_hash` amplification key; operator-bearer-gated routes `POST /api/fleet/activate`, `POST /api/fleet/enroll-token`; read-only `GET /api/fleet/capacity` |
| file-grant | Cognitive (box-local governed file access) | canonical (v1: box-local, read-only) | Governed File-Grant v1: a `FileGrant` record persisted in the StateStore's `_file_grants` namespace (encrypted, signed, monotonic-versioned); a pure `planGrantTree` planner + injected `FsOps` interface (real POSIX impl in `fs-ops.ts`) that place/scrub grant-tree entries as plain symlinks in an OPERATOR-owned per-agent subdir (R3-3: v1 never cross-uid chowns the subdir -- it never applies the functional cross-uid read primitive anyway, so a chown accomplished nothing useful while blocking a non-root operator's later revoke; the deferred acceptance drill is what applies the real ownership/ACL primitive); mint/revoke/list orchestration (`mint.ts`/`revoke.ts`/`list.ts`) pure over an injected clock; a runtime `reconcileFileGrantTree` (`reconcile.ts`) makes `planGrantTree` the enforced plan and scrubs expired entries + flips status; `sanctuary file-grant mint\|list\|revoke` CLI (`cli/file-grant.ts`). Enforcement label is HONEST and never overclaims (Invariant #5, build-spec section 10): the same-uid check is derived from the SOURCE file's owner (via `stat`, never `process.getuid()`, so `sudo` cannot fabricate a false "enforced"); `met` requires an actual agent-uid readability verification, a bare uid split reports `unverified` ("configured; on-hardware read-scope not yet verified"), and a same-owner / no-agent-uid box reports `unmet`. v1 ships records + tree placement + the honest label (build-spec option B); the functional cross-uid read primitive (POSIX ACL / ownership) + its readability probe are the separate Erik-present acceptance drill (build spec section 8), where `met` is produced | principal-policy (owns the `NON_RELAXABLE_FILE_GRANT_TIER1_OPERATIONS` sibling const `file_grant` is spread into; this module does not touch the force-lists itself); the OUT-of-scope daily-driver pull surface / `request_file_access` / write-mode grants / synced-folder copies (deliberately NOT built in v1 - see the build spec section 8) | Yes | `mode` is immutable `"read"` in v1 (any other value is a hard schema reject, `FileGrantModeRejectedError`); namespace `_file_grants` (reserved - the agent-facing `state_read`/`state_list`/`state_delete`/`state_export` MCP tools reject it via the reserved-namespace firewall in `cognitive/tools.ts`, same posture as `_audit`/`_identities`); op strings `file_grant` (force-pinned Tier 1)/`file_grant_revoke`/`file_grant_list` (Tier 3); the reconcile's unreadable-record audit uses a DISTINCT op string `file_grant_reconcile_failed` (nothing was revoked and no access changed, so the revoke op's access-reduction justification does not reach it); the mint's durable pre-place audit uses a DISTINCT op string `file_grant_recorded` (round 4: reverted from a shared-enum `result:"pending"` widening that fanned out to SIEM export + other audit consumers) - it is an `AuditLog` entry `operation` value only, never dispatched to `ApprovalGate.evaluate`/`classifyOperation`, so it carries no tier of its own |
| memory-checkpoint | Cognitive | canonical | Local encrypted, content-addressed checkpoints of the StateStore exportable namespace bundle: `createCheckpoint`, `MemoryCheckpointStore`, retention pruning, optional scheduler, and `sanctuary checkpoint create, list, show, prune, restore`. Restore verifies the harness is parked, seals current exportable state as a forensic checkpoint, prints the poison map, and reconstructs exportable state from a chosen checkpoint. Persists encrypted bundle bytes plus plaintext non-secret metadata under `<fortress>/checkpoints/<id>/`; creation and restore never open a network or fleet-sync path | the four "audit" things (audit posture, audit-log storage, audit/chain hashing, transparency anchoring); castle-wall/export enforcement-event exporter; transparency checkpoints; state_export MCP tool | Yes | HKDF label `memory-checkpoint-v1`; audit ops `memory_checkpoint_created`/`memory_checkpoint_pruned`/`memory_checkpoint_restore`; source `forensic_quarantine`; Tier-1 op `memory_checkpoint_restore`; on-disk `<fortress>/checkpoints/`; CLI verb `checkpoint` |
| policy-engine | Cognitive (declarative authoring) | canonical | English-to-rules compiler (authoring-time) + automated machine gates: slot/egress/budget/commitment-boundary + signed gate-receipts, no human prompt | principal-policy (the human gate) | Yes | tool `soft_warn`; 4 PolicySlot tokens `memory/credentials/plans/outputs`; `COMPILED_POLICY_SCHEMA_VERSION`; egress/budget `event_class` tokens; `GATE_REASON_CODES`; `is_sentinel` flag; gates forbid network/LLM imports (structural test) |
| sentinel | Operational (Sentinels) | canonical | Rule-based behavioral-watcher framework: base class + per-fortress dispatcher + encrypted finding store + registry + concrete watchers under `sentinel/sentinels/` | anomaly-detection (learned drift); `policy-engine/sentinel-role.ts` (a capability flag) | Yes | route `/api/sentinels` (+`/subscribed`,`/findings`); `sentinel_id` tokens (egress-volume, credential-usage, etc.); audit ops `sentinel_finding_emitted`/`_evaluation_failed`; `sentinel-subscriptions.json`. Inner `sentinels/ebpf/*.rs` is a placeholder, NOT the daemon |
| compiled-context | Enforcement-surface (memory integrity) | canonical | Slice B final-artifact screening contract: bounded compiler adapters, exact-byte/policy cache, typed future provenance-clustering handoff, and dispatcher-wired scan outcomes before local or remote intelligence providers | security/InjectionDetector (detection primitives); intelligence selector (sole production assembler); sentinel/auto-trigger (finding consumers) | Yes | `COMPILED_CONTEXT_CONTRACT_VERSION`; `COMPILED_CONTEXT_SENTINEL_ID`; six `CompiledContextOutcome` tokens; hard byte/contributor/metadata/signal/cache bounds; exact authoritative assembler inventory frozen by `compiled-context-structural.test.ts`; findings persist hashes + bounded metadata only, never raw context |
| auto-trigger | Operational (Sentinels) | canonical | Nu-1 escalation ladder: routes findings through a 3-rung escalation (inbox / auto+cancel / fire-now); persists tuned thresholds; promotion recommendations | sentinel/anomaly/honeypot (they detect; this decides) | Yes | route `/api/auto-trigger`; rule-id wire format `sentinel__`/`anomaly__`/`honeypot__` (this IS the AAD); HKDF `l2-auto-trigger-rules-v1`; `AUTO_TRIGGER_AUDIT_OPS` |
| anomaly-detection | Operational (Sentinels) | canonical | Chi-1 statistical pipeline: per-fortress feature extractors, classifiers (rolling-baseline/CUSUM/PSI), encrypted classifier-state store; emits drift findings | sentinel (hand-written rules); the `anomaly-trigger` watcher inside sentinel | Yes | route `/api/anomaly`; HKDF `l2-anomaly-classifier-state-v1`; `anomaly-subscriptions.json`; `anomaly_*` audit ops; numeric-only feature-vector invariant |
| honeypot | Operational (Sentinels) | canonical | Pi-1+ honeypot authoring + runtime: English->TrapSpec compiler, registers bait (http/filesystem/tool_call/credential), fires findings into the sentinel finding store | sentinel (passive observer; honeypot plants active bait) | Yes | route `/api/honeypot` (+`/compile`,`/deploy`,`/traps`,`/tool-traps`,`/credential-traps`,`/traps/<id>`); `TrapClass` tokens; `honeypot_*` audit ops |
| lockdown | Operational | thin/utility | One small status-marker helper: read/write `lockdown/status.json` (active+activated_at+reason) + operator banner. It records a fortress lockdown marker only; it does not enforce a write freeze | principal-policy gate (graduated consent) | Yes | on-disk `lockdown/status.json` shape `{active, activated_at, reason}`; modes 0o600/0o700; the banner string consumed by CLI/UX |
| audit | Cross-cutting (posture + chain crypto) | canonical | Sovereignty Audit Tool (read-only scan -> 0-100 score) PLUS the audit-chain hash/checkpoint primitives (`chain.ts`), the fortress checkpoint signer/resolver binding (`checkpoint-identity.ts`, the `AuditLog` constructor default that closed IC-05) + SIEM export (CEF/OCSF) | the other three "audit" things (see CLUSTER 1) | Yes | tools `sovereignty_audit`/`audit_export_siem`; labels `AUDIT_CHAIN_GENESIS="GENESIS"`, `AUDIT_CHECKPOINT_DOMAIN="sanctuary.audit-checkpoint.v1"`; SIEM `filter_layer` enum l1..l4; result version `"1.0"` + `l1_cognitive..l4_reputation` keys |
| transparency | Cross-cutting (tamper-evident + anti-rollback) | canonical (security-critical) | Sigstore/Rekor anchoring + offline-verifiable enforcement-checkpoint chain + anti-rollback counter-floor. Opt-in, default-OFF, fail-closed | audit (local score); attestation (badge); `audit/chain.ts` (hashes log entries, not checkpoints) | Yes | tsup bin entrypoint `offline-cli.ts` (=> `verify-transparency`); domain labels `sanctuary.enforcement-checkpoint.v1`, `sanctuary.transparency.anchor-commitment.v1`, `...rule-label.v1`; Rekor `/api/v1/log/entries`; `SANCTUARY_TRANSPARENCY_INTERVAL` |
| attestation | Verifiable-Reputation-adjacent / UX | canonical (one Layer-4 stub) | Attestation badge UX: global/per-agent/per-action green/yellow/red/offline/local_only badges from signed events; 9-row failure catalog; degrade-not-destroy | transparency/audit; the host/workload TEE primitive (that lives in `workload-lifecycle/host-attestation.ts`) | Yes | closed enums `BADGE_STATES` + `BADGE_STATE_LABELS` copy; `FAILURE_MODE_CODES` (9); `ATTESTATION_EVENT_TYPE_PREFIX="attestation_"`; owns served `server/public/attestation-reference.html`; `custody-provenance-stub.ts` is a deliberate v1.0 no-op for Key-17 |
| health | Cross-cutting (runtime evidence) | thin/utility (1 file) | `buildHealthEvidenceReport`: assembles an un-scored, un-signed in-process runtime snapshot (wall armed? audit persists? per-layer l1..l4 evidence) | audit (scores); shr (signs+exports); posture (dashboard) - see CLUSTER 2 | Yes | JSON report shape: `layers.l1..l4` keys, `castle_wall`/`audit`/`state`/`egress`/`degradations` fields, `RuntimeStatus` union; reads `StatusResponse` from castle-wall IPC |
| shr | Verifiable-Reputation / Selective-Disclosure | versioned-frozen (v1.0) | Sovereignty Health Report: a signed, versioned, portable capability advertisement an agent presents to counterparties; plus the decommissioning-certificate variant | health (unsigned internal); audit (local score) | Yes | tools `shr_generate`/`shr_verify`/`shr_gateway_export`; schema `shr_version:"1.0"` + `layers.l1..l4` parsed by external counterparties; Ed25519-over-canonical-body; reuses mesh `SignatureScheme` |
| compliance | Compliance (EU AI Act + NIST AI RMF) | canonical | EU AI Act (2024/1689) toolset under `eu_ai_act/`: Annex III classifier, coverage matrix, signed bundle generator (6 MD + manifest + zero-dep PDF), each file hashed + Ed25519-signed. NIST AI RMF 1.0 crosswalk under `nist_ai_rmf/`: maps AI RMF subcategories to the SAME control catalog (the EU matrix's evidence_emitter tool names) and renders a tooling-coverage Markdown via `compliance nist-ai-rmf`; pure data->doc, no server boot/no keychain | audit (sovereignty posture, not regulatory) | No | tools `compliance_eu_ai_act_annex_iii_classify`/`compliance_generate_eu_ai_act_bundle` (the `eu_ai_act` token is ALSO in these wire strings - a dir rename must NOT touch them); CLI verbs `compliance eu-ai-act` / `compliance nist-ai-rmf`; Apache-2.0 SPDX headers; `templates/*` assets |
| evidence-pack | Compliance (law-firm evidence pack) | canonical (slices 1-2 + honesty chokepoint) | HONESTY CHOKEPOINT (`read-outcome.ts`): every read-dependent claim flows through a typed `ReadOutcome<T>` = `Populated`/`EmptyVerified`/`ReadFailed`, and a definitive negative ("none"/"full quarter covered"/"no denials") may be emitted ONLY via `claimFromCompleteRead(Complete<T>, ...)` - a `ReadFailed` is not a `Complete<T>`, so a failed/absent read CANNOT render a false census (unrepresentable at the type level). The DURABILITY of that exclusion is kept by a SOURCE-compiled guard (`_assertReadFailedExcludedFromComplete` in `read-outcome.ts`, which `npm run typecheck` over `src/**` fails on if `Complete<T>` is ever widened to admit `ReadFailed`; `test/` is excluded from tsc, so the guard lives in `src`, not a test file). Every source (audit aggregation, coverage bound, inventory, discrete exports, custody) is a `ReadOutcome`, and the manifest `coverage` is a `{determinable:true,...}|{determinable:false,reason}` union so a failed read never serializes a false `shortfall:false`. DRY-10 CHOKEPOINTS (round 10): (a) ATTESTED-WINDOW COUNTING (`censusOverAttestedWindow` in `aggregate.ts`) is the SINGLE construction site for the (counts, coverage) pair, so decision counts are taken ONLY over `[covered_from, covered_to_exclusive)` and never the wider calendar quarter - `buildEvidencePack` no longer pairs `aggregateQuarter` with `detectShortfall` itself, which is what let a signed report print "Total recorded audit operations: 1" beside "the counts above are therefore zero for the attested window"; a zero-width window now yields zero counts and a not-determinable window yields a `read_failed` aggregation, both by construction. (b) READ-FAILURE DISCLOSURE (`sanitizeReason` + `describeReadFailureCause` in `read-outcome.ts`): `ReadFailed.reason` is a BRANDED `SanitizedReason`, so `readFailed()` is the only constructor and no raw `Error.message` (absolute fortress path, username, uid/gid) can reach the signed report or manifest; the CLI additionally maps causes to a CLOSED category set and keeps raw diagnostics on the operator console only. Durability guard `_assertReasonFieldStaysBranded` fails `npm run typecheck` if the brand is ever dropped. (c) OUTPUT DIRECTORY (`writePackDirectory` in `cli.ts`): the delivered directory is a GENERATED SET (stale conditional exports from a prior run are swept, foreign files are REFUSED not deleted, and the directory is reconciled against the manifest after writing), which makes section 1's signed "each Markdown and export file in this pack is recorded in the manifest" claim true by construction. DRY-11 CHOKEPOINTS (round 11, all four findings were one shape: a claim asserted without consulting what would settle it): (a) HISTORY ATTRIBUTION (`diagnoseHistoryGap` in `history-attribution.ts`) is the SINGLE construction site for every causal claim about why the retained log's history begins where it does - it consults `ever_pruned` (the definitive discriminator) on every path, so the zero-coverage arm can no longer tell a never-pruned fortress "this almost always means earlier entries were pruned" while the sibling arm on the SAME fortress renders "not that entries were pruned"; a fail-closed construction guard throws rather than return a diagnosis its own facts contradict, and the pruning vocabulary is confined to that module (source-scan test). The advice no longer says "raise the retention cap" - the shipped server exposes no way to raise it. (b) PACK-RELEVANT FILE SET (`pack-files.ts`): the writer's ignore-list and the SIGNED section-1 recipe read from ONE closed, named set of inert OS metadata, so a hidden `.counsel-notes.md` can no longer ship unmanifested beside a signed claim that every Markdown/export file is in the manifest. (c) ANCHOR CONFIRMATION (`anchorConfirmationLines` in `sections.ts`): the public-anchor arm is cross-gated on the transparency bundle actually being IN the pack, since anchor receipts are checked AGAINST the checkpoint records that bundle carries. (d) ARTIFACT SCOPE (`artifact-scope.ts`): a total `Record<PackArtifactId, ArtifactScope>` forces every artifact whose scope differs from the reporting quarter to declare it, so a quarter-scoped signed pack can no longer ship a whole-retained-log export or a generation-time inventory silently. UNMAPPED-OP GUARD (`aggregate.ts`): `GATE_DECISION_OP_CATEGORIES` + a source-reading exhaustiveness test - an unmapped `gate_*` decision op surfaces as `uncategorized`, never a flattering fold (closes the slice-1 `gate_approve` root cause). The quarterly law-firm Evidence Pack generator: a NEW calendar-quarter aggregation over audit activity (`aggregate.ts` buckets `AuditEntry[]` into a `YYYY-Qn` window, inclusive-start/exclusive-end, and counts by decision category derived from the shipped `gate_*` operation prefixes + the cross-harness approval resolution op, separating human control-point decisions from automated tiers) plus covered-window shortfall detection (`detectShortfall` flags + discloses BOTH a start-side FIFO-pruning shortfall AND an end-side in-progress-quarter shortfall, capping the attested covered-through instant at min(quarter end, generation time)), the CNA-five-question section renderers with honest coverage-basis + scope-and-limits language and clearly-labeled placeholders for later slices (`sections.ts`), a signed manifest reusing the EU AI Act bundle's Ed25519 discipline (`signer.ts`), and the PDF render via the reused zero-dep writer (`renderMarkdownDocumentsToPdf` in `compliance/eu_ai_act/pdf.ts`). SLICE 2 adds the real AI-tool inventory (`inventory.ts` pure `buildInventorySnapshot` from the hub agent-registry records + configured MCP servers + observe candidates; the coverage-basis + "never a complete census" disclosure prints whether the inventory is empty OR full) and the discrete third-party verification exports gathered into one directory (`cli.ts` assembles the signed transparency bundle via `readPersistedCheckpoints`, the audit-chain JSONL via `exportAuditChain`, each signed into the manifest; the verification appendix references concrete files + the offline `verify-transparency` command). CLI `sanctuary evidence-pack generate --quarter 2026-Q3 --firm "Name"` (`cli.ts`) enumerates READ-ONLY from persisted state, no live connections/network. Pure core (`generate.ts` `buildEvidencePack`, `inventory.ts`, `aggregate.ts`) is I/O-free + hermetically tested | compliance/eu_ai_act (the regulatory bundle whose PDF writer + signing pattern this reuses); transparency (whose bundle/verifier the appendix reuses, NOT reinvents); audit (posture score, not a period report). The pure core NEVER reads a real fortress | Yes | working product name `Sanctuary Evidence Pack` (cover copy); output filenames `00_pack_manifest.json`/`01_evidence_pack.md`/`evidence-pack.pdf`/`transparency-bundle.json`/`audit-chain.jsonl`/`anchor-evidence.json`; CLI verb `evidence-pack generate`; the PDF is deliberately UNSIGNED (integrity lives in the manifest); a populated inventory NEVER carries a completeness/total flag; no per-rule per-flow claim (thesis-gate honesty bound). SWEEP-HARDENED (2026-07-10, rounds 1-2): human vs automated decisions are attributed by the gate's `details.decided_by` field ("human" -> `human_approved`/`human_denied`; else automated `allowed`/`denied`), with `cross_harness_approval_resolved` observational (`other`), so one inbox decision counts EXACTLY ONCE in BOTH directions (no round-1 double-count; `human_denied` is a real producer, not structurally 0 as after round-1); retention shortfall checks BOTH FIFO caps (entries AND on-disk size via `AuditLog.getRetentionUsage`) + the rotation-anchor `ever_pruned` signal, affirming "genuine inactivity" ONLY when the log never pruned and is below both caps; `custody.outbound_denied_by_default` is a `ReadOutcome` rendering "not determinable for this install", NEVER a hardcoded "yes" on an un-walled host; the incident line is "not assessed", never a flattering "none surfaced"; the cover uses evidence-toward framing naming which CNA sections are placeholders (not "answers, one for one"); `pack_version` is `0.1-preview`; the observed-egress column is a "Destination risk class" (elevated/standard) with a legend stating it is NOT a finding that data left the firm (never a bare "Exfil risk: yes"); the exec-summary tool count carries an active/recorded denominator |
| errors | Cross-cutting (typed load errors + untrusted-value diagnostic rendering) | thin/utility (3 files) | Two typed Error subclasses: `ConfigLoadError` + `ProfileLoadError`, carrying path/classification/recovery for fail-loud config and profile loading. PLUS `untrusted-diagnostic.ts`, the CHOKEPOINT for putting a stored/imported/wire-supplied value into any thrown message, error message, or operator log line: `describeUntrusted(value)` is TOTAL (never throws, for any input), BOUNDED IN OUTPUT LENGTH (`MAX_UNTRUSTED_DIAGNOSTIC_CHARS` + the truncation marker, independent of input size; the WORK bound is narrower and its residuals - the engine's own key enumeration, a hostile Proxy's traps, BigInt magnitude comparison - are named in that file's header, not claimed away), and NON-DECEPTIVE (a shortened result carries `UNTRUSTED_TRUNCATION_MARKER`; control characters are escaped so a value cannot forge log lines). A declared type is not evidence here: `const e: StateEntry = JSON.parse(bytes)` is a bare assertion, so `${e.kid}` can hand a deeply nested value to `String()` and overflow the stack WHILE BUILDING the diagnostic, replacing the real failure with an unrelated one (defect `STATE-STORE-ERRMSG-INTERP-01`) | `mesh/errors.ts` and `attestation/errors.ts` (SEPARATE local files); `evidence-pack/read-outcome.ts`'s `sanitizeReason`, which REDACTS operator paths out of a signed document and is a different concern - `describeUntrusted` is not a redactor | Yes | symbols `ConfigLoadError`/`ProfileLoadError`/`describeUntrusted`; classification literals `corrupted`/`schema-mismatch`/`invalid-value`/`unreadable`/`wrong-key` (`invalid-value` = scalar-value typo on a structurally-valid config file: refuse-to-start but NOT quarantined); `.name` strings (used in messages/tests); a short primitive MUST render byte-identically to `${value}` so converting a call site never changes an honest diagnostic |
| entitlement | Cross-cutting (paid-fleet tier gating + license issuance + PR-B activation/enforcement) | canonical (verify/resolve core + PR-1 issuance + PR-B activate/enforce) | Offline Ed25519 entitlement-token verify + tier resolution for the paid fleet control plane, PLUS the PR-1 issuance side: a v2 signed-license schema (`EntitlementClaimsV2`  -  carries `entitledCount`/`featureFlags`/`pricingUnit`/`period`/`graceUntil`/`licenseId`/`issuer`, all signed), the tamper-evident append-only issuance ledger (`ledger.ts` hash-chain + per-row revocation signature + signed VERSIONED head anchor carrying a monotonic `generation`; `ledger-io.ts` load/save + an advisory `withLedgerMutationLock`; `ledger-antirollback.ts` the EXTERNAL master-MAC'd generation anchor in `_meta` that catches an old/empty-snapshot rollback the within-file check cannot), and the `sanctuary license issue/list/revoke` CLI (`cli/license.ts`). PLUS the fleet-billing PLAN CATALOG (`plan-catalog.ts`, pure data + a pure lookup, no I/O/crypto): maps a purchasable plan name (currently just `team`) onto a claim template (`tier`/`pricingUnit`/`featureFlags`/`defaultGraceDays` + the overage-math `entitledCount(extraNodes)` = `TEAM_INCLUDED_NODES + extraNodes`, no dollar amounts anywhere per the public-repo pricing-wording rule) that `cli/license.ts`'s `sanctuary license issue --plan team --extra-nodes N` fills into the SAME `issueLicense` path a raw-flag issuance uses; `--plan` combined with a raw flag it would also fill (`--tier`/`--nodes`/`--features`/`--pricing-unit`) REFUSES loudly rather than silently overriding. No schema change, no new tier token; `token.ts`/`tier.ts`/the domain labels stay untouched. PLUS the PR-B activation + node-count ENFORCEMENT: `activation.ts` (verify a pasted license through the shipped core, persist it in a master-MAC'd `_meta` record, and RE-RESOLVE it against the current clock on every read  -  fail-closed to community on absent/tampered/wrong-key/expired; `decodeIssuerPublicKey` decodes the pinned operator identity key) and `fleet-cap.ts` (the pure NODE-COUNT gate: `resolveFleetCap` maps a resolution + grandfather baseline to a cap, `applyFleetCap` drops nodes beyond the cap from the CENTRAL roster). PLUS the Add-Machine slice's `fleet-capacity.ts` (the pure enrollment-headroom view: `computeFleetCapacityView(cap, activeNodes)` answers active/max/remaining/at_capacity for the `GET /api/fleet/capacity` read and the `POST /api/fleet/enroll-token` advisory pre-check, both in `principal-policy/dashboard.ts`; NEVER the enforcement gate itself, which stays `applyFleetCap`). PR-B wires these onto the DAEMON federation roster (`principal-policy/dashboard.ts`), NOT the wrap process (whose roster is empty by construction). The gate is NODE COUNT on the central management surface ONLY; it NEVER gates a node's Castle Wall, its local dashboard, kill safety, or free policy-push, and preserves the roster's `policy_distribution` rail verbatim. Grandfathering: an existing >5-node fleet keeps its count free indefinitely via a per-fleet baseline captured from the durable `summary.admitted`. PLUS the PR-3 downgrade UX + distributed revocation rail: `revocation-list.ts` (a SIGNED, operator-pushed revocation list of license ids  -  Ed25519-signed via the operator-signed message construction under `sanctuary.fleet.revocation-list.v1`, verified against the PINNED issuer key + a strictly-greater MONOTONIC version like the ledger anchor, persisted master-MAC'd in `_meta`; `isLicenseRevoked` consulted on re-resolve to force a revoked license CLOSED to Community), `revocation-antirollback.ts` (the revocation-verifiability CHOKEPOINT + external anchor. `revocationVerifiability` is the SINGLE fail-closed signal both enforcement callers consult: it fails closed UNIFORMLY on absent-after-a-push, corrupt, and rolled-back-below-floor, keeping a paid grant ONLY on a verifiably-clean list OR an absent list with NO established floor (the legit never-revoked fleet). Effective floor `max(storedListVersion, standalone anchorVersion, custody-MAC'd witness latch)` is > 0 iff a revocation was EVER pushed, and the witness latch (the `revocation_floor` field of the epoch witness in `core/anti-rollback.ts`) survives deletion of BOTH the list file AND the standalone anchor, so a bare delete of the list cannot un-revoke a license. `persistPushedRevocationListSerialized` is the ONE write path both push callers use: an O_EXCL cross-process lock re-reads the floor INSIDE the lock and re-verifies monotonicity, then writes list -> anchor -> witness latch in crash-safe order so two concurrent pushes cannot interleave. A transient IO read error is retried/graced (`storage/transient-read.ts`), never laundered into corrupt. Residual: deleting list + anchor + master-MAC'd epoch witness on a never-rotated offline fortress is the SAME full-wipe residual `core/anti-rollback.ts` documents, bounded by Stage 2/4), `downgrade-log.ts` (an append-only, master-MAC'd, operator-readable log of every tier/cap transition with reason + affected node ids, bounded to `MAX_DOWNGRADE_LOG_ENTRIES`), and `re-resolve.ts` (the scheduled re-verify orchestration: `runFleetReResolve` re-checks expiry + revocation, applies AUTO-CAPTURE of a never-activated over-cap fleet's grandfather baseline once/grow-only (grow-only enforced at the `writeFleetActivation` persistence layer + the daemon tick serialized by an in-flight latch so overlapping ticks cannot lower the baseline), and logs transitions via pure `diffCapTransition`). PR-3 wires these onto the daemon (`principal-policy/dashboard.ts`): an hourly re-resolve tick, `GET /api/fleet/status` (banner), `GET /api/fleet/downgrade-log`, `POST /api/fleet/revocation-list`, plus CLI `sanctuary federation revoke-push`/`downgrade-log`. PLUS the signed compliance-ATTESTATION export: `compliance-attestation.ts` (pure `buildAttestationBody` / `serializeSignedAttestation` / `verifySignedAttestation` composing already-resolved posture facts - roster counts, resolved cap, policy-distribution rollup, license status - into ONE canonical, operator-signed, OFFLINE-verifiable document; signs with the SAME default operator identity `license issue` uses, no new key) and the `sanctuary fleet attest export|verify` CLI (`cli/fleet.ts`, sharing the custody-unlock helper extracted to `cli/custody-unlock.ts` so `license.ts` and `fleet.ts` never copy-paste the custody helper). THE HONESTY INVARIANT: the attestation is an OPERATOR SELF-ATTESTATION of the fortress's OWN locally-verifiable posture at a point in time - it is NOT a third-party audit and NOT a per-flow rule-attributed audit trail (the unbuilt C4 build); the `claims_scope` field on every document and all CLI copy say so plainly, verified by an overclaim-guard test. FAIL-CLOSED: absent/invalid/expired/tampered/malformed/REVOKED all resolve to the COMMUNITY tier/floor and never grant a paid tier; a tampered OR rolled-back ledger reports `tampered` and is never trusted. `list` pins the issuer key from the fortress (NOT the ledger file) in verified mode, and labels an unverified (no-passphrase) read `UNVERIFIED` rather than claiming valid. NOT built yet: a ledger re-baseline path (restore-attest analogue), billing/self-serve | NOT an enforcement gate for SECURITY (only central-management node count; never the wall/local-dashboard/policy-push/kill-safety); NOT identity/attestation of an agent (verifies an issuer-signed grant, not an agent identity); NOT a third-party audit (the compliance-attestation export is a SELF-attestation, never claim otherwise); ledger `revoke` is LOCAL issuer bookkeeping, DISTINCT from the pushed `revocation-list.ts` rail; the daemon-roster wiring + routes/tick live in `principal-policy/dashboard.ts`, not here | Yes | domain labels `sanctuary.fleet.entitlement.v1` (FROZEN) + `sanctuary.fleet.entitlement.v2` (FROZEN); ledger revocation domain `sanctuary.fleet.ledger.revocation.v1`; ledger head domain `sanctuary.fleet.ledger.head.v1` (v1/v2 versioned by field-set, label frozen); external generation-anchor MAC domain + `_meta` key `fleet-license-ledger-generation-anchor-v1` (additive: absent reads as gen 0; monotonic write-guard; wrong-key/tampered fails toward tampered, never bricks); activation MAC domain `sanctuary.fleet.activation.v1` + `_meta` key `fleet-activation-v1` (additive: absent reads as no-license; wrong-key/tampered fails toward community, never grants); revocation-list sign action `sanctuary.fleet.revocation-list.v1` (FROZEN) + record MAC domain `sanctuary.fleet.revocation-list.record.v1` + `_meta` key `fleet-revocation-list-v1` (monotonic version; verified against PINNED key; a PRESENT-but-corrupt list is FAIL-CLOSED at the enforcement callers - a paid grant drops to Community, an absent list keeps it); revocation-version anchor MAC domain `sanctuary.fleet.revocation-list.version-anchor.v1` + `_meta` key `fleet-revocation-list-version-anchor-v1` (external monotonic floor so a corrupt list cannot roll the version back to 0; additive: absent reads as 0); the revocation FLOOR LATCH reuses the EXISTING epoch-witness MAC (no new HKDF label) - it is the additive `revocation_floor` field of `core/anti-rollback.ts`'s custody-MAC'd epoch witness (`custody-epoch-witness-v1`), so the "a version-N revocation existed" proof survives deletion of both the list file and the standalone anchor; the cross-process push lock is `_meta/fleet-revocation-list.push.lock` (O_EXCL, fail-closed); downgrade-log MAC domain `sanctuary.fleet.downgrade-log.v1` + `_meta` key `fleet-downgrade-log-v1` (append-only, bounded, no secrets); compliance-attestation schema domain `sanctuary.fleet.compliance-attestation.v1` (FROZEN - the ONLY new frozen token this slice adds; signing reuses the EXISTING default-operator-identity key, no new HKDF label); `COMMUNITY_FREE_NODE_CAP=5`; closed tier set `ENTITLEMENT_TIERS` (community/team/fleet/enterprise) with `COMMUNITY_TIER` as the floor; `resolveEntitlement`/`resolveActivation`/`activateFleet`/`runFleetReResolve`/`isLicenseRevoked`/`verifySignedAttestation` never throw + never return key material; issuer secret decrypted only transiently in `sign()` (never printed/logged); Ed25519-over-canonical-JSON, offline, no network |
| http | Cross-cutting (HTTP safety helpers) | thin/utility | Shared HTTP response helpers for production route surfaces, including the caught-exception error envelope that logs redacted operator diagnostics while returning stable public codes | console/auth-middleware (auth only); dashboard/console route modules (callers) | Yes | `PublicErrorCode` literals; `sendCaughtError` must never serialize `err.message`, stack, or `String(err)` to a client (hard invariant); operator-log redaction is best-effort allow-list (Bearer tokens, keyed secrets, PEM private-key blocks), not fail-closed |
| security | Enforcement-surface (injection) | canonical (single file) | The `InjectionDetector`: fast, zero-dep, never-throws scanner of tool args for prompt-injection signals; returns allow/escalate/block; outbound secret-leak scanning | NOT the approval gate (principal-policy calls this); NOT crypto (core). Name overpromises | Yes | symbols `InjectionDetector`/`DetectionResult`/`InjectionSignal`; the never-throws invariant; SEC-034/035 (Unicode sanitization + decoded re-scan); detection PATTERN strings are behavior. 1,353-line god-file split candidate (Phase 5) |
| mesh | Networking | versioned-frozen (v1) | The intra-operator mesh fabric (internally titled "Sanctuary Federation Protocol v0.1"): libp2p transport, node lifecycle/join, guardian, v0.1 trust-root, additive hybrid trust-root v2 cert-chain module, audit-batch sync, recovery flows | federation (inter-instance); `v1/federation.ts` (HTTP wrapper around mesh) | Yes | `PROTOCOL_VERSION='0.1'`; libp2p protocols `/sanctuary/fed/v0.1/...`; HKDF `sanctuary-fed-v0.1-transport` / `...-audit-chain`; `SIGNATURE_SCHEME_V1='ed25519-v1'`; event-type + capability-bit masks; `NodeMode` union |
| federation | Networking | canonical | MCP-to-MCP peer federation between two SEPARATE Sanctuary instances: in-memory peer registry (handshake-enrollable only) + three `federation_*` MCP tools. 3 files, 665 lines | mesh (intra-operator); `v1/federation.ts` (no import edge - verified) | Yes | tool names `federation_peers`/`federation_trust_evaluate`/`federation_status` (NOTE: live third tool is `federation_status`, not the `federation_exchange_reputation` the scoping doc implied) |
| v1 | Networking | versioned-frozen (v1) | The versioned HTTP `/v1/*` API: session ceremony, status, agents protect/unprotect, `/v1/federation/*` admin + join ceremony, `/v1/nodes`; the operator-authority event log includes node eviction and signed policy-bundle records. Fail-closed: single generic 401 | federation/ (the dir) - v1's federation routes wrap MESH, not the federation/ registry (the real "split-brain") | No | frozen routes `/v1/status`,`/v1/session/init|complete`,`/v1/agents/protect|unprotect`,`/v1/federation/enable|disable|status|authorize/*|sync`,`/v1/nodes`; event kinds `node_eviction`/`operator_policy_bundle`; domains `sanctuary.v1.operator-signed`/`sanctuary.v1.federation-policy-bundle`; uniform-401/generic-403 behavior |
| coordination | Operational | canonical | Local-only agent-to-agent handoff inside ONE fortress: signed handoff record lifecycle, encrypted HandoffStore + L2 audit chain, context-slot transfer, handoff SSE routes | hub (the aggregator); mesh (node transport) | Yes | route `/api/coordination/handoffs` (+`/stream`,`/:entry_id`); signing `ed25519-v1`; handoff status enum + `previous_status:'created'` chain convention; L2 records keyed off contracts/v1.1 |
| hub | Operational | canonical | The Operator Hub API: aggregates agent-registry + unified approval inbox + activity feed + fortress-scope Tier-1 control, served by HubService under `/api/hub/*` | coordination (point-to-point handoff); console/dashboard (render); principal-policy (owns the gate hub enqueues into) | Yes | `HUB_ROUTES` (/api/hub/inbox, /agents, /activity, /fortress/lockdown, /chat/concierge, ...); `HUB_INBOX_ACTIONS` ['approve','deny','dismiss']; Tier-1 `['unwrap','lockdown']` must stay lockstep with principal-policy loader; `HUB_FORTRESS_AGENT_ID_SENTINEL='all'` |
| composition | Cross-cutting | default-off-allocated | Opt-in, DEFAULT-OFF Concordia+Verascore composition: sidecar lifecycle manager, degrade monitor, receipt/mandate adapter, verascore hook, pipeline orchestrator. Sidecar crash never halts the fortress | the unrelated `composition` key in index.ts's SIM descriptor (name collision) | Yes | `COMPOSITION_CONFIG_NAMESPACE='_composition'`; HKDF `sanctuary-composition-v1`; `COMPOSITION_EVENT_TYPES` (additive-only per federation §10.3); default-off toggle. NOT dead despite just 1 static importer (`concierge-query-grammar.ts`) |
| recovery | Cross-cutting | canonical | The guardian-threshold Recovery Cascade (WP-MVP-8): DMswitch operator-absence evaluator, guardian roster, threshold evaluator + signed approvals, cascade state machine, multi-principal boundary | `mesh/recovery-flows` (wire ceremonies); `core/master-rotation` (rotates the key); wrap recovery-key (per-wrap unlock) | Yes | `RECOVERY_SIGNATURE_SCHEME='ed25519-v1'`; `RECOVERY_EVENT_TYPES`; `RECOVERY_ACTIONS`; `RECOVERY_GATE_REASON_CODES`. Imports canonical-JSON + guardian types FROM mesh (a real edge, not a dup) |
| castle-wall | Enforcement-surface (Cognitive) | canonical | The IN-SERVER (TypeScript) Castle Wall surface: wire constants, allowlist schema, IPC framing, audit-event builder/consumer (producer-sig verify), daemon client/installer, in-process egress CONNECT proxy. `runtime/enforcement-availability.ts` is the v3 macOS availability chokepoint: verifies producer-signed extension-originated reports, stamps `observed_at` on daemon receipt, resolves least-green-of-fresh across live connections, and backs `castle.sock` availability queries; daemon intent/status files/heartbeats never mint green. `subject-binding.ts` is the protection-claim subject chokepoint: derives canonical `fortress/uid-N` subjects, rejects legacy audit-token evidence under exact matching, and confines the named `fortress_scoped` mode to daemon/coarse-wall liveness facts rather than per-agent enforcement claims. `audit/emission-liveness.ts` (Slice M, 2026-07-17) is the emission-liveness watchdog: a pure decided-vs-emitted divergence detector the macOS daemon feeds (`flow_decision_recorded` receipts + gated egress-probe attempts) and ticks; when decisions keep arriving with zero persisted emissions past a grace window it fails LOUD (greppable `[slice-m-emission-stall]` stderr line + `audit_emission_stall`/`_recovered` chain entries), never fires at zero traffic, stands down with the wall on arm-lease revoke, and piggybacks its own liveness snapshot (incl. `last_evaluate_at_ms`) onto every `castle_wall_heartbeat`. HONEST BOUND: it CANNOT fire on the exact 07-17 stall signature (there the sysext stops reporting decisions entirely, so the detector receives neither decisions nor emissions and stays quiet; the Swift `handleNewFlow` decided-counter feed and the probe-correlated-with-its-own-audit-entry instrument are owed follow-ups), and it does NOT close Slice M. `observe/` (v1, 2026-07-07) adds the Observe / Learn Allow-List engine: a pure fold (`fold.ts`, denied-flow audit events -> deduped `CandidateObservation` rows) + an idempotent, allowlist-aware refresh chokepoint (`refresh.ts`, 2026-07-14 sweep R3-1 + Codex-gate hardening: a persisted fold watermark over `streamVerifiedChain`'s authenticated chain sequence, BOUND to the chain identity by the entry hash at that sequence, folds every event exactly once -- refreshes never inflate `times_seen`, promoted/discarded candidates are never resurrected from stale history, and a reset/regrown chain triggers a recompute instead of a silent prefix skip -- with cross-process mutual exclusion (injected `RefreshLock`; the CLI's O_EXCL `.observe-refresh.lock` with stale-holder break), a review-marker-aware recompute-heal for pre-watermark stores (existing rows healed from full history; NEW rows minted only from events after the chain's last discard/promote audit marker, so pre-upgrade discards do not resurrect), and a CROSS-ENFORCER-CONSERVATIVE verified-allowlist filter+prune (the three shipped enforcers diverge on precedence/scope/pattern syntax, so the verdict never reconstructs any one of them: a generous UNION deny/prompt veto -- any deny/prompt whose match-clause could fire under ANY enforcer's syntax keeps the row -- plus an EXACT unconditional scope-covering allow on the axes every enforcer agrees on: catch-all destination, exact host, or exact ip, i.e. exactly the shape promote synthesizes; pattern/cidr allows leave rows pending, disclosed conservative staleness; a rule scoped to template A never suppresses template B's identical destination; `ruleScopeCoversAgent`/`ruleProtocolMatches` shared from `allowlist/match.ts`, destination/protocol proxy matcher extracted from `egress-proxy.ts`) so observe/ still never imports the evaluator module) + pure synthesis (`synthesize.ts`, candidate -> `AllowlistRule` via the existing `validateRule`, exact-host default per D-Q2) + an encrypted, agent-invisible candidate store (`store.ts`, reserved `_castle_wall_observe` namespace) + a Tier-1-gated promote orchestration (`promote.ts`) that ONLY calls its injected `publish` after an injected `approve` returns allowed, and that carries the existing ruleset forward ONLY through an injected `readVerifiedManifest` (the CLI verifies the on-disk manifest's Ed25519 signature + per-rule sha256/`validateRule` via `allowlist/parse.ts` and fails closed on tamper), with a publish-time digest compare-and-set that aborts if the manifest changed since the approval basis; the Tier-1 approval context shows each SYNTHESIZED effective match (not the raw observed host:port) so a widening is never signed unseen. ENFORCEMENT REACH (2026-07-27 fix, Option A + fix-round): promote publishes its rule FILES into `policy/egress/rules/` -- the ONE TRUE rule source the macOS signing daemon composes from (`runtime/macos-daemon.ts` `loadManifestState`, exported for exactly this end-to-end test) and the Linux enforcer resolves manifest entries against (`policy_dir/rules/<file>`) -- never beside `manifest.json` (the pre-fix dead-drop no enforcement path read); promote routing follows the fortress's exclusive-routing marker via `resolvePromoteRouting` (marker present => the promoted rule is scoped to the GATE principal, `scope.uids=[gate_uid]`, the same axis provisioned rules use, because a template/agent-scoped promoted rule in the composer's source is an agent-reachable off-box allow the exclusive assertion refuses by throwing), re-resolved AFTER the Tier-1 approval returns (F5 marker TOCTOU: a mode flip during the approval wait aborts `routing_changed`, nothing published); a same-id previously-promoted rule whose SCOPE disagrees with the current routing resolution is REWRITTEN to the current-mode synthesis on re-promote (F3 mode-transition residue; `ruleScopesEqual`), never silently carried forward under a success message; the CLI publisher's orphan-cleanup owns files by MEMBERSHIP in the previous promote-signed manifest (F4; never by name-prefix, so provisioned/operator files -- even one named `derived-observe-*` -- are structurally undeletable by a promote); `allowlist/match.ts:ruleScopeCoversAgent` treats a uids-ONLY scope as NOT covering the agent (F2), so a gate-scoped rule the gate daemon has not loaded never suppresses its still-denied candidate from `observe candidates`; EXCLUSIVE-MODE HONESTY BOUND (F1): the gate daemon loads its per-uid destination rules snapshot (`/var/db/sanctuary/gate-runtime/<uid>-rules.json`, root-written at G4 arming) once at start, so an exclusive-mode promote states plainly that the destination becomes reachable at the next re-arm (`sudo sanctuary protect --repair-egress-gate`) and KEEPS the candidate rows pending until then (gate 403s are stderr-only, never audit-folded, so a removed row would have nothing to re-mint it); a rule file found only at the legacy beside-manifest location, or missing from rules/ entirely, is an explicit named-remedy refusal, never a silent fallback read or a bare "tampered" (F8). ROUND-3 HARDENING (2026-07-27 re-gate): a cross-process PUBLISH LOCK (widened in round 4 to the shared `.egress-policy.lock`; same O_EXCL never-auto-broken discipline as the refresh lock) is held across the publish-time re-read + digest CAS + publish/cleanup, so two approved promotes can never clobber each other's signed manifest (the loser aborts `publish_in_progress`, nothing published -- R1); on an exclusive fortress promote ALSO re-scopes ORPHANED observe-promoted rules with no surviving candidate (empty selection allowed; the coarse-era promote consumed its candidate, which left the bricked compose with no product path out -- R3), and the exclusive-violation reason NAMES that recovery for `derived-observe-*` rules; an all-already-promoted selection is an honest no-op (`already_promoted`, no Tier-1 ceremony, no re-sign -- R2); the candidates listing marks gate-covered rows "promoted, awaiting re-arm" (`candidatePromotedAwaitingRearm`: destination legs shared with the suppression verdict, and the covering rule must carry the observe id prefix AND bind to the CURRENT marker's gate uid, so provisioned or stale-gate rules never mark a row; also an additive `promoted_awaiting_rearm` field in the `--json` listing) and the exclusive copy states the rows persist until operator discard; duplicate `--destination` flags dedupe at parse (R5). ROUND-4 SEAM FIXES (Codex re-gate): the promote lock is now the fortress-wide EGRESS-POLICY WRITER lock (`.egress-policy.lock`, single source `provision/lockfile.ts` `egressPolicyWriterLock`) that provisioning's publish/scrub/restore (`provision/egress.ts`) ALSO acquire, so no two writers of the shared `policy/egress/rules/` + `manifest.json` pair can interleave (the manifest-digest CAS is blind to rule-file-only mutations; root and operator writers share the O_EXCL file, unlink governed by the operator-owned fortress dir), and the published rescope set is re-derived from the CURRENT verified rules INSIDE the locked section (C1); every promote-signed manifest carries EXACTLY ONE genuine derived habeas local lane (C2: the Rust daemon's composed-manifest gate refuses a lane-less manifest, so a fresh promote used to publish a manifest Linux enforcement rejects while reporting success -- lane derived via the shared `deriveHabeasDistressRules`, and the two `rules/`-directory readers, `loadManifestState` + `readEgressRulesFromDisk`, SKIP the byte-genuine lane file via `isGenuineDerivedHabeasRule` since they derive their own lanes and the file exists only for the Linux `rules/<file>` resolution contract; a claimed-but-non-genuine habeas id still hits the reserved-namespace firewall; and round-5 RC1: the lane-presence decision uses the GENUINE-lane predicate, never id presence, so a signed basis carrying a schema-valid but non-genuine reserved-id rule is REPAIRED -- the claimer is dropped from the signed set, its orphaned file swept, and the genuine derivation ensured -- never re-signed into a manifest the Rust gate rejects); rescope eligibility is MEMBERSHIP-bounded (C3: only rules the promote-signed manifest itself owns, never a rules/-directory scan, so an operator-authored `derived-observe-*` file is untouchable, and the exclusive-violation remedy distinguishes promote-owned from operator-authored) (test/castle-wall/observe/promote-enforcement-reach.test.ts). THE RED-LINE INVARIANT: the enforcing evaluator (`egress-proxy.ts`) never reads the candidate store; observing never becomes allowing except through the explicit, human-approved promote. CLI: `sanctuary castle-wall observe start\|status\|candidates\|promote\|discard` (`cli/castle-wall-observe.ts`). `provision/` folds the auto-provision-agent-account flow (Build 1 / step 2: detect shared-account, plan+create the dedicated OS account, re-home adapter, arm-time uid-existence gate, verify-before/after-arm, unprovision) into `sanctuary protect`; `provision/egress.ts` provisions the confined agent's OWN network egress in the same flow (per-uid signed `provisioned-<harness>-*` allow rules for the declared `HarnessEndpointSet`, a static pre-arm parity verify through the same TS matcher + #380 derived-DNS presence, an as-uid post-arm probe, refuse-to-arm fail-closed so a mis-provisioned wall never confines the agent into non-functionality; audit ops `egress_provisioned`/`egress_provision_refused`/`egress_probe_failed`), pure-plan + injected-ops so the privileged logic is unit-tested against mocks; real `sysadminctl`/`dscl`/`launchctl` side effects live in `wrap/auto-provision.ts` and are drill-only. `allowlist/exclusive-routing.ts` (Slice 5 S5-4, 2026-07-16) adds the EXCLUSIVE ROUTING manifest composition (design BLOCKER-1; requires the merged + drill-proven S5-0 two-confined-uid model): in exclusive mode the provisioned endpoint rules are RE-SCOPED AWAY FROM THE AGENT to the gate principal (`scope.uids = [gate_uid]`, via `provision/egress.ts`'s new `ProvisionedEgressRouting`), the derived gate-channel rule binds to the AGENT principal (`gate-derivation.ts`'s additive `scope_to_agent_uid` option), and a COMPOSE-TIME ASSERTION (`assertExclusiveRoutingComposition`) fails CLOSED (no manifest, no arm) if ANY agent-reachable direct off-box endpoint allow survives - so the agent's only sanctioned off-box path is the gate. The assertion classifies each allow by exact shape (never id-trust): the genuine habeas lanes + the agent-scoped gate rule are justified lanes (the habeas webhook is an EXPLICITLY documented residual), provably-loopback allows are inert, the derived DNS rule is checked like any rule (its parent-union scope binds to the gate principal, so the agent gets NO direct DNS - the gate resolves), and anything agent-reachable is a violation. The COARSE-ONLY fallback (`composeExclusiveRoutingRules` mode `coarse-only`) exists ONLY as an explicit audited mode: it refuses gate-scoped provisioned residue ("never both models at once") and REQUIRES a successful audit emission with the DISTINCT op `exclusive_routing_coarse_fallback` (never a widened shared enum) before returning; S5-P renders that coarse-only state non-green on every surface (the design ordering rule). macOS-ONLY (`scope.uids` is the S5-0 macOS axis; the Rust daemon fail-closed refuses uids rule files by design). Cross-language parity: `test/castle-wall/fixtures/manifest-parity-vector-exclusive-routing.json` (regenerable via `scripts/gen-exclusive-routing-parity-fixture.ts`) is verified byte-for-byte by BOTH the TS `manifest-parity-vector.test.ts` and the Swift `ManifestParityVectorTests.swift`. HONEST BOUND: the composition is WIRED (S5-6): the signing daemon (`runtime/macos-daemon.ts`) routes EVERY manifest compose through it when the exclusive-routing marker is present, and the install/repair degrade path routes the audited coarse-only fallback; it signs/arms nothing itself and advances no capability claim (S5-DRILL owed). `allowlist/routing-marker.ts` + `provision/exclusive-arm.ts` (Slice 5 S5-6, 2026-07-17) add the durable exclusive-routing MODE MARKER (`<fortress>/policy/egress/exclusive-routing.json`: written at the exclusive bring-up's manifest publish, removed by the coarse restore/unprotect; PRESENT-but-malformed THROWS so the daemon refuses to compose rather than guess a mode -- absent = coarse, byte-unchanged) and the PURE exclusive-egress arming/repair/boot sequencing layer (`runExclusiveEgressArming`: bring-up -> S5-5 release barrier -> outcome, with the degrade-loud coarse fallback + distinct `exclusive-egress-unarmed-coarse-active` outcome on every failure; `runEgressGateRepair`: the MED-7 drift-guard-first repair verb, foreign transient pf rules REFUSE without the TTY-only audited `--override-transient-pf-rules`; `runBootExclusiveEgressRelease`: per-agent fail-closed boot release that never throws), all over injected ops; `runProvisionFlow` gains the `fineGrainedDeclared` mode (fail-closed preflight + parked-install assertion + 1:1 outcome mapping) and the production side effects live in `egress-gate/arming-wiring.ts`. `provision/exclusive-unprotect.ts` (Slice 5 S5-7, 2026-07-18) adds the PURE per-agent UNPROTECT sequence (`runEgressGateUnprotect`, CLI `sudo sanctuary protect --unprotect-egress-gate --stand-down-agent`): verified persistent park FIRST -> generation recovery -> gate-daemon down (loud) -> credential/oracle teardown -> policy surfaces off -> provisioned-rule scrub -> registry remove LAST (remaining uids re-verified live, flush only when the last agent leaves, removed generation folded into the persisted floor); every failure is an honest staged outcome with a VERIFIED parked claim, remaining protection stays intact, re-run converges (idempotent); makes Slice 5 code-complete (S5-DRILL owed; account deletion + coarse-wall disarm deliberately out of scope -- `unprovision.ts` owns those). `provision/fortress-custody.ts` (fortress-ownership spec 2026-07-30) is the fortress custody repair + never-root-owned normalize chokepoint: one shared walk/plan/apply semantic (lchown, fd-pinned dev/ino identity re-check before fchown, foreign-uid entries skipped and reported, mid-walk vanished entries tolerated as notes, symlinks never descended or chmod'd) behind BOTH `sudo sanctuary castle-wall repair-custody` (`cli/castle-wall-custody.ts`: observe-first versioned manifests under `/Library/Application Support/Sanctuary/custody-repair/` written O_EXCL before any mutation, `--rollback <manifest>` replays recorded ownership, exit codes 0 changed / 3 already-clean / 2 refused / 1 failed) AND `normalizeFortressCustody`, called at the tail of every euid-0 flow that touches the fortress (protect via `finishProvisionOutcomeWithCustodyNormalize`, sudo arm/disarm, install-boot) so an install can never silently leave a root-owned `~/.sanctuary` behind (the Mini2 2026-07-30 stuck-at-undetermined root cause); also hosts the R2/G4 `resolveSudoIdentityDecision` sudo-identity chokepoint (moved verbatim from `wrap/auto-provision.ts`, which re-exports it). `export/` (v1, 2026-07-10) adds the OPERATOR-side enforcement-event exporter: a FROZEN public event schema `sanctuary.enforcement-event.v1` (`schema.ts`, three closed classes egress_decision/policy_change/distress, METADATA-ONLY) + a CLOSED allowlist mapping (`map.ts`) that constructs each event field-by-field and NEVER spreads raw audit `details` + safe-by-default sinks (`sink.ts`, file/stdout `FileExportSink.touchesNetwork===false`; outbound `HttpCollectorSink` fails loud, no redirect-follow, no secrets in payload) + a Tier-1-gated + pinned-destination `EnforcementExporter` (`exporter.ts`) that only arms the HTTP push after an injected `approve` returns allowed and fails closed otherwise. The streaming slice (2026-07-10) adds a verified-chain consumer (`stream.ts`, `EnforcementExportStreamer`) driven off `AuditLog.streamVerifiedChain` with a DURABLE, MASTER-KEY-AUTHENTICATED CURSOR (`cursor.ts`, `FileExportCursorStore` at `<fortress>/state/cortex-export-cursor.json`, schema `sanctuary.enforcement-export-cursor.v2`; advanced+persisted only AFTER a batch is confirmed delivered, so a crash resumes BEHIND the delivered frontier = at-least-once, no gap, no re-send storm; the record is MAC'd with a master-key purpose key and BOUND to the chain identity by the `entry_hash` at the cursor sequence, so a hand-written/tampered/stale-high/wiped-chain cursor is DISCARDED fail-LOUD via `_cursor_reset` + a re-scan from the start rather than a silent zero-forward that blinds the console -- 2026-07-16 sweep fix 1) + bounded batch RETRY that re-throws + records `_retry_exhausted` after the budget (never a silent drop, never a fallback lane), an operator config-file loader (`config-loader.ts`, `<fortress>/policy/egress/cortex-export.json`, fail-closed), and a `sanctuary cortex-export run` CLI verb (`cli/cortex-export.ts`) whose default is file/stdout (no network, no approval) and whose outbound push routes through the Tier-1 gate. Feeds a Palo Alto Cortex marketplace content pack (`Review/Strategy/PANW_Cortex_Content_Pack_Scoping_2026-07-10.md`) | the native enforcers castle-wall-daemon/macos/vmm at repo root; fortress (posture); lockdown (flag); `observe/` is distinct from the LIVE signed ruleset (`allowlist/manifest.ts`) it eventually feeds -- a candidate is never itself a rule until promoted; `egress-gate/harness-daemon.ts` (the daemon plumbing `provision/` wires, not reimplements); `export/` is distinct from the agent-callable REDACTED `audit_export_siem` (`audit/siem-tools.ts`) -- export/ is the operator full-fidelity-metadata push, gated Tier-1 | Yes | cross-language wire constants that MUST byte-match the Rust daemon: `CASTLE_WALL_PRODUCER_SIG_DOMAIN_PREFIX`, `..._KEY_ID_V1="cw-audit-producer-v1"`, `CASTLE_WALL_AUDIT_LAYER="l1"`, `CASTLE_WALL_AUDIT_PROVENANCE_KEY/VALUE`, `CASTLE_WALL_IPC_NAMESPACE`, Content-Length header; `observe/` reserved namespace `_castle_wall_observe` (agent-facing `state_*` tools reject it, `cognitive/tools.ts`); force-pinned Tier-1 op `castle_wall_observe_promote` (`NON_RELAXABLE_CASTLE_WALL_OBSERVE_TIER1_OPERATIONS`, principal-policy/loader.ts + gate.ts); `provision/`'s dedicated account name convention `sanctuary-<agent>`; account-REMOVAL auto-delete is explicitly OUT of `provision/unprovision.ts` (Erik-present, separate PR); `export/` FROZEN public schema string `sanctuary.enforcement-event.v1` + distinct local audit ops `enforcement_export_enabled`/`_emitted`/`_refused`/`_cursor_advanced`/`_retry_exhausted`/`_cursor_reset` (never widen a shared enum) + force-pinned Tier-1 op `enforcement_export_enabled` (`NON_RELAXABLE_ENFORCEMENT_EXPORT_TIER1_OPERATIONS`, loader.ts + gate.ts) + durable AUTHENTICATED cursor file `<fortress>/state/cortex-export-cursor.json` (schema `sanctuary.enforcement-export-cursor.v2`, master-key-MAC'd + chain-identity-bound) + operator config `<fortress>/policy/egress/cortex-export.json` + CLI verb `cortex-export` |
| egress-gate | Enforcement-surface (Cognitive) | canonical (drill-acceptance pending) | The exclusive-egress enforcement core (Unified Protect Slices 1-4+8): loopback-TCP policy gate (reuses the egress-proxy evaluator), per-uid pf loopback anchor + fail-closed liveness check, advisory lsof peer-uid recovery, agent-harness `UserName` LaunchDaemon plumbing, NEFilter/pf policy-parity guard. `anchor-registry.ts` (Slice 5 S5-1, 2026-07-15) adds the LOCKED MULTI-UID pf-anchor registry: the root-owned single source of truth for the shared `sanctuary.egress-gate` anchor's contents (one entry per confined uid), so the anchor is only ever mutated as the lock-serialized UNION of all confined uids -- a second uid never overwrites the first's rules and unprotecting one agent never strips another's (the review HIGH-4 destructive-full-replace flaw). Every mutation runs under the shipped O_EXCL `provision/lockfile.ts` lock, journals a `pending` desired-set BEFORE touching the anchor, arms the union via the arm-equivalent `armPfAnchorUnion` (hook+enable+settle+reference-counted `-E`, `pf-anchor.ts` -- never a bare `-f` that leaves rules unhooked), verifies EXACT-union liveness (`checkPfAnchorUnionLiveness`: the anchor holds exactly the union, no stray permissive rule), rolls back to the previous committed union on failure, and marks itself `dirty` (posture MUST read red) if rollback also fails; reconcile-on-entry re-asserts a drifted committed union before layering a new mutation. Flush (`disarmPfAnchor`'s `-F all`) is permitted ONLY when the last uid leaves; a structural test forbids any non-owner egress-gate module from calling the raw arm/disarm/flush. HONEST BOUND: this LANDS the safe-mutation primitive; install-time arming now routes through it via S5-6's `arming-wiring.ts` (the only production anchor-mutation path), with HIGH-4 closure evidence S5-DRILL-owed. `generation.ts` (Slice 5 S5-2, 2026-07-15) adds the GATE-PORT GENERATION STATE MACHINE + bind-first helper: each exclusive-egress bring-up is a monotonic-per-agent generation staged through G1 (bind-first ephemeral port -- bind `127.0.0.1:0` and learn it before any rule references it, closing the pre-bind squat window) -> G2 (owner check) -> G3 (pf load via the registry at the staged port) -> G4 (publish the generation-bearing gate policy) -> G5 (commit = remove the staging record), with a crash-recovery rule for each transition. An uncommitted crash BLOCK-ONLY TOMBSTONES the uid (drop the stale gate pass, keep the four block-drops so non-gate loopback stays closed -- `renderPfAnchorRulesForUids`/`checkPfAnchorUnionLiveness`/`armPfAnchorUnion` + the registry `tombstone()` mutation thread an additive `PfAnchorUnionEntry.tombstone`), never dropping the whole entry. `evaluateGenerationMatch` refuses traffic unless the pf pass port == manifest port == committed registry port AND manifest generation == committed generation; `resolveGateRestart` resumes only on a committed-port rebind with a passing owner check. Adds an ADDITIVE optional `generation_id` to the registry entry (v1 on-disk state loads unchanged; version stays 1). ALL over injected ops (bind, owner-check, registry, manifest-publish, staging store), host-free. HONEST BOUND: the generation LIBRARY only -- the gate service uid / client-auth / liveness oracle live in S5-3, the exclusive routing manifest in S5-4 (G4 here only publishes the generation-bearing gate policy), park/unpark in S5-5; the production caller is S5-6's `arming-wiring.ts` (WIRED; S5-DRILL owed). `gate-account.ts` + `gate-credential.ts` + `gate-client-auth.ts` + `liveness-oracle.ts` (Slice 5 S5-3, 2026-07-15) add the GATE-AS-TCB surface: the loopback gate is a trusted-computing-base member, so (a) it runs as its OWN dedicated `sanctuary-gate-<agentId>` service uid (`gate-account.ts`, reusing `provision/account.ts`'s hardening; per-agent so each gate uid is kernel-bounded to only its agent's endpoints under S5-0) -- never the operator (allow-all) or the agent (kernel-denied); (b) client authorization is FAIL-CLOSED (`gate-client-auth.ts`, wired into `gate-server.ts` via the optional `clientAuth`): every CONNECT must present a generation-bound, current-only bearer credential (`gate-credential.ts`: root writes a random secret atomically pre-unpark to a 0600 agent-readable token file + a 0600 gate-readable accept file holding only the sha256, bound to the committed generation; constant-time compare; a stale generation is rejected before the secret compare so an old token never replays after a new G5) AND resolve to the agent uid -- bearer never overrides peer, and an unresolved/capped peer DENIES (a `client_denied` audit event + 403 `X-Sanctuary-Gate: client-denied`; the honest availability bound: peer-lookup saturation denies the agent's own connects rather than admit an unverified peer); (c) the non-root gate learns pf-liveness WITHOUT holding pf privilege via a root-owned oracle (`liveness-oracle.ts`): the root supervisor runs `checkPfAnchorLiveness` and publishes a short-TTL, generation-bound Ed25519-SIGNED freshness token the gate verifies per-CONNECT (deny on absent/expired/bad-signature/binding-mismatch/live!=true), so no cached green survives a flush (the supervisor invalidates on flush; a lingering token is bounded by the sub-second-to-low-seconds TTL, stated as an honest residual). FAIL-CLOSED SELF-CONSISTENCY (two-family gate fix-round): `createExclusiveEgressGate` REFUSES to construct when its injected config is cross-principal -- `clientAuth.agentUid` must equal `policy.agent_uid`, and a liveness probe that advertises a `binding` must match `policy.{agent_uid, gate_port}` -- so a credential/liveness verdict computed for a DIFFERENT principal can never be wired in and read as green (generation binding stays the wiring layer's `evaluateGenerationMatch`); `planGateAccountProvision` refuses (`GateUidCollisionError`) a gate uid that collides with the agent uid or an operator uid; and per-CONNECT liveness freshness for the oracle probe is a CONSTRUCTION GUARD, not a caller obligation (second-family fix-round): the oracle probe self-declares an explicit `coalescing:"forbidden"` marker (never inferred from its `binding` -- a pfctl-backed probe that grows a binding keeps its single-flight pfctl-amplification bound), the constructor auto-disables single-flight when `singleFlightLiveness` is omitted and REFUSES an explicit `true` for a marker-declaring probe (closing the post-flush shared-green window structurally), and TCB mode (`clientAuth` present) REFUSES construction outright unless the probe is oracle-shape (marker + policy-matching binding) -- so the S5-6 wiring just passes `createOracleLivenessProbe` and omits the option. HONEST BOUND: this LANDS the gate-TCB primitives (uid provisioning + client-auth + liveness oracle) as a LIBRARY over injected ops; the fail-closed `clientAuth` and the oracle probe are WIRED by the S5-6 gate daemon (`gate-daemon.ts` constructs both; the legacy advisory peer path remains only for clientAuth-absent library callers). Real account provisioning + install-time arming run through S5-6's install flow; the composed behavior is S5-DRILL-owed and no capability claim advances. `posture.ts` (Slice 5 S5-P, 2026-07-16) adds the EXCLUSIVE-EGRESS POSTURE OBJECT + the aggregate-green capping rule (design rev3 section 6, the HIGH-3 hard predecessor): a first-class, queryable per-agent posture (`manifest_gate_rule_generation`, `pf_liveness` full reasons vector, `gate_process` up + port-owner-verified, `generation_match`, `mode` = `exclusive`/`coarse-only`/`unprotected`) built PURELY from injected probe results over the REAL S5-1/S5-2/S5-3 surfaces (`resolveCommittedGeneration` + `evaluateGenerationMatch` are the same functions the gate's serve decision uses; the S5-3 oracle's `verifyLivenessToken` output drops in as the pf probe -- no parallel bookkeeping), plus `summarizeExclusiveEgressStatus` (the wall-level fold) and `exclusiveEgressCapsAggregateGreen` (THE one capping rule): `principal-policy/posture.ts:buildCastleWallPosture` caps a would-be `armed` to the DISTINCT non-green `coarse_only` arm-state, and `feature-health.ts` caps the `castle_wall_egress` row from `active` to the distinct `coarse_only` status (basis `exclusive_egress_not_live`), so every posture surface that renders `arm_state` or the feature-health row (CLI status, posture home, v1.1 console, hero-shield aggregator via `computeOverall`'s armed-only green, fortress-view, /v1/status, posture APIs) computes protected-green from the ONE capped verdict; the wrap first-run protection banner routes through `egress-gate/protection-claim.ts`: `wrap/cli.ts:probeCastleWallProtectionClaim` requires the exclusive-egress provider and renders green only from the branded `exclusive` claim; `probeCoarseCastleWallEnforcementObserved` is only the producer coarse-evidence helper. `buildHome` resolves the provider EXACTLY ONCE and threads the SAME snapshot into both the wall posture and the feature-health panel (so an intermittent provider can never cap one home surface while another greens), and the dashboard fault-raise panel + wrap banner share that fail-closed resolve. Providers are OPT-IN + FAIL-CLOSED: absent OR a `null` return = affirmatively no-fine-grained-agent for posture APIs; wrap banner rendering treats absent, null, or throwing providers as a non-green unknown claim; a dashboard provider that THROWS resolves to `failedExclusiveEgressStatus` (caps green); the producer (S5-6) MUST use `failedExclusiveEgressStatus` on a roster-read failure, never `summarizeExclusiveEgressStatus([])`. HONEST BOUND: the posture PRODUCER is WIRED (S5-6: `arming-wiring.ts` `createExclusiveEgressPostureProducer`, bound by `dashboard-standalone.ts` on darwin over the real marker/registry/staging/runtime/oracle-token surfaces), the coarse-only state is producible by the S5-6 degrade-loud path, and the render-first ordering constraint held (S5-P merged before the producer); the library itself arms/probes nothing. `release-barrier.ts` (Slice 5 S5-5, 2026-07-17) adds the RELEASE BARRIER (design BLOCKER-2'): the agent harness cannot start before the G5 generation commit, at install OR at boot. Three layers, none controlled by the agent uid: (1) the harness LaunchDaemon is rendered in a PARKED form (`Disabled=true`, `RunAtLoad=false`, `{Crashed:true}` KeepAlive -- crash-restart only, chosen over `SuccessfulExit` because launchd.plist(5) documents `SuccessfulExit` as IMPLYING `RunAtLoad=true` and it would restart wrapper refusals in a loop; `harness-daemon.ts` renderer gains those additive options; legacy render byte-identical) and `launchctl disable`d, so launchd never auto-starts it; because `launchctl enable` PERSISTS, the release sequence RE-DISABLES the job as its final step after bootstrap (re-parking the boot path -- without that re-park the next boot would auto-start pre-G5, the exact B2' escape); (2) a root-owned hold file `/var/db/sanctuary/agent-harness/<uid>.release` (root 0644 in a root 0755 dir -- integrity by ownership, DISCLOSED deviation from the design's 0600 note since the wrapper runs as the agent uid via the proven `UserName` drop and must read it; no secret in the file) written ONLY at the G5 commit, naming `{generation_id, agent_uid, harness_label, argv_digest, boot_session_uuid}`; (3) a STATIC root-owned POSIX-sh exec wrapper (`ProgramArguments[0]`; zero render-time interpolation, all inputs arrive as launchd argv) that refuses (exit 78, no exec) unless the hold file matches the plist-embedded expected generation, its own runtime uid + label, the sha256 NUL-separated argv digest of the exact argv, AND the CURRENT `kern.bootsessionuuid` -- so a stale plist, a stale hold file surviving reboot, a swapped argv, or a parked-sentinel (generation 0) plist can never release. `runReleaseBarrierSequence` sequences reassert-parked -> re-arm -> gate-verify -> commit -> verify-committed (the committed identity is RE-verified as the live gate generation, closing the verify-A/commit-B TOCTOU) -> hold-file -> released-plist re-render (the parked plist embeds generation 0, which the wrapper refuses; every release re-renders with the committed id) -> enable -> bootstrap -> verify-running -> re-park -> verify-running over injected ops; "released" is returned ONLY after the harness reports stable-running both after bootstrap and after the final re-park (no silent green-on-down; `kickstartAgentHarnessDaemon` enforces the same stability bar); EVERY abort branch removes the hold file, leaves the job disabled, and restores the parked plist (cleanup failures loud in the outcome, never swallowed), and a failed final re-park is the DISTINCT `released-repark-failed` outcome (callers must render amber). `executeParkedHarnessInstall` structurally cannot bootstrap. HONEST BOUND: the barrier is WIRED (S5-6): `runProvisionFlow`'s exclusive stage + the safe-mode boot daemon route through it via `arming-wiring.ts` (S5-7 still wires unprotect); the wrapper checks the argv DIGEST not binary content, and the `{Crashed:true}` KeepAlive + disable-while-running launchd semantics are S5-DRILL-owed behaviors. `gate-daemon.ts` + `drift-guard.ts` + `runtime-fs-plan.ts` + `arming-wiring.ts` (Slice 5 S5-6, 2026-07-17; fix-round 2026-07-17) add the ARMING WIRING that retires the cluster's "no production caller" staging disclosures: `gate-daemon.ts` is the long-lived per-agent gate LaunchDaemon (label `ai.sanctuaryprotocol.egress-gate.<uid>`, `UserName` = the dedicated gate account, NEVER root/agent; binds EXACTLY the committed policy port or exits non-zero -- never squats; publishes a runtime-state file `gate-runtime/<uid>/state.json` inside its ROOT-PRE-CREATED gate-uid-owned subdir, which the owner check + posture read; constructs the S5-3 TCB wiring: `createOracleLivenessProbe` + fail-closed `clientAuth`, `singleFlightLiveness` OMITTED so the #955 construction guard auto-disables single-flight; CLI entrypoint `sanctuary castle-wall egress-gate-daemon --agent-uid=N`); `runtime-fs-plan.ts` (fix-round BLOCKER-1/2) is the ONE computed ownership/mode/order plan for every exclusive-egress runtime dir (per-uid gate-owned runtime subdir; 0711 traversal-without-listing on gate-cred/gate-liveness so the gate uid reaches its .accept/.token and the agent uid its .token; 0755 parents; explicit chown/chmod after every mkdir, never umask luck; applied fail-closed by root at arming + re-asserted at boot); `drift-guard.ts` is the MED-7 transient-pf-rule diff (running main ruleset vs base-config-derived via `pfctl -n -v -f` normalization; any pfctl/fs failure THROWS so repair refuses, never hook-installs blind); `arming-wiring.ts` maps the pure S5-1..S5-5 libraries onto real side effects for the three callers (install via `wrap/auto-provision.ts`; repair via `--repair-egress-gate`, which parks the possibly-live harness VERIFIED-stopped before any mutation and never claims PARKED unverified; boot via the safe-mode daemon's `startExclusiveEgressBootSupervisor`, which bootstraps the RunAtLoad=false gate daemon before verifyGate, re-parks unresolvable agents FOR REAL, and owns the PERSISTENT oracle refresh cadence by re-scanning the registry each tick -- covering agents armed after boot; a host with no resident policy daemon fail-closes within one token TTL) and provides the S5-P posture producer (pid_start-enforcing `verifyLoopbackTcpPortOwner` port ownership). HONEST BOUND (S5-6): arming is wired and per-branch unit-tested; the composed WHOLE FLOW on a real host is S5-DRILL-owed (Erik-present, signed host) and no capability claim advances until that drill captures evidence. `arming-wiring.ts` `createUnprotectExclusiveEgressOps` (Slice 5 S5-7, 2026-07-18) is the production mapping for the per-agent UNPROTECT sequence (`castle-wall/provision/exclusive-unprotect.ts`): the WHOLE sequence runs under the provision lock (`withProvisionLock(PROVISION_LOCK_PATH, ...)`, the arm/repair lock -- fix-round-2 HIGH-1) and step 0 asserts the sole-exclusive-agent invariant (any OTHER committed non-tombstone registry entry sharing the leaving uid's fortress/harness REFUSES fail-closed, nothing torn down -- the single-uid marker + fortress-keyed policy + harness-keyed rules are surfaces a per-agent teardown cannot safely half-remove; the shared-fortress multi-agent teardown is out of scope until built), then for the sole user: verified persistent park (restore-or-remove plist) -> generation recovery -> gate-daemon bootout (loud, never swallowed) -> credential + oracle-token removal by their single-source uid-keyed paths (`gateCredentialAcceptPath`/`gateCredentialTokenPath`/`gateLivenessTokenPath`, so revocation works after the gate account is gone) -> provisioned-rule scrub -> policy surfaces off (per-uid AND the fortress marker + gate policy file) -> registry `remove()` LAST (union re-verified live; anchor flushed; the removed entry's generation_id folds into the persisted `generation_floor`, and `computeNextGenerationId` now refuses a headroom-less allocation at the allocator via the shared `hasGenerationHeadroom` bound). Slice 5 is CODE-COMPLETE with S5-7 (the multi-agent shared-fortress unprotect stays owed); the S5-DRILL (incl. leg 6 multi-uid unprotect) stays owed. `peer-resolver-protocol.ts` + `peer-resolver-daemon.ts` + `peer-resolver-client.ts` (S5-3 fix, 2026-07-24) RETIRE the "advisory lsof peer-uid recovery" framing above for the fail-closed TCB path: the 2026-07-24 Mini1 drill root-caused a HIGH finding on hardware -- the unprivileged gate uid's `lsof` cannot see the confined agent's cross-uid socket, so peer resolution returned `null` on 100% of real connects and S5-3's fail-closed rule denied every one, including valid on-manifest credentialed requests. The fix adds a narrow, root-owned peer-resolver LaunchDaemon (one per confined agent, mirrors `gate-daemon.ts`'s own per-agent shape) that answers exactly one question -- uid/pid for a loopback client port -- over a per-agent-uid-scoped Unix domain socket (root 0711 parent + the socket file chowned to the ONE gate uid, mirroring `liveness-oracle.ts`'s token-file confinement; no `SO_PEERCRED` on this platform, so filesystem permission IS the uid boundary, same precedent as `supervisor/socket-server.ts`). The wire protocol (`peer-resolver-protocol.ts`) carries no `command`/`argv` field, so it cannot become a general root command oracle; the client (`peer-resolver-client.ts`) implements the EXISTING `PeerCommandRunner` seam `gate-daemon.ts` now defaults to, so `peer-identity.ts`'s `parseLsofPeer`/`resolveLoopbackPeer` are UNCHANGED; every transport fault resolves the same synthetic non-zero exit `exec-runner.ts` already uses, so resolver-down/timeout denies fail-closed by construction. Interim mechanism is root-`lsof` (native `proc_pidinfo` filed as DEBT). Wired into S5-6 install/boot bring-up and S5-7 unprotect/coarse-restore (mirrors the gate daemon's own bootstrap/bootout) and `runtime-fs-plan.ts`'s layout. HONEST BOUND: CI-green + a fail-before/pass-after regression (`gate-peer-resolver-regression.test.ts`) only; the real cross-uid privilege boundary is Erik-present Mini1 S5-DRILL-owed and gates the merge (HALT-before-merge) `pf-enable-state.ts` (F-PFBOOT + F-PFTHIRDPARTY fix, 2026-07-26) is THE pf ENABLE-REFERENCE CHOKEPOINT: one module owns the `pfctl -E` reference lifecycle end to end (acquire, resolve, release), because that lifecycle was reasoned about inline at three `pf-anchor.ts` call sites and cost two HIGH wrong-allows measured on Mini1 against merged main `ed7722ce`. A `-E` reference is VOLATILE kernel state a reboot zeroes; the registry file recording its token is not. F-PFBOOT: the arm path decided from the RECORD alone (`if (existingEnableToken === undefined)`), so after one reboot the persisted token was byte-identical while pf was `Status: Disabled`, the anchor was inert, and the confined uid regained loopback reach (ports 22/5900/11434/5432 CONNECTED 3/3 against blocked 3/3 armed); every recovery verb then deadlocked on the same missing `-E`. F-PFTHIRDPARTY (fails OPEN, no reboot): with pf held enabled by a reference Sanctuary does NOT own and its own token kernel-invalid, the product reported the gate LIVE, and releasing that foreign reference destroyed confinement in the SAME boot session and generation while the supervisor logged `registry is clean again`. THE INVARIANT: whenever the confined-uid union is non-empty, Sanctuary holds ITS OWN live reference minted in THIS boot session, established from three observations -- (1) `kern.bootsessionuuid` (a definite negative costing zero pfctl calls, and the one leg an unreadable pfctl cannot corrupt; the SINGLE reader, shared with `release-barrier.ts`'s hold-file binding), (2) `pfctl -s info` -> `Status: Enabled` (necessary, NOT sufficient -- that gap IS F-PFTHIRDPARTY), (3) `pfctl -s References` naming our exact TOKEN (the only usable discriminator: `Process Name` is always `pfctl` and the owning PID has exited). Every observation is a discriminated union with a `known: false` arm, and the resolver's three-state verdict permits reuse on `ours-live` ONLY -- `unknown` re-acquires, whose worst case is one extra pf enable reference (over-enforcing, releasable, visible) rather than a pf that is disabled while we report armed. The post-acquire invariant is ASSERTED (pf re-read enabled, our token re-read present) and a refusal releases the reference it took. `releasePfEnableReference` recognizes BOTH captured pfctl absence messages (`pf not enabled` when pf is disabled, `pf: token invalid` when it is enabled) and still throws on any failure it cannot account for, which is what lets `--unprotect-egress-gate` clear a registry entry whose token a reboot invalidated. The registry persists `enable_token_boot_session` beside `enable_token` (ADDITIVE optional; state version stays 1) and reads/writes the pair through one accessor so no path (including rollback) can restore a token without its provenance; `mutate` no longer reconciles before a LAST-uid teardown, since re-asserting a union about to be flushed entirely is what deadlocked the operator's only escape hatch. Anti-drift: a structural test forbids `pfctl -E`/`-X` in any other egress-gate module, and the tests run against byte-exact captured pfctl output (`test/egress-gate/fixtures/pf-hardware-capture.ts`) rather than a model. HONEST BOUND: the code paths are unit-proven against captured output and mutation-checked; the composed post-reboot behavior on real hardware is DRILL-OWED and no capability claim advances until it is captured. | proxy (MCP-tool proxy, NOT egress); `castle-wall/egress-proxy.ts` (VM/vsock CONNECT evaluator whose DECISION logic this reuses); `policy-engine/egress-gate.ts` (EXACT-NAME confusable: the compiled-policy per-agent egress allowlist gate, `evaluateEgressGate`, unrelated to this module); castle-wall (manifest-side rule derivation lives in its `allowlist/gate-derivation.ts`) | Yes | pf anchor name `sanctuary.egress-gate`; derived rule id `derived_exclusive_egress_gate`; config `policy/egress/exclusive-egress-gate.json`; daemon label `ai.sanctuaryprotocol.agent-harness`; S5-1 registry file `/var/db/sanctuary/egress-anchor-registry.json` (+ `.lock`, root-owned 0600, internal artifact NOT a frozen surface); S5-2 per-uid generation staging file `/var/db/sanctuary/generation-staging-<uid>.json` (root-owned 0600, internal artifact NOT a frozen surface); registry entry `generation_id`/`tombstone` are ADDITIVE optional fields (state version stays 1; v1 state on disk loads unchanged); S5-P wire values on the posture/status APIs: mode tokens `exclusive`/`coarse-only`/`unprotected`, arm-state `coarse_only`, feature-health status `coarse_only` + basis `exclusive_egress_not_live`, and the `exclusive_egress` block field names (additive; consumers fail non-green on unrecognized values by construction); S5-5 hold dir `/var/db/sanctuary/agent-harness/` (hold file `<uid>.release`, wrapper `release-exec-wrapper.sh`; root-owned internal artifacts NOT frozen surfaces) + hold-file header `sanctuary-agent-harness-release v1` + wrapper refusal exit 78; no absolute bypass-resistance claims in user-visible strings (honesty bound) |
| fortress | Cognitive (posture / mode tiers) | canonical | The Fortress Mode state machine: three posture tiers (`tier_1_private`/`tier_2_federated`/`tier_3_interop`) gating which mesh capability bits are live; signed mode-transition events | castle-wall (network enforcement); supervisor (OS process); the operator word "fortress" = the on-disk SANCTUARY_FORTRESS_PATH dir | Yes | `FORTRESS_EVENT_TYPE_PREFIX="fortress_"` + `MODE_TRANSITION="fortress_mode_transition"` (reserved per Federation §10.3); `MODE_TIERS` union (persisted in config, keys `TIER_CAPABILITY_DEFAULTS`) |
| wrap | CLI (install-time onboarding) | canonical | The `sanctuary wrap`/`init` onboarding CLI: detect + back up + rewrite a harness's MCP config so Sanctuary becomes the upstream gateway; passphrase/recovery-key/keychain custody; Hermes YAML emitter (`hermes-yaml.ts`) guarded by a PyYAML parse-parity check (`hermes-yaml-parse-parity.ts`) that refuses to edit config.yaml when the line-scanner's view disagrees with a real PyYAML parse or when that parser cannot run (fail-closed); the read-only install preflight (`preflight.ts`) for `sanctuary protect --preflight` and the protect refuse-early table; the read-only disk-backed fleet-roster provider (`fleet-roster-provider.ts`), injectable into the surviving principal-policy dashboard via `setDependencies` (dashboard fold PR-1); and the fold PR-4 ensure-and-reuse mechanic (`ensureMainDashboardForWrap`: tenant-scoped runtime.json + health-probe detection, EADDRINUSE same-fortress race-close, port walk): `sanctuary protect` reuses or starts the ONE principal-policy dashboard (starter injected by `src/cli.ts` over `startStandaloneDashboard`; wrap/cli must not import dashboard-standalone, import-cycle baseline) and no longer writes/clears runtime.json (single-writer: dashboard-standalone owns it). `credential-registry.ts` composes the complete external-credential service list for one fortress (every keyring/fallback service `passphrase.ts` and `keychain-custody.ts` can produce, deduplicated) so destructive reset has one inventory to delete against instead of a hand-written list that can drift as new credential families are added | supervisor (runtime process); the Layer-2 managed-child path (`wrap --tier-b`); castle-wall; `principal-policy/fleet-roster.ts` (the presenter `fleet-roster-provider.ts` feeds) | Yes | `WRAP_META_FILENAME="wrap-meta.json"` + the legacy read-only `LEGACY_WRAP_META_FILENAME` constant (its value is a retired-vocabulary filename kept read-only for back-compat; the ONLY permitted carrier of the retired term; removing it breaks unwrap of old releases) + surface-scoped `wrap-meta-<12-hex-surface-tag>.json` slots (per-surface unwrap pointers; written when a wrap would clobber another surface's canonical meta, scanned by every reader after the canonical + legacy filenames); env `SANCTUARY_FORTRESS_PATH`/`SANCTUARY_STORAGE_PATH`; verbs `wrap`/`protect --preflight`/`init`/`--unwrap` |
| supervisor | Operational (process lifecycle, Tier-A) | canonical | The split-process supervised daemon (Phase S1): a separate process that launches/monitors/restarts a wrapped child, holding a TRANSIENT master key (survives crash, not reboot); authenticated local-socket protocol | wrap (install-time); castle-wall (egress); fortress (in-process state machine) | Yes | socket constants `SUPERVISOR_PROTOCOL_VERSION=1` (leading frame byte), `MAX_FRAME_BYTES=64KiB`; key-handoff env `SANCTUARY_SUPERVISOR_KEY_FD`; `SUPERVISOR_KNOWN_HARNESSES` |
| broker-mcp | Selective-Disclosure (standalone broker MCP) | thin/utility | `broker-server.ts` wraps the L3 Secret Broker as a STANDALONE MCP server (four tools only); `liveness-constants.ts` + `liveness-heartbeat.ts` add the long-running `broker-server` daemon's PROCESS-LIVENESS heartbeat + clean-stop stand-down (Option C, read by `principal-policy/feature-health.ts`). Renamed from `mcp/` (2026-06-14) so the name no longer reads as the core MCP plumbing | NOT the core MCP tool-router (that is root index.ts/router.ts/sanctuary-tools.ts); the heartbeat is PROCESS-liveness only, NOT token-mint/deny correctness | Yes | tools `broker/request_token`/`read_secret`/`list_grants`/`audit_query`; server name `"sanctuary-broker"`; version routed via `config.SANCTUARY_VERSION` (do not re-inline package.json); at-rest ops `broker_daemon_heartbeat`/`broker_daemon_stopped` + marker `broker_source=broker_daemon` |
| storage | Cross-cutting (persistence substrate) | canonical | The pluggable StorageBackend abstraction in RAW BYTES (encryption is one layer up): FilesystemStorage (default; `{ns}/{key}.enc`, bijective `!`-escape encoding, 3-pass secure delete; optional create-with-fchown `owner` so a ROOT daemon writing inside an operator-owned fortress creates operator-owned files only when the namespace directory already exists and verifies inside the storage root, fail-closed on missing parent or chown failure -- same `owner` option on `custody-fs.ts`'s `writeFileCustody`) + MemoryStorage; `read-only-guard.ts` is the decorator a declared-read-only code path wraps a backend in so mutation is refused structurally rather than by convention (reads delegate, `write`/`writeDurable`/`delete` throw; NOT a security boundary -- it bounds one process's own storage calls, nothing else). `cross-process-lock.ts` is the kernel-backed cross-process custody write barrier (a private Unix-socket-based exclusive/shared lock namespaced to the fortress `_meta` dir; degrades read-only or refuses per `degradeOnEnvironmentalLoss` when the invoking uid, filesystem, or `_meta` perms are non-owner/non-local/looser-than-0700). `directory-capability-worker.ts` is the short-lived Darwin-only child process spawned with the protected directory as its cwd (macOS cannot traverse `/dev/fd/<dirfd>/child` and Node has no `openat(2)`), reused by `filesystem.ts` for directory-fd-scoped operations. `custody-lock-scaffold.ts` classifies the persistent custody-lock scaffold file's on-disk shape (`secure`/`legacy-0644`/`unsafe`) for the stale-socket reaper; the file itself is inert, the Unix socket is the real ownership primitive. `directory-fsync.ts` is the single `isBenignDirectoryFsyncError` classifier for platform-tolerated directory-fsync capability failures (Windows EISDIR/EPERM; real Darwin/Linux durability failures still propagate). `fresh-fortress.ts` recognizes a fresh/killed-init fortress directory shape (only the zero-byte lock plus an allowed empty scaffold) so recovery can distinguish "nothing durable happened yet" from a partially-written fortress | `cognitive/state-store` (owns encryption + namespace semantics); `substrate/` (unrelated); `core/master-custody.ts` (the barrier's CALLER and the custody semantics it protects, not the lock mechanics themselves) | Yes | the StorageBackend method shape (88 importers); on-disk `{ns}/{key}.enc` + the `!`-escape encoder (changing it orphans existing fortress files); 0o700/0o600 modes; `directory-capability-worker`'s tsup build entrypoint + its `new URL(...)`-relative sibling resolution in `filesystem.ts` (a move changes a runtime resolution path, not just a bundler entry) |
| exit | Operational (Exit principle) | versioned-frozen (v1) | The operator's data-portability path: export/import `SANCTUARY_EXIT_BUNDLE_V1` (manifest + hashed JSON; private keys never emitted), optional re-key, did:web binding, verifier, `inspect.ts` (read-only "which credential does this bundle DECLARE it needs" report backing `sanctuary exit inspect`; describes the artifact and never predicts an import, never opens a fortress context, never writes), `source-custody.ts` (the ONE `source_custody` shape-check, shared by the import refusal and the inspect classifier so the report can never describe a block import would reject; requires the structural + recovery-key-only rules of EVERY wrap but an openable payload from only SOME wrap, mirroring `unwrapMatchingWrap`'s first-wrap-wins loop; separate file because `bundle.ts` imports `verifier.ts`), memory-class stamper, consent-release + tombstone | reputation/recognition (it consumes them); storage (raw bytes) | Yes | `SANCTUARY_EXIT_BUNDLE_V1`, `EXIT_BUNDLE_MANIFEST_VERSION`, `EXIT_BUNDLE_ARTIFACT_KINDS`; audit ops `exit_consent_release`/`exit_tombstone`; Tier-1 export gate. HONEST FLAG: `memory_class` is FORGEABLE on un-instrumented write paths today (M-1/A1) - do not over-claim sealed provenance |
| workload-lifecycle | Compliance / Operational | default-off-allocated | The workload-lifecycle audit schema: versioned signed chain-bound lifecycle events for DECLARED workloads, `sealLifecycleEmission`, WorkloadRegistry, host-workload-attestation builder/verifier. **Schema exists + tested; no production bootstrap wires the seal yet** | `operational/audit-log` (the log it rides on); agent-contract (its only 2 external consumers) | Yes | `WORKLOAD_LIFECYCLE_OPS` op strings (workload_instantiated/paused/.../host_attestation/undeclared_detected); `WORKLOAD_HOST_ATTESTATION_DOMAIN/SCOPE_TEXT/...`. Do NOT market as a shipped enforcement capability; consciousness/mind-crime framing is OUT |
| intelligence | Intelligence/IO | canonical | The per-surface LLM substrate selector ("the selector IS the architecture"): binds each surface (concierge, direct-agent-gate-advisor, sentinel-scoring, gate-explanation, privacy-filter-tier-2, template-suggestion) to Local/Venice/Frontier/Disabled; operator fallback; audit. `surfaces.ts` is the dependency-free shared surface contract used by selector types and manifest/catalog verifiers. `model-manifest.ts` is the historical V1 manifest verifier; V1 never arms Q5. `model-manifest-v2.ts` is the current signed Ollama-manifest-root and armed-state contract. `model-catalog-v3.ts` plus `catalog-v3/` define pure, inert catalog/index/overlay body parsers, keyring-bound verified envelope entry points, bounded per-domain integer-only RFC 8785-compatible signature preimages, companion schemas, and runtime-integrity-pinned SPDX data. Signed catalogs and indexes are accepted only through an epoch keyring; the compiled key policy has a pinned digest. Retired epochs preserve historical verification only inside their closed ranges; revoked epochs intentionally invalidate historical signatures. Overlay signer ids are derived from the supplied operator public key. The combined validator accepts only its signature verifiers' module-authenticated envelopes/bodies, plus a caller-supplied fortress binding and rollback floor, then treats catalog and overlay models as an independent bounded union: noncolliding overlay models are accepted, collisions by model id/full identity/runtime tag are refused in either update order, and every authorized surface must meet its applicable catalog and compiled assurance floors. S1 continuity values are explicitly untrusted structural observations; authenticated persistence, anti-rollback state, network fetch byte/time limits, issued-at freshness/monotonicity, model-to-surface hardware compatibility, and activation remain deferred to S2. The compiled index value is an all-null genesis sentinel, not evidence of a real authenticated checkpoint. These contracts perform no discovery, host read, persistence, or activation. `provisioning.ts` is the Q5D injected, consent-gated, cross-process-locked verify/pull/verify/one-config-save sequencer; its already-present path performs the same authoritative commit, and provenance projects only afterward. The production catalog loader remains deliberately absent until its bounded loader slice and real signed asset ship. `runtime-light-verifier.ts` supplies bounded `/api/show` existence and exact `/api/tags` digest inspection. `immune-disk-verifier.ts` supplies bounded OCI parsing, no-follow descriptor hashing, and cadence/single-flight mechanics; no production path can invoke either verifier or arm state without the absent signed asset loader | concierge (a consumer surface; every live concierge route delegates here) | Yes | at-rest `INTELLIGENCE_NAMESPACE="_intelligence"`, `SUBSTRATE_CONFIG_KEY`, HKDF `intelligence-substrate-config`; V1 config is historical-unarmed and armed V2 requires one complete `LocalIntegrityStateV2`; dedicated O_EXCL lock `.q5-provisioning.lock`; `INTEL_OPS`; provider category `model-registry`; `SURFACES` names; contracts/v1.2 intelligence-events; frozen historical domains `sanctuary.model-manifest.v1` and `sanctuary.model-manifest.v2`; Rung-2 domains `sanctuary.model-catalog.v3`, `sanctuary.model-catalog-index.v1`, and `sanctuary.model-overlay.v1`, all with newline framing; pinned catalog key-policy, raw/JCS schema, and SPDX digests; explicit genesis sentinel (no real index checkpoint yet) |
| query-anonymity | Selective-Disclosure | canonical | Outbound de-identification: strips fingerprinting headers, classifies intent, rewrites/redacts PII (regex Tier A + optional LLM Tier B via the selector), reversible mapping store, and the Tier 3a network-path transport (two-hop CONNECT/MASQUE egress proxy composed BENEATH the wrapped fetch; opt-in, disarmed by default, fail-closed; IP-decoupling only) | intelligence (consumer of, not a router); proxy (forwards tool calls; this scrubs content) | Yes | routes `/api/query-anonymity` + `/api/query-anonymity/pii`; at-rest `_query_anonymity_tier_b`, `_query_anonymity_reverse_map`; audit op enums incl. `query_anonymity_tier3_*`; `PII_CATEGORIES`; `PII_REWRITE_LLM_SURFACE="privacy-filter-tier-2"` |
| concierge | Intelligence/IO | canonical | A stateless read-only Q&A concierge: assembles a context bundle from `CONCIERGE_READ_SURFACES`, preserves the deterministic empty-context short circuit, and delegates every inference call to `SubstrateSelector.invokeSummarize("concierge", ...)` | chat (the persisted operator-chat surface adds threads, query rewriting, dynamic context, and memory); intelligence (the one provider-selection authority) | Yes | `CONCIERGE_READ_SURFACES` array + derived type; CLI-local and `/api/hub/concierge/*` response envelopes; no production provider-client construction outside `intelligence/substrates/` |
| chat | Networking | canonical | TWO co-located surfaces: (1) agent-to-agent encrypted MESH group chat (MLS-style per-epoch AES-256-GCM over libp2p, presence, pgvector); (2) the persisted OPERATOR chat service (threads, concierge memory, selector-routed concierge surface) | concierge (stateless Q&A). The two halves split by barrel: `index.ts` = mesh chat, `operator-chat-index.ts` = operator chat | Yes | the operator message log uses namespace `_chat_messages` and the concierge-memory store uses `_chat` + key prefix `concierge_memory.` (separate namespaces, not a shared-namespace collision); `CONCIERGE_THREAD_KEY="_fortress"`, `OPERATOR_CHAT_MAX_THREAD_LENGTH=500`; per-epoch AES-256-GCM + HKDF rotation; `OPERATOR_CHAT_OPS` |
| proxy | Enforcement-surface | canonical | The MCP proxy router: wraps UPSTREAM third-party MCP tools under `proxy/{server}/{tool}` and forces every call through the full enforcement chain (injection, gate, context-gating, v1.1 privacy filter, audit). No bypass | query-anonymity (content scrub); substrate (off-by-default plugin contract) | Yes | wire prefix `proxy/` + literal `proxy/{server_name}/{tool_name}` (public tool names); `UpstreamUnavailableError`; the "no bypass" invariant |
| substrate | Enforcement-surface | default-off-allocated | The plugin-host substrate: S1 vendor contract (canonical/strict-JSON + restricted-YAML parsers, verdict algebra, allowlists, signed-manifest verification) plus S4 lifecycle/supervisor, confinement-report gate, egress consultation, and plugin attribution types. **S5 adds `reference-plugin/`: TWO first-party BUNDLED reference plugins, each independently signed with its own key: `blocklist/` (bare-domain-per-line egress vetoer, entry `bin/blocklist.mjs`) and `hosts-blocklist/` (Pi-hole / hosts-format egress vetoer, entry `bin/hosts-blocklist.mjs`); both binaries import NOTHING from Sanctuary. Host-side: the blocklist-specific loader/signer in `blocklist.ts`, the frozen compiled-in multi-plugin registry + fail-closed registry-keyed loader in `bundled-plugins.ts` (`BUNDLED_PLUGINS`, `loadBundledPlugin(plugin_id)`; loads ONLY a registered plugin, resolving its dir internally and realpath + identity-asserting it against the registry, never an arbitrary caller path), the spawner in `spawn.ts`, the `sanctuary plugin list/status/test` CLI (enumerates + integrity-verifies BOTH), and the hostile-plugin drill.** These are bundled reference plugins, NOT third-party or marketplace plugins; third-party install registry and egress broker remain deferred (F1-gated). | `intelligence/substrates/` (LLM backends - one keystroke apart, unrelated); proxy (live upstream wrapper); `supervisor/` (wrapped-agent runtime, not plugin host) | Yes | contract tokens `BUNDLE_SCHEMA`, `SIGNATURE_FILENAME`, `SUPPORTED_ALG`, `PLUGIN_DECISIONS`, `FIELD_ALLOWLIST`, `MAX_*` caps, `PLUGIN_CONFINEMENT_KIND`, `PLUGIN_SECCOMP_PROFILE_ID`, plugin audit op strings, each reference-plugin bundle layout (`governance.yaml`/`SIGNATURE.json`/`first-party-signer.json` + its entry `bin/*.mjs`), the bundled-plugin ids `ai.sanctuary.blocklist` + `ai.sanctuary.hosts-blocklist`, and canonicalization (changing it breaks signature verify or launcher gating). |
| console | Enforcement-surface | versioned-frozen (v1) | The Operator Console v1.0 CONTROL surface (`/console` + `/api/console/*`, six views + attestation header). Owns the SHARED auth gate (`enforceAuth`/`authMiddleware`) reused by ~12 route files; SSE stream; static serving | dashboard (read-mostly status); `principal-policy/dashboard.ts` (approval channel) | Yes | routes `CONSOLE_API_PREFIX="/api/console"`, `CONSOLE_HTML_PATH="/console"`, the `API_ROUTES` map + concrete service paths; asset ownership `server/public/console/{index.html,console.js,console.css}` via `resolvePublicDir()` |
| dashboard | Enforcement-surface | versioned-frozen (v1) | The (RETIRED-FROM-PRODUCTION-SPAWNING) wrap-era Sovereignty Dashboard server + the SHARED dashboard building blocks. Dashboard one-surface fold (ratified 2026-08-02): `sanctuary protect` no longer spawns this server; the ONE production dashboard is the principal-policy `DashboardApprovalChannel`, which `protect` now ensures-and-reuses (`wrap/cli.ts:ensureMainDashboardForWrap`) and which serves every route group this dir was the only home of: read-only `GET /api/fleet/roster`, the `/api/templates` routes (init behind the Tier-1 approval gate), the `POST /api/approvals/:id/(allow|deny)` wire shape (aliased onto `handleDecision`), and the mobile companion PWA `/m/*` (`mobile.ts` renders; principal-policy serves). What REMAINS live from this dir: the SHARED v1.1 concierge dispatch (`v1_1/`, mounted on the principal-policy router too), `aggregator.ts` (the ProtectionSnapshot aggregator + `composeAggregatorSources`, the ONE sources construction site both servers use), `mobile.ts` (asset source for the PWA), `design-tokens.ts` (the canonical "paper/ink" design-token block every surface interpolates), `process-identity.ts`, and `multi-*.ts` (server C, `--multi`, port 3500, untouched by the fold). `server.ts`/`api.ts`/`index.ts`'s `startDashboard`/`startDashboardServer` stay EXPORTED (pinned public surface + live test anchors; deletion is a coordinator-scoped counted disposition) but have no production caller | console (control surface); `principal-policy/dashboard.ts` (the surviving server; approval channel); `principal-policy/fleet-roster.ts`; `wrap/fleet-roster-provider.ts` (the read-only disk-backed provider, now injectable into the principal-policy server via `setDependencies`). NOTE: root `dashboard-standalone.ts` serves the principal-policy channel, NOT this dir | Yes | port 3501; routes `/v1.1`, `/api/hub/*`, `/api/identities`, read-only `/api/fleet/roster`; UI token `HERO_COPY="Your agent is protected."` + `id="hero-copy"`; the snake_case subdir `v1_1/` doubles as a frozen path token; exported names `startDashboard`/`startDashboardServer` |
| agent-contract | Identity | versioned-frozen (v1) | Runtime enforcement of the ten-point Agent Contract v0.1: signs/validates Agent Cards, usage events, capability grants, and the six-state lifecycle, with per-harness adapters. Registers NO MCP tools | agent-native (the live tool facade); `contracts/` (shared wire-event bundles) | Yes | `signature_scheme:"ed25519-v1"`; `schema_version:"0.1"`; event `agent_lifecycle_event`; the six LifecycleState values + transition rules; §3/§6 JSON-Schema shapes; adapter ids (claude-code, cline, hermes, mastra, tier-b-sdk, vm-launcher) |
| agent-native | Operational | canonical | The agent-facing cooperative MCP tool facade (3 files): sanctuary_remember/recall/forget, events cursor, who_am_i, help, capabilities, hide, audit_search, compound_execute, active_protections + shared safety primitives in `safety-base.ts` + first-class-surfacing catalog in `capabilities-catalog.ts` (feeds the MCP server `instructions` + sanctuary_capabilities) | agent-contract (signed identity, no tools); handshake (peer verification) | Yes | tool names sanctuary_remember/recall/forget/who_am_i/help/capabilities/hide/events_open_cursor/read/close/audit_search/compound_execute/active_protections; denial string "This action is not available in the current context."; `safety-base.ts` is a SHARED crypto util (distress + others import it) |
| handshake | Identity | versioned-frozen (v1) | The two-party sovereignty handshake: nonce-based 4-step challenge/response exchanging SignedSHRs, proving key-control + liveness, yielding a TrustTier; plus one-shot structural-preview | agent-contract (intra-fortress identity); recognition (one-way DID publication, no liveness) | Yes | tool names handshake/handshake_initiate/respond/complete/status/abort/exchange/verify_attestation/verify_completion/auto_publish; `protocol_version:"1.0"`; `liveness_proven` invariant (preview always false); `HANDSHAKE_SESSION_TTL_MS=120000` |
| contracts | Cross-cutting | versioned-frozen | Versioned cross-workstream contract TYPE bundles: `v1.1/` (privacy/hub/local-agent/handoff/Exit V1 events + constants), `v1.2/` (intelligence + operator-chat events + the distinct Exit V2 SDW memory manifest/artifact/lineage types). Types-only | agent-contract (one agent's signed identity vs many-subsystem wire types - the singular/plural trap) | Yes (per-version) | INTERNAL-only; NOT part of the published npm package - external consumers must not import. Dir names `v1.1/`,`v1.2/` are import-path tokens; the event/record shapes inside ARE wire contracts. There is NO top-level contracts/index.ts (correct - would merge two frozen versions) |
| recognition | Identity | versioned-frozen (v1) | did:web identity recognition (Principle 5): issue/resolve/publish W3C-DID-Core did:web docs bound to the fortress key + a hosted-alternative registry/route for operators without a domain | handshake (interactive liveness); key-17 (signs external-protocol payloads, not DID docs) | Yes | FROZEN external route `GET /<handle>/.well-known/did.json` from `identity.sanctuaryprotocol.ai`; `HOSTED_DID_PATH_RE`; Content-Type `application/did+json`; at-rest `_recognition_hosted_did_web`; HKDF `l2-recognition-hosted-did-web-v1` (frozen crypto label, NOT a vocab sweep target). Any reorg needs a route-smoke gate (D3) |
| key-17 | Identity | default-off-allocated | **NON-SELF-DESCRIBING NAME.** The cross-protocol sovereign SIGNER: derives protocol-scoped subkeys from the master key (HKDF) and signs three external standards - x402 settlement, ERC-8004 agent-identity (secp256k1/Ethereum), AP2 mandates. Also the ERC-8004 verifier read side (`erc8004-resolve.ts`): default-offline read-only verification of a presented ERC-8004 record (reuses the signer's verify path, no new label). Optional ERC-8004 registry-owner confirmation is DEFAULT-OFF and only runs when operator config enables it and supplies an RPC endpoint; the read path reuses Verascore's real `ownerOf(uint256)` ABI via viem and routes the RPC through the egress gate. | core (owns the master key; key-17 derives from it); recognition (DID docs); handshake (nonces) | Yes | HKDF labels `key-17:x402-signer:v1`/`key-17:erc8004-identity:v1`/`key-17:ap2-mandate:v1` (the dir token is embedded - rename must NOT touch them); `erc8004.identity.*` + `erc8004.resolve.*` audit ops; tool `sanctuary/resolve_erc8004_identity` (read-class); wire algorithm ids; config `erc8004.registry_confirmation.*` default OFF. 1 cross-module importer (off/allocated) |
| distress | Operational | canonical | The guaranteed agent distress lane (HABEAS PORT): the `sanctuary_distress` tool emitting a closed, control-stripped, rate-limited, always-audited+signed envelope to an operator-fixed destination. Cannot be silenced by policy | workload-lifecycle/attestation (record events); it's an out-of-band alarm, not a lifecycle transition or exfil vector | Yes | tool `sanctuary_distress` (policy loader REJECTS it under Tier 1); audit ops `sanctuary_distress_rate_limited`/`_local_received`/...; config `<fortress>/policy/egress/distress.json`; closed `DISTRESS_REASONS`/`DISTRESS_SEVERITIES`; `signing_unavailable:true` fallback (availability outranks signature) |
| bridge | Cross-cutting | default-off-allocated | The Sanctuary side of the Concordia bridge: three tools (bridge_commit/verify/attest) that canonicalize a ConcordiaOutcome, create a Charter commitment + signature, optionally link to Heralds. NO new crypto primitives | the Concordia Python sidecar; l3/l4 (it delegates to them). 0 cross-module importers (wired only via tool registration) | Yes | tools `bridge_commit`/`bridge_verify`/`bridge_attest`; tokens `sanctuary-concordia-bridge-v1`, namespace `_bridge`/`bridge-commitments`, `terms_hash`; trust-boundary invariant (never elevate Concordia's trust - must-never-do #4); canonicalization rejects NaN/Infinity |
| core | Cognitive | canonical | The crypto core AND the master-key SECURITY trio: primitives (AES-256-GCM, hashing, Ed25519, HKDF, random, encoding) PLUS master-custody (`establishMaster` + wraps), master-rotation (F7), anti-rollback (epoch-anchoring), PLUS `bounded-map.ts` (the shared `BoundedMap` primitive that the attacker-writable in-memory maps route through: handshake sessions, handshake results, federation peers; register LD2-03/LD2-04. It enforces a hard global entry cap, an optional per-origin quota that REFUSES a new entry once its origin already holds its share (never evicting a different origin's entry to make room), and a per-map, caller-supplied eviction policy that decides which entry to sacrifice once the global cap is hit and the incoming origin is still within quota. The primitive owns capacity enforcement only; each caller states what "safe to evict" means for its own map), PLUS `time.ts` (the strict ISO-8601-mandatory-offset timestamp predicate `parseIsoInstantWithOffset`; every timestamp entering trust arithmetic funnels through it, pinned server-wide by `test/structure/tz-strict-offset-adoption.test.ts`). the gravitational center (most-imported module in the tree) | key-17 (derives external subkeys); recovery (operator-facing orchestration vs core's rotation mechanics) | Partial (primitives only) | custody labels `recovery-key-wrap`/`keychain-wrap`, Argon2id+salt, envelope `_meta/custody-envelope`; persisted `__custody_epoch_keys`/`__head_anchor`; audit-log key label `audit-log` (NEVER re-encrypted); fail-closed invariants (no plaintext fallback, no key material in logs, NEVER refuse boot on rollback - F3); AES-256-GCM/Ed25519/HKDF-SHA256 frozen. The security trio is deliberately OUTSIDE the barrel |
| sdw | Selective-Disclosure | canonical | The SDW (Sovereign Data / working-state) store with a STRUCTURAL secret-provenance boundary: a taint lattice + write-gate that refuses to persist forbidding-taint values; encrypted catalog/corpus/query-history/working-state stores; LMDB backend; replay anchors; Rung-1 Claude Code and Codex memory-file mirror adapters (`adapters/claude-code-file-adapter.ts`, `adapters/codex-memory-file-adapter.ts`) and manual memory portability tools (`memory-file-tools.ts`, CLI `memory_ingest`/`memory_emit`/`memory_transcode`/`memory_transcode_restore`) that mirror harness plaintext into the encrypted vault, emit same-format plaintext, or create a cross-harness plaintext projection plus a versioned encrypted archive for exact source recovery. Exit V2 adds an explicit local library API and the operator-directed `memory_archive_export`/`memory_archive_import` CLI composition for a selected completed transcode archive: a fresh per-artifact transfer key encrypts the validated logical archive; a local macOS dialog, not terminal presence, gates approval and hidden key custody; both verbs are force-pinned Tier 1; and import authenticates before any destination write, then creates destination-local ids plus signed lineage in one atomic passage batch. Other platforms fail closed until they have an equivalent local-human dialog. No automatic carriage path is added. | `cognitive/state-store` (general state); core (provides the encryption sdw uses) | Yes | namespaces `_sdw_catalog`/`_sdw_document_corpus`/`_sdw_query_history`/`_sdw_vector_memory`/`_sdw_working_state`/`_sdw_meta`; HKDF `sdw-*-v1`; `sdw_export`/`sdw_import` (`tools.ts`, `createSdwTools`) are wired into the production composition root (index.ts, 2026-08-22) as non-relaxable Tier 1 (`NON_RELAXABLE_SDW_VAULT_TIER1_OPERATIONS`, spread into both the loader and gate forced lists): `sdw_export` writes a signed, still-encrypted bundle into a fresh per-export directory under the fortress's own `sdw-exports/` (operator-directed archive, never a carriage path; the shipped filesystem fortress is enumerated through `StorageSnapshotSdwInventorySource`, which re-reads the live store before every scope enumeration). Containment is BOUNDED: every path component is refused if a symlink, the fresh directory's inode and realpath are rechecked immediately before the O_EXCL open and immediately after the rename, and anything written is unlinked on a change; a swap inside the window between a recheck and the next syscall is not detected (no openat/renameat in Node). `sdw_import` is all-or-nothing inside one transaction and refuses with `storage_not_transactional` at gate time on the shipped filesystem backend (no transaction primitive); only the `this-fortress` source key ref resolves. `sdw_export_delete` (`createSdwExportDeleteTool`, unchanged from its pre-wiring form) is NOT wired: it needs a backend-wide serialization boundary that does not exist yet. Both shipped vault tools run the shared isolation guard at gate time, before any enumeration or prompt. The SDW export signing domains live in `core/signing-domains.ts`, whose refusal list covers the domains enumerated in this change (SDW manifest, state envelopes, audit events, receipts, audit checkpoints); other identity-signed domains exist (operator-signed v1, sign-challenge, bridge, reputation, memory-attest, exit-lineage) and are tracked separately; `identity_sign` refuses payloads carrying them. Exit V2's selected completed-transcode-archive path is the narrower, per-artifact composition. Also live: the sovereign-memory substrate `memory_insert`/`memory_get`/`memory_search`/`memory_list`/`memory_count`/`memory_delete` + `sdw_memory_provenance` (company-brain phase 1, wired 2026-06-18 via `memory-tools.ts`/`memory-provenance-tool.ts`/`adapters/sdw-memory-backend.ts`; insert + delete Tier 1, delete force-pinned un-relaxable, insert body redacted from the approval channel), and the four manual memory portability commands (Codex reads exactly `memories/{MEMORY.md,memory_summary.md,raw_memories.md}` and never its SQLite/telemetry siblings; cross-harness projections may be structurally lossy and exact restoration uses the encrypted archive; no watch/sync/automatic propagation path; recoverable failures verify rollback and unverifiable cleanup reports partial_scope; per-file classifier skips are named instead of aborting; an explicit, per-file, audited `--allow-file`/`allow_files` escape hatch (`adapters/memory-file-allow-list.ts`'s `screenMemoryFileEntries` and audit op `MEMORY_INGEST_CLASSIFIER_OVERRIDE`, shared by both harness adapters so the two cannot drift on the classifier_reject-only scoping) ingests one named, exact-match source file despite a classifier refusal, retains the refusal metadata (detector, line) instead of discarding it, and reports an allow-listed path the classifier never refused as unused; there is no global bypass; re-ingest REPLACES by a harness-distinct fortress-keyed id so the topic name never becomes a storage key. The waiver is an opaque `ClassifierOverrideAuthorization` token (write-gate.ts): the object itself is Object.frozen and carries no field a holder could read or mutate, and its bound content hash lives only in a module-private WeakMap keyed by the token, so a look-alike object or a mutated field never verifies. It is mintable ONLY from `memory-file-allow-list.ts` (structurally pinned by a src/-wide reference scan in sdw-architecture.test.ts), and re-scoping it to one record (deriveClassifierOverrideAuthorization) requires that record's text be a literal substring of the text the parent authorization covers, so a genuine parent token can never be used to authorize unrelated content. The per-file override audit record is durably written BEFORE the vault commit, not after, so a crash between them cannot leave waived content unrecorded. A rollback of a previously-waived passage restores the EXACT captured ciphertext bytes verbatim (`restorePriorChunk`/`restorePriorDocument`, verified against the captured content hash before writing), never a fresh re-encryption of decrypted plaintext and never through the classifying write path. A `memory_ingest` call naming `allow_files` is ALWAYS Tier 1 at the gate (`principal-policy/gate.ts`), even under a hand-authored policy that relaxed plain `memory_ingest` to Tier 3. `memory-isolation.ts` holds the shared multi-agent isolation guard: index.ts builds ONE instance for every tool family over the shared `fleet-self` scope (memory, memory-file, provenance AND vault tools), because a per-family guard pins its own first caller and would let an agent refused by `memory_get` dump the corpus through `memory_emit`; the identity is `SANCTUARY_AGENT_ID`, which `sanctuary wrap` writes (`wrappedAgentId` = `<harness>:<fortress-id>`) into the harness MCP entry and `wrappedAgentIdentityFromEnv` resolves. BOUND: the pin lives in the server process, so the guard separates distinct identities reaching ONE process; two harnesses over one fortress run separate processes and are not separated (IC-16 open; a cross-process design needs a create-if-absent pin plus a true compare-and-swap), and the value is plaintext in the harness-controlled config, so the guard does not defend against an agent that rewrites its own harness config; taint-lattice monotonicity + the honest non-retroactive precondition. Has the ONLY pre-existing README in the tree |
| templates | Operational | canonical | The onboarding TEMPLATE library: five named role bundles (research-assistant, coding-assistant, ops-runner, planner, handoff-coordinator) that compile into a starter Principal Policy + egress/budget/retention defaults | `context-gate-templates` + `principal-policy-templates` (internal YAML fragments); policy-engine (the authoring engine) | Yes | `TEMPLATE_NAMES` list (operator-selectable ids); each maps to a per-role subdir of NON-TS bundle assets under `templates/<name>/` (invisible to the import graph - a dir move can orphan them); `TemplateValidationError` |
| cognitive | Cognitive | canonical | Castle Wall / Cognitive Sovereignty: the encrypted, signed, Merkle-verified state store + identity root (Ed25519) + memory-attestation tools. The foundation every layer reads/writes through | operational (l1 = what is stored; l2 = the record it happened) | No | tools identity_create/import/list/sign/verify/rotate/set_primary, state_write/read/list/delete/export/import, state_disclose_unattributed, memory_attest; domains `sanctuary.audit.v1`/`sanctuary.receipt.v1`/`sanctuary.state-envelope.v1`; persisted `state-envelope-public-keys-v1`, `STATE_ENVELOPE_SCHEMA_VERSION=2`; state_export/import Tier-1 gated; state_disclose_unattributed is Tier-1 NON-RELAXABLE (force-pinned in principal-policy's loader + runtime gate), audited per call, read-only, and returns `UnattributedStateDisclosure`, a shape deliberately non-assignable to `ReadResult` in both directions -- `unattributed-disclosure.ts` is the one place that performs it, shared by the MCP tool and `cli/state-disclose.ts`. The CLI verb writes the disclosed content byte-for-byte to a file under the fortress `disclosures/` dir and prints only a bounded printable-ASCII receipt naming it; content never reaches the terminal, and TTY `--json` refuses. Dynamically imported (rename grep must catch dynamic specifiers) |
| operational | Operational | canonical | Sentinels / Operational Isolation: the append-only encrypted audit log (`audit-log.ts`), process-hardening, the call-governor, model-provenance, privacy filter/placeholder vault, the context-gate (egress enforcement), task-coordination sub-module. `audit-store-split.ts` (F2 Option A, 2026-07-14) fixes shared-fortress audit contamination: on an armed box the root Castle Wall daemon writes its OWN `_audit-daemon` chain (via a `DaemonAuditStorageAdapter` namespace remap over the same `FilesystemStorage`: `_audit`->`_audit-daemon`, `_audit_checkpoints`->`_audit-daemon_checkpoints`, `_meta`->`_audit-daemon_meta`), never the shared `_audit` the operator CLI reads; `migrateFortressAuditStoreSplit` (root-only, idempotent, crash-safe) seals a pre-existing mixed chain behind a MAC-authenticated, operator-readable split-boundary record (`audit-log.ts`'s `writeAuditStoreSplitBoundary`/`readAuditStoreSplitBoundary`, raw file at `<fortress>/state/_audit_migration/boundary-v1.json`, deliberately outside the encrypted StorageBackend contract) that `AuditLog.ensureLoaded()` itself consults (`AuditLogConfig.consultSplitBoundary`, default true) to skip re-reading the sealed legacy region rather than weakening the integrity check; `verifyFortressAuditFullPicture` is the honest dual-chain reader (reports `present_unreadable`, never `verified`, when the daemon chain exists but this privilege cannot open it). Adversarial-gate hardening (2026-07-14): a present-but-invalid or deleted-after-migration boundary FAILS CLOSED (`split_boundary_invalid`/`split_boundary_missing` findings + rotation-anchor TOFU suppressed, so a truncated suffix is never blessed); deletion of a sealed entry is caught from the directory listing (`sealed_prefix_incomplete`); `verifySealedLegacyPrefix` is the root-capable crypto re-walk of the sealed region (needs read access, not decryption) that backs `audit-store-status`'s `sealed_region` verdict and reserves a bare operator "verified" for a fully-verified sealed region; the Castle Wall audit readers (`runAuditDump`/`runAuditVerify`, `audit-chain export`) are chain-aware (include `_audit-daemon` when readable, mark output INCOMPLETE when it exists but is unreadable, `--operator-only` gate on export); master rotation refuses BY NAME on the `_audit-daemon*` namespaces (the boundary MAC is keyed off the rotating master and is not re-stamped, so rotation must stay refused until that lands). Second adversarial-gate round (2026-07-14): the boundary carries a MAC'd `sealed_base_sequence` so deletion of the LOWEST sealed entry is caught too; a durable MAC'd `_meta` migration-established marker (`audit-store-split-established-v1`, NOT co-deletable with the `_audit-daemon*` set) makes boundary-loss fail closed even when all daemon namespaces are stripped, and also drives the master-rotation refusal on its own; `runAuditFindings` runs the crypto walk and NEVER prints "verifies clean" over an unverified/tampered sealed region; `verifyFortressAuditFullPicture` reports daemon `missing` (a finding, fail-closed) when a valid boundary names a now-deleted daemon chain; `doctor`'s audit-chain check refuses OK on a migrated fortress; and `collectDaemonCastleWallEntries` surfaces (never discards) daemon-chain integrity findings so a readable-but-tampered daemon chain reads `complete:false` | cognitive (l2 records, l1 stores); audit/ (posture); cli/audit*.ts (commands). FOUR distinct "audit" things | No (sub-barrels only) | tools `l2_hardening_status`/`l2_verify_isolation`, governor_*, context_gate_*; `AuditEntry.layer: 'l1'..'l4'`; HKDF labels `l2-context-gate`/`l2-privacy-*`/`audit-log`/`audit-head-anchor`/`audit-rotation-anchor`/`audit-store-split-boundary`; persisted `__custody_epoch_keys`; context-gate template ids; new-in-F2 namespaces `_audit-daemon`/`_audit-daemon_checkpoints`/`_audit-daemon_meta` (root-owned, additive, never touch the frozen `_audit`/`_audit_checkpoints` namespaces). 100+ files reach `audit-log.js` directly - broad rename impact |
| disclosure | Selective-Disclosure | canonical | Charter / Selective Disclosure: SHA-256 commitments, disclosure policies, real Ristretto255 ZK proofs (Pedersen/Schnorr/range), and the pluggable Secret Broker (macOS Keychain backend, scoped tokens) | l2 context-gate (l3 = peer-proof; l2 context-gate = egress redaction) | No | tools proof_commitment/proof_reveal/disclosure_set_policy/disclosure_evaluate/zk_commit/zk_prove/zk_verify/zk_range_prove/zk_range_verify; HKDF `l3-policies`/`l3-commitments`; ZK invariants (NUMS generator H; commitment = SHA-256(value‖blinding)); broker: raw creds never touch disk; dynamic import `./disclosure/broker/open.js` |
| reputation | Verifiable-Reputation | canonical | Heralds / Verifiable Reputation: Ed25519-signed EAS-compatible attestations of interaction OUTCOMES; queries return aggregates only; sovereignty-tier weighting; export/import; trust bootstrapping (escrow + guarantees) | l2 audit log (l4 = portable cross-agent; l2 = local operation log); l1 attestations | No | tools reputation_record/query/export/import/query_weighted, bootstrap_create_escrow/provide_guarantee, reputation_publish; HKDF `l4-reputation`/`identity-encryption`; EAS format; `sovereignty_tier` enum + `TIER_WEIGHTS`; `reputation_publish` emits frozen display labels 'L1'..'L4' + 'Cognitive Sovereignty' etc. (user-visible, NOT renamable); queries-return-aggregates invariant |
| cli | CLI | canonical | The per-subcommand handler library for the `sanctuary` binary: one `run*Command` per subcommand (status, doctor, audit, identity, federation, transparency, secrets, sentinel, anomaly, policy, etc.), including `memory-archive.ts` for explicit Exit V2 SDW archive carriage, + the large `castle-wall.ts` command (+ `castle-wall-custody.ts` for `repair-custody`) + the `agents/` multi-tenant sub-CLI; the `audit-chain` verbs (`audit-chain-export.ts`, `audit-chain-verify.ts`, `audit-chain-repair-plan.ts`) run OFFLINE against a fortress directory, deliberately not through the MCP server, because the chains they diagnose are the ones that stop the server booting; `local-fortress-unlock.ts` is the ONE credential-resolution + master-unlock chokepoint the ordinary local memory verbs (`memory_ingest`/`memory_emit`/`memory_transcode`/`memory_transcode_restore`, memory-archive export/import) share, trying argv/stdin/env credentials first, then falling back read-only to the EXACT-fortress OS-keyring/fallback-file passphrase so a host `sanctuary protect` already provisioned can run its first memory verb hands-free, and never minting custody; `hidden-prompt.ts` is the ONE no-echo hidden-TTY-prompt reader `secrets.ts`, `reset-passphrase.ts`, and `restore-attest.ts` share (TTY-only by contract, refuses piped/non-raw-mode-capable stdin, best-effort zeroization of its own decode buffer) so a passphrase or recovery key is never hand-copied into a second echo-suppression implementation | root `cli.ts` (the FILE = the argv router that lazy-imports these handlers) | No (sub-barrels only) | `TOP_LEVEL_SUBCOMMANDS` string array (the public command surface); basename-dispatched bins `verify-exit-bundle`/`import-exit-bundle`/`verify-transparency`; `cli/transparency.ts` anchoring-off-by-default; many dynamic-import path strings `./cli/<name>.js` (a rename grep must cover them) |

Gaps note: none. All 61 module directories under `server/src` (verified by `ls`) have a row above,
and every row maps to a real directory (verified by diff). No best-effort placeholder rows were needed.

**Two reading notes.** (1) The `default-off-allocated` modules - `composition`, `substrate`,
`workload-lifecycle`, `bridge`, `key-17` - are real code but DORMANT in a default install; they do
nothing at runtime unless explicitly opted in (so "why doesn't my composition/workload event fire?"
usually means that subsystem is one of these). (2) Importer counts and god-file line counts here are
a SNAPSHOT and drift with every merge - run `npm run refresh-reorg-evidence` for live numbers and
`npm run check-import-cycles` for the live dependency-cycle baseline.

---

### Deployment-held SDW memory provenance migration

Memory Integrity C3 adds the operator-invoked, force-pinned Tier-1
`MI_C_SDW_MEMORY_PROVENANCE_V1` migration inside the existing `sdw` module. It
uses a bounded encrypted journal, honest `legacy_unattested` companions,
replace-in-place quarantine, replay-anchored completion/repair, and
completed-state unsigned refusal. It never auto-starts on boot, reads, or
upgrade. C2+C3 remain one populated-fortress deployment unit; this adds no
Exit V2 carriage, foreign tiers, fleet sync, capsule runtime, release, or
public capability claim.

Memory Integrity C4 also provides the operator-invoked, force-pinned Tier-1
`memory_provenance_prune_signers` maintenance path in
`sdw/memory-provenance-signer-prune.ts` and its focused MCP facade. The CLI
reuses the same bounded authenticated mark-and-sweep of the separately capped
`memprov.` signer partition under the shared Exit/corpus locks and durable Exit
journal. It does not run automatically and introduces no new HKDF label.

## Confusable clusters - read these if a name confuses you

The confusion in this tree is overwhelmingly LEXICAL, not structural. Cross-imports were verified;
these subsystems are largely independent. The fix is documentation, not deletion or merging.

### Policy / enforcement

- **principal-policy vs policy-engine.** principal-policy is the RUNTIME human-in-the-loop gate
  (Tier 1/2/3, baseline anomaly, approval channels, unified inbox). policy-engine asks NO human: it
  is the authoring-time English-to-rules compiler plus automated machine gates (slot/egress/budget/
  commitment). Confirmed: `principal-policy/gate.ts` calls `channel.requestApproval()`;
  `policy-engine/gates/index.ts` forbids network/LLM imports and never prompts. Both remain canonical.
- **sentinel (the watcher) vs policy-engine/sentinel-role.ts (the capability flag).** Same word,
  two unrelated subsystems: the sentinel module is the observer runtime; sentinel-role.ts is the
  declarative `is_sentinel` constraint ("inward-facing only", denial `sentinel_inward_restriction`).
- **detect vs decide vs gate.** sentinel / anomaly-detection / honeypot DETECT and emit a
  SentinelFinding; auto-trigger DECIDES what to do (3-rung escalation + the frozen rule-id wire
  format that is also the AAD); principal-policy is the upstream human-approval GATE that runs before
  a tool executes. Three stages - do not collapse them.
- **lockdown vs principal-policy gate.** lockdown is a coarse GLOBAL status flag (one boolean
  in `lockdown/status.json`, read by CLI/banner surfaces today); the gate is per-operation
  GRADUATED consent. lockdown is a 1-file tripwire, not an approval engine.

### Recording / posture (CLUSTER 1 - the worst trap, four "audit" things)

- **audit/** - POSTURE assessment (read-only scan -> 0-100 score + gap analysis) AND the shared
  audit-chain hash/checkpoint crypto in `chain.ts`. Scores locally; never anchors externally.
- **transparency/** - tamper-EVIDENT, externally-anchored record (Sigstore/Rekor + signed
  enforcement-checkpoint chain + anti-rollback floor). Proves NON-ROLLBACK to a third party against a
  PUBLIC log. Fail-closed, opt-in. It hashes enforcement CHECKPOINTS, not log entries.
- **attestation/** - the operator-facing BADGE UX (green/yellow/red/offline/local_only + 9-row
  failure catalog + degrade-not-destroy). Presentation/derivation, not a log or a score. The raw
  host/workload attestation PRIMITIVE is NOT here - it is in `workload-lifecycle/host-attestation.ts`.
- **operational/audit-log.ts** - the easily-missed 4th "audit": the actual append-only ENCRYPTED
  LOG of every operation. It IMPORTS and applies the hash primitives that `audit/chain.ts` DEFINES.
- Crisp rule: **audit = score it; audit-log = store it; audit/chain = hash it; transparency =
  anchor+prove it; attestation = badge it.**

### How-am-I-doing surfaces (CLUSTER 2)

- **audit/** = self-scored 0-100 posture gap report (the opinionated diagnostic).
- **health/** = a single un-scored, un-signed, in-process runtime EVIDENCE snapshot (the factual
  internal snapshot; `buildHealthEvidenceReport`, 1 file).
- **shr/** = a SIGNED, versioned, portable capability ADVERTISEMENT an agent hands a counterparty to
  PROVE posture without being trusted (the external cryptographic claim).
- **principal-policy/posture.ts + posture-routes.ts** = the operator-facing posture HTTP
  DASHBOARD (the live human UI surface). Since the dashboard one-surface FOLD (ratified 2026-08-02;
  builds on the 2026-06-30 default-flip) there is **ONE production serving surface: the
  `DashboardApprovalChannel`** (`principal-policy/dashboard.ts`, the channel used by the MCP
  server boot path and `sanctuary dashboard`). The v1.1 concierge is the single default page at
  `/`; the static posture board shell is preserved at `/posture` (and folded into the concierge via
  the seal-click expand), still carries no posture data, and fetches the evidence from
  `/api/posture/*`, which stay behind `checkAuth`. `sanctuary protect` no longer spawns the
  separate wrap server (`dashboard/api.ts`): it ensures-and-reuses the principal-policy dashboard
  (`wrap/cli.ts:ensureMainDashboardForWrap`), which now also serves the formerly-wrap-only groups
  under ITS stricter auth: read-only `GET /api/fleet/roster`, `/api/templates` (init behind the
  Tier-1 approval gate), and the `POST /api/approvals/:id/(allow|deny)` alias onto the same
  `handleDecision` as `/api/approve/:id` + `/api/deny/:id`. Those folded READS additionally
  refuse loopback auto-auth (`isFoldedOperatorReadRoute` in `principal-policy/dashboard.ts`):
  they admit an operator-derived credential only (the bearer, or a session/cookie minted from
  it), never network position, because a co-resident agent shares the loopback interface.
  `isOperatorOnlyReadRoute` is the CHOKEPOINT the request gate consults for that class, and it
  adds the fleet-posture reads that predate the fold and carried the same exposure:
  `GET /api/fleet/status`, `/api/fleet/capacity`, `/api/fleet/downgrade-log`. Their one non-browser
  client, `sanctuary fleet attest export` (`cli/fleet.ts`), presents the operator bearer through
  `cli/dashboard-request.ts` (`SANCTUARY_DASHBOARD_AUTH_TOKEN`) and FAILS LOUDLY on a refusal
  rather than signing an attestation that reports an unmeasured roster as absent. `GET /api/pending`
  (the approval queue) is in that same set; it landed last because gating it alone would have
  silently emptied the operator's inbox, so it ships with the posture-home client change that
  renders an explicit LOCKED state (`posture-home-html.ts`: `lockedApprovalsHTML`,
  `approvalsWaitingValue`) instead of the calm "nothing needs you" over a queue it was refused.
  `GET /api/approval-inbox*` (the cross-harness inbox: every waiting operation across ALL wrapped
  harnesses, the resolved decision history, and the per-entry decrypted payload) carries the SAME
  rule but is NOT reachable from `isOperatorOnlyReadRoute`: `handleRequest` routes that prefix to
  `dispatchApprovalInbox` before `handleLegacyRequest` runs, so the gate lives at the dispatch site
  (the `dispatchDistress` precedent) and a path added to the predicate would be dead code that reads
  like coverage. It is gated through `checkAuth` rather than the router's own `authMiddleware`
  because that middleware reads neither `?session=` nor the `sanctuary_session` cookie, so
  tightening it there would 401 the operator's own browser. Same client pairing: a denial on EITHER
  approval source now sets the one `locked` flag, and the per-row "review" anchor routes its href
  through `credentialedPath` so the link is not a naked 401.
  The mobile PWA `/m/*` is ALSO served
  here, but its shell/manifest/service-worker are deliberately PUBLIC static assets (client-side
  auth, the same posture as the concierge SPA and as the wrap surface served them); only the PWA's
  DATA routes (`/api/pending` + the decision routes) sit behind the auth gate. Approvals
  are LIVE on this one surface; `/api/status`'s `decision_capable` is derived honestly
  (`!standalone_mode`), never hardcoded. The retired wrap router keeps its exports + tests only
  (see the `dashboard` row). Do NOT
  confuse this dashboard posture with the
  unauthenticated `/api/health` probe: `/api/health` is a cheap O(1) liveness answer carrying ONLY
  `{ ok, mode, instance, since }` (no arm-state, no audit scan) - `instance` is an opaque per-process
  boot id and `since` is the process start time (`dashboard/process-identity.ts`, a single shared
  source across all three health handlers), so the host app can detect a restart honestly. The
  readiness/supervisor signal is AUTH-GATED on the SEPARATE `/api/readiness` endpoint, NEVER on
  `/api/health`: an unauthenticated `ready: locked` would be a co-resident-agent oracle for fortress
  unlock state (Dashboard Server Lifecycle Hardening brief HIGH-1). The detailed evidence-gated
  Castle Wall arm-state is served exclusively behind auth, via `/api/posture/castle-wall` and the
  SESSION_TOKEN-gated `/v1/status` document. The Phase 2 Evidence View (`posture-evidence-html.ts`, `GET /posture/evidence` HTML +
  `GET /api/posture/evidence` JSON) is the third IA level: a filterable, operator-gated audit-entry
  table with integrity_findings surfaced on-view; it reuses `AuditLog.query()` verbatim and adds no
  new backend query logic.
- Crisp rule: **audit scores you, health snapshots you (internally), shr signs-and-advertises you
  (externally), posture shows you (in a dashboard).**

### Networking (mesh / federation / v1 split-brain + coordination / hub / composition)

- **mesh** = the INTRA-operator fabric (one operator's many nodes; libp2p, join-ceremony,
  trust-root). It is internally titled "Sanctuary Federation Protocol v0.1" - **this is the root of
  the whole cluster's name confusion.** A new engineer reading "Federation Protocol" will land in
  mesh/, not federation/. It is the gravitational center (one of the most-imported subsystems).
- **federation/** (the dir) = INTER-instance MCP-to-MCP peer trust (handshake-gated peer registry +
  the `federation_*` tools). Tiny, 3 files. Shares NO types with mesh.
- **v1** owns the HTTP `/v1/*` routes including `/v1/federation/*` - and the documented "federation
  split-brain" is THIS: the `/v1/federation/*` routes wrap MESH lifecycle primitives
  (bootstrap-token, MeshNode join); they do NOT touch the federation/ dir. Verified: NO import edge
  between v1/federation.ts and federation/. Do NOT physically consolidate `v1/federation*.ts` into
  federation/ (§3 Cluster C).
- **coordination** = LOCAL point-to-point agent-to-agent handoff protocol. **hub** = the many-agent
  AGGREGATOR (registry + inbox + activity + Tier-1 control). Aggregator vs handoff.
- **composition** = the DEFAULT-OFF Concordia/Verascore sidecar subsystem (reserved `_composition`
  namespace, `composition_*` events per federation spec §10.3). NOT dead despite just 1 static importer (`concierge-query-grammar.ts`) -
  deleting it is product removal. Beware the unrelated `composition` key in index.ts's SIM
  capabilities descriptor (a name collision).
- Tool-name correction: the federation third tool is `federation_status` (NOT the
  `federation_exchange_reputation` the scoping doc implied).

### Recovery (four "recovery" things on one word)

- **recovery/** = the abstract guardian-threshold cascade + DMswitch + multi-principal boundary
  (`initRecovery`/`executeRecovery`). Imports guardian types + canonical-JSON FROM mesh (a real edge).
- **mesh/recovery-flows/** = the concrete mesh-wire ceremonies (device-loss, node-revoke,
  canonical-audit promotion, master-rotation broadcast-with-acks). recovery/ = the engine;
  recovery-flows = the wire ceremonies.
- **core/ master-rotation + master-custody + anti-rollback** = the at-rest MASTER-KEY rotation/
  custody mechanics. recovery/ authorizes a rotation; core/ actually rotates the key material.
- **wrap recovery-key flows** = per-wrap unlock material for one protected workload. Unrelated to the
  fortress-level guardian cascade.

### Enforcement-surface (castle-wall / fortress / wrap / supervisor / mcp / lockdown)

This is the most onboarding-hostile collision.

- **castle-wall** = the IN-SERVER (TypeScript) enforcement surface: allowlist schema, IPC wire
  contract, audit-event ingestion with producer-signature verify, in-process egress CONNECT proxy.
  It only types, frames, and audits the native enforcers - it is not the OS filter.
- **fortress** = the agent's posture MODE-TIER state machine (private/federated/interop, which mesh
  bits are live). Capability posture, NOT network enforcement. Distinct from the operator word
  "fortress" meaning the on-disk `SANCTUARY_FORTRESS_PATH` directory.
- **wrap** = the INSTALL-TIME `sanctuary wrap`/`init` CLI that rewrites a harness config so Sanctuary
  becomes the upstream gateway, plus custody flows. One-shot installer.
- **supervisor** = the RUNTIME split process that launches/monitors/restarts the wrapped child and
  holds the transient master key over an authenticated socket. Process + key custody, not egress.
- **broker-mcp** = ONLY a standalone Broker MCP server (four `broker/*` tools) adapting the L3 broker.
  It is **NOT** the core MCP tool-router (that is root `index.ts`/`router.ts`/`sanctuary-tools.ts`).
  Renamed from `mcp/` (2026-06-14) precisely because the old name read as core MCP plumbing.
- **lockdown** = a tiny status flag (`lockdown/status.json` + a banner). Not an enforcer, posture
  machine, installer, or process manager.

### Intelligence / IO (intelligence / query-anonymity / concierge / chat / proxy / substrate)

- **intelligence** = LLM substrate ROUTING (the SubstrateSelector binds each surface to
  Local/Venice/Frontier/Disabled). The only thing that "selects a substrate."
- **substrate/** = the PLUGIN-HOST signed-contract algebra (0 importers, default-off, slice S1).
  Never routes LLMs. One keystroke from `intelligence/substrates/` and totally unrelated.
- **concierge** = a stateless read-only Q&A assistant whose CLI-local and hub routes both delegate
  to the selector at `surface:"concierge"`; it owns context assembly, not provider choice.
- **chat/operator-chat** = the PERSISTED, threaded operator conversation that routes its concierge
  sub-surface THROUGH the selector at `surface:"concierge"`. chat ALSO carries the separate
  agent-to-agent encrypted MESH group chat - the two halves split by barrel (`index.ts` =
  mesh chat, `operator-chat-index.ts` = operator chat).
- **proxy** = wrapping UPSTREAM third-party MCP tool servers under `proxy/{server}/{tool}` and
  forcing them through the enforcement chain.
- **query-anonymity** = scrubbing IDENTITY (fingerprinting headers + PII in content, reversible) out
  of outbound requests; only borrows the selector for its optional Tier-B rewrite.
- There is exactly one Venice client: `intelligence/substrates/venice.ts`, constructed only by the
  selector. The concierge module must never regain a provider-client constructor.

### Console / dashboard (web surfaces)

- **console/** = the Operator Console v1.0 CONTROL surface (`/console` + `/api/console/*`, six views)
  AND the shared `enforceAuth` middleware that ~12 route files import. Its assets live OUTSIDE src at
  `server/public/console/`.
- **dashboard/** = the read-mostly Sovereignty "hero-shield" STATUS surface (port 3501, HERO_COPY,
  L1-L4 state) + the `/v1.1` + `/api/hub/*` bindings via its `v1_1/` subdir.
- **principal-policy/dashboard.ts** = a THIRD, separate web server: the Principal Dashboard APPROVAL
  channel (`POST /api/approve/:id`, blocks the MCP tool call on a Promise). It is NOT in `dashboard/`.
- Landmine: the root loose file **dashboard-standalone.ts** is named "dashboard" but actually boots
  the principal-policy approval channel, not the `dashboard/` module.

### Identity / agent (agent-contract / agent-native / handshake / contracts / recognition / key-17)

- **agent-contract** = the SIGNED per-agent identity (Agent Cards, capability grants, six-state
  lifecycle; emits events, registers NO tools).
- **agent-native** = the LIVE agent-facing tool facade (sanctuary_remember/recall/who_am_i/...) the
  agent actually calls. Its `safety-base.ts` is a SHARED crypto util, not facade-local.
- **handshake** = INTER-fortress two-party liveness verification (nonce challenge-response + SHR
  exchange, 120s TTL).
- **contracts** = versioned cross-workstream EVENT/RECORD type bundles (`v1.1/`, `v1.2/`). The
  singular/plural trap: agent-contract = one agent's signed contract; contracts/ = shared wire types.
- **recognition** = one-way did:web identity PUBLICATION (DNS+TLS trust, no liveness). Owns the
  FROZEN external route `GET /<handle>/.well-known/did.json` from `identity.sanctuaryprotocol.ai` -
  any reorg needs a route-smoke gate (D3).
- **key-17** = **non-self-describing.** The cross-protocol sovereign SIGNER for three external
  standards (x402 payments, ERC-8004 agent-identity, AP2 mandates). It DERIVES isolated subkeys from
  core's master key. A new engineer MUST be told this every time the name appears. Distinct from core
  (master key), recognition (DID docs), handshake (nonces).

### The four sovereignty layers (state vs log vs disclosure vs reputation)

- **cognitive** owns the encrypted at-rest STATE + identity keys (`StateStore`).
- **operational** owns the tamper-evident LOG of operations against that state (`AuditLog`), plus
  the governor and the context-gate. Every l1 write produces an l2 audit entry - partners, not dups.
  Give-away: l1 holds `state_*`/`identity_*` tools; l2 holds the audit log + governor + context-gate.
- **l2 context-gate vs disclosure**: context-gate is AGENT-TO-INFRASTRUCTURE egress control
  (redact what leaves on an outbound provider call); disclosure is AGENT-TO-AGENT cryptographic
  disclosure (commitments, ZK proofs, disclosure policies to a peer). Bytes-leaving vs prove-to-peer.
- **reputation vs l2 audit log**: l4 produces PORTABLE, EAS-format, signed reputation attestations
  about interaction OUTCOMES (queried as weighted aggregates); the l2 log is the LOCAL, principal-only,
  append-only operation record that never leaves except via a gated metadata export.

---

## Intentional root surfaces

Eight loose files legitimately stay at `server/src` root so root reads as a deliberate shared surface,
not a junk drawer. Do not move these casually.

| File | Why it stays at root |
|------|----------------------|
| `index.ts` | The server factory + tool-registration hub (~68 KB). Re-exports `StateStore`, `AuditLog`, and `L1Status..L4Status`. |
| `cli.ts` | The binary entry point: parses argv, owns `TOP_LEVEL_SUBCOMMANDS` dispatch, lazy-imports each `cli/` handler. |
| `router.ts` | MCP plumbing (`ToolDefinition`/`toolResult`). Imported as `../router.js` by nearly every module. |
| `config.ts` | `SanctuaryConfig`. Imported widely as `../config.js`. |
| `paths.ts` | `resolveStoragePath` + path constants. Also the INTENDED fortress-default chokepoint going forward: `homeFortressPath()` is where `<home>/.sanctuary` should be composed, and new code must not open-code that join. It is not yet the only one: roughly fifteen sites still compose it directly, including `config.ts` (deliberate, and documented there) and the `wrap/` keychain-naming helpers. `assertHermeticStoragePath()` fails a Vitest run closed when a resolution lands on the operator's own fortress, and `saveConfig` applies the same guard at the config write, so a test can no longer silently read real custody or write into `~/.sanctuary`. Escape hatch `SANCTUARY_ALLOW_OPERATOR_FORTRESS=1`; test-side fix is `test/helpers/temp-fortress.ts`. |
| `strings.ts` | Small, dependency-free string helpers shared across modules that need identical trimming behavior for operator-configured values (endpoints, URLs, filesystem home paths); currently only `stripTrailingSlashes`. The chokepoint rationale: one linear-scan implementation so a copied trailing-slash regex cannot drift back into a call site, imported by `cli/`, `wrap/`, `castle-wall/provision/`, `egress-gate/`, `transparency/`, and `intelligence/`. |
| `claim-witness.ts` | Structural-honesty witness brands and the `auditClaim` chokepoint for claim-bearing audit fields. Distinct from `egress-gate/claim-basis.ts`, which owns the literal ratchet/claim registry and does not mint witnesses or type audit records. |
| `version.ts` | The version constant. |

Note: there are 10 other loose root files (`sanctuary-tools.ts`, `sovereignty-profile.ts`,
`sovereignty-profile-tools.ts`, `dashboard-standalone.ts`, `system-prompt-generator.ts`,
`tool-args.ts`, `update-check.ts`, `release-manifest.ts`, `mcp-child-fortress-refusal.ts`,
`first-run-notice.ts`). These are Phase-2 MOVE candidates, not part of the curated six above.
`dashboard-standalone.ts` is the landmine called out in the console/dashboard cluster - it serves
the principal-policy approval channel, not `dashboard/`. `release-manifest.ts` is the
Ed25519-signed-release-manifest verifier (pinned release key, canonical-JSON signed body,
fail-closed refusal) that `update-check.ts` re-exports; the two move together.
`first-run-notice.ts` (2026-07-05) is the one-time zero-outbound-by-default stderr notice printed
at server startup; it is a fail-open UX helper, not a gate, and has no re-export dependents.

---

## Non-module runtime assets

These are invisible to the TypeScript import graph, so a directory move can silently orphan them.
Each needs an asset-specific smoke gate, not a TS-only check.

| Asset | Owned by | Note |
|-------|----------|------|
| `server/public/console/{index.html,console.js,console.css}` | `console/` (via `resolvePublicDir()` / `serve-static.ts`) | Console UI lives OUTSIDE `src` while console code lives in `src/console`. |
| `server/public/attestation-reference.html` | `attestation/` | Served reference asset (asset-route smoke per round-6). |
| `src/templates/**` (JSON/MD/YAML bundles) + `principal-policy/templates/*.yaml` | `templates/` and principal-policy | Per-role bundle assets under `templates/<name>/`, invisible to imports. |
| `sentinel/sentinels/ebpf/probe-loader.rs` | `sentinel/` | An IN-SERVER Rust PLACEHOLDER owned by sentinel - NOT the castle-wall-daemon. |

---

## Adjacent out-of-scope systems

Named "Castle Wall" but living OUTSIDE `server/src`, at `sanctuary-pro/` repo root. These are the
actual OS-level enforcers; the in-server `castle-wall/` dir only types, frames, and audits them.

| System | Language | Location |
|--------|----------|----------|
| castle-wall-daemon | Rust | `sanctuary-pro/castle-wall-daemon/` |
| castle-wall-macos | Swift | `sanctuary-pro/castle-wall-macos/` |
| castle-wall-vmm | Swift (own CI) | `sanctuary-pro/castle-wall-vmm/` |

Why the L1 layer dir is `cognitive/` (target name) and NOT `castle-wall/`: the name `castle-wall` is
ALREADY TAKEN inside `server/src` by the in-server enforcement dir (`castle-wall/`) and by
`cli/castle-wall.ts`. Renaming the layer dir to `castle-wall/` would collide. The plain-English
subject name `cognitive/` is the safe target (now applied).

---

## Conventions

### Barrel convention

Every module SHOULD expose a thin re-export `index.ts` barrel so consumers import the module surface,
not its internal file layout. Deep imports are the documented exception (used where the surface is
deliberately partial - e.g. `core/` re-exports only the primitives and keeps the master-key security
trio out of the barrel on purpose; `contracts/` uses per-version barrels with no top-level barrel).

**Status today: 53 of 61 modules have a barrel** (backfilled 2026-06-14; file-grant added its own
2026-07-07; evidence-pack shipped with its own barrel 2026-07-10; memory-checkpoint shipped with its
own barrel 2026-08-03). This is additive (adding a
barrel never changes existing deep-import call sites). The 8 without one are intentional or deferred:
the four layer dirs (`cognitive`, `operational`, `disclosure`, `reputation`) get theirs in a
follow-up; `operational`'s in particular shrinks that follow-up's impact (100+ files reach
`audit-log.js` directly); `cli` and `v1` are command / versioned-route surfaces; `compliance` nests
its code under `eu_ai_act/`; and `contracts` deliberately has no top-level barrel (it would falsely
merge two frozen wire versions).

### Frozen surfaces a reorg must never change

Directory names and relative import-path strings may move in the rename phase. The following must
survive byte-for-byte - they are live wire, route, crypto, or display contracts:

- **The did:web route** - `GET /<handle>/.well-known/did.json` (`recognition/`), served from
  `identity.sanctuaryprotocol.ai`. Any `recognition/` reorg needs a route-smoke gate (D3).
- **`l2_*` tool names** - `l2_hardening_status`, `l2_verify_isolation` (and all other frozen MCP tool
  names listed per-row).
- **HKDF labels** - e.g. `l2-context-gate`, `l2-privacy-*`, `l3-policies`, `l3-commitments`,
  `l4-reputation`, `sanctuary-fed-v0.1-*`, `intelligence-substrate-config`, the `key-17:*:v1` labels,
  `l2-recognition-hosted-did-web-v1`. Renaming a dir must NOT touch labels that embed its token.
- **`layers.lN` keys** - `l1`..`l4` and `l1_cognitive`..`l4_reputation` in audit/SIEM/shr/health/JSON.
- **`L1Status..L4Status` exports** (root `index.ts`) and the `'L1'..'L4'` display labels +
  "Cognitive Sovereignty" / "Operational Isolation" / "Selective Disclosure" / "Verifiable
  Reputation" strings emitted by `reputation_publish` (user-visible, not renamable).

The L1..L4 numbering is being retired in PROSE only. The wire tokens above are explicitly carved out
of any L-number-retirement or vocabulary sweep.
