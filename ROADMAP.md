# Sanctuary Roadmap

In the physical world, your body provides the perimeter, the custody, the memory, and the audit trail by default. In the agent world, action moves to substrates that do not carry those guarantees. Sanctuary installs the architecture that does: a wall the operator imposes, keys only the operator holds, and an audit trail nobody can silently edit. The same substrate serves any principal acting through agents: a person, a company, eventually an agent itself.

This roadmap covers what Sanctuary ships today and what's coming next, with rationale for why each piece matters. Detailed shipped history lives in [`CHANGELOG.md`](CHANGELOG.md). Trust claims trace to rows in the [Sanctuary Assurance Matrix](ASSURANCE_MATRIX.md), preserving the platform, gap, and next-proof limits named on each row.

Last updated: 2026-08-02. Freshness is enforced: a CI guard requires feature PRs to update this file (or carry an explicit `roadmap-exempt` label), and a weekly job files a drift issue listing any shipped features not yet reflected here. See `.github/workflows/roadmap-freshness.yml`.

---

## Architecture: the Castle

Sanctuary's enforcement model is the Castle Architecture, codified at [`server/rfcs/RFC-0003-castle-architecture.md`](server/rfcs/RFC-0003-castle-architecture.md). Five named mechanisms, each with a distinct enforcement contract.

- **Castle Wall (the perimeter).** OS-level egress enforcement at the operator-external boundary. The kernel itself blocks unauthorized cross-boundary calls. Even prompt-injected agents cannot bypass. Linux backend proven (shipped 2026-05-06). macOS proven: per-uid allow/deny plus reboot-survival (N=5) on a Dev-ID-signed and notarized binary, drills 2026-06-11 through 2026-06-22; the per-flow rule-attributed audit trail is the named remaining gap. Windows on the roadmap.
- **Sentinels (the nerves).** Internal observation via process introspection and behavioral baselining. Anomalies surface to the operator via menubar and notifications. Observation, not enforcement.
- **Charter (the will).** Cooperative MCP surface for compliant agents. Encrypted state, signed audit, mandate primitives, four canonical policy slots, substrate selector, Concordia receipt integration, Verascore reputation hooks.
- **Heralds (the voice).** Concordia receipts for cross-castle commitments, Verascore reputation aggregating across operators. Cross-castle accountability post-action.
- **Mantle (the unique-substrate-binding).** Install-time substrate-binding. The check that locks Sanctuary to the operator's machine at install time and rejects orphan agent identifiers not bound to a wrapped harness.

Castle-walking principle: real enforcement AND delightful operator experience. Hard enforcement at the wall AND approval response under two seconds. Default-deny outbound AND smart always-allow rules. Sentinels observe AND do not surface noise. The Charter cooperative path remains additive and fully usable.

---

## What's shipping today, and why

### Wrap any AI agent harness in one command

`sanctuary protect` wraps OpenClaw, Hermes, Claude Code, Cursor, and Cline today (others via the `--wrap` flag for any MCP-compatible harness). The operator's existing harness continues to work; Sanctuary adds the substrate underneath, invisibly. **Why it matters:** the install motion has to be one minute or operators bounce. Per-harness compatibility status lives in the [Assurance Matrix](ASSURANCE_MATRIX.md).

*Status: shipped through v1.7.0 (latest release tag, 2026-07-26).*

### Castle Wall on Linux: kernel-level enforcement

OS-level egress filtering via netfilter / NFQUEUE with per-process cgroup routing. Outbound calls blocked at the kernel even when the agent is prompt-injected, jailbroken, or simply not bothering to cooperate. **Why it matters:** this is the load-bearing security claim. Without kernel-level enforcement, the substrate is a polite fiction; with it, the architecture holds under adversarial conditions.

*Status: shipped (Phase 1, 2026-05-06). ASSURANCE_MATRIX row "Egress enforcement: Linux (Castle Wall Phase 1)" is proven.*

### Castle Wall on macOS: signed system extension, enforced and reboot-surviving

The signed system extension, host app, content-filter provider, and retail UX shipped through v1.3.3 and the Tahoe fix rounds. The proven claim, exactly: Castle Wall on macOS enforces a signed operator policy with a clean per-uid allow/deny demonstration (agent blocked off-allowlist, agent reaches its allowlisted destination, operator unaffected, in the same armed window) that survives reboot (N=5 real reboot cycles), on a Dev-ID-signed and notarized binary built from the mainline tree. One-command arming (`enable --agent-uid`, configure-then-arm in a single step) shipped 2026-07-06. **Why it matters:** Macs are where retail operators live; without macOS enforcement parity the security claim is asymmetric.

*Status: proven. ASSURANCE_MATRIX row "Egress enforcement: macOS" is proven; strongest evidence: [boot-survival re-drill 2026-06-22](docs/audit/castle-wall-macos-boot-survival-redrill-2026-06-22.md) plus the [full-scope re-drill 2026-06-20](docs/audit/castle-wall-macos-fullscope-redrill-2026-06-20.md). Honest bounds carried on the row: one host and one OS version so far, and NOT an audited per-rule-per-flow trail; that trail is the top "coming next" item below.*

*Unified Protect / two-confined-uid capability (S5-0, merged #930, drill-proven 2026-07-14): "confine a second account (a policy gate) alongside the wrapped agent, as a distinct principal with its own rule scope" is built AND proven on hardware. The signed manifest carries an optional second uid, the macOS classifier routes it to the same default-deny path as the agent (never the operator allow-all fast-path), and an endpoint rule can bind to one uid without matching the other. Proven operator-present on a Dev-ID-signed and notarized binary (build 1130): the gate account reaches its one declared endpoint while the agent account is denied that same endpoint and the operator is unaffected (per-uid allow/deny, N=3 deterministic), and the two-account confinement survives reboot (N=5 real cycles, the safe-mode boot daemon re-assembling and re-enforcing the two-uid policy each boot). Evidence: [drill-evidence 2026-07-14](../Review/Sanctuary/drill-evidence-2026-07-14/RESULTS.md) (coordinator record). This unblocks Unified Protect Slice 5 (the loopback gate must run as its own confined account). Still owed to WIDEN the ASSURANCE_MATRIX row: the per-flow rule-attributed audit trail (a separate build; Slice M producer-signing remains blocked on an audit-emission code fix), and a second OS family.*

*Unified Protect Slice 5 (install-fusion) foundation, S5-1 locked multi-uid pf-anchor registry (built 2026-07-15, code + host-free unit tests only; NOT wired into install; NO drill): the safe-mutation primitive the fine-grained exclusive-egress wall's install-flow arming will use. It makes the shared `sanctuary.egress-gate` pf anchor a lock-serialized union of all confined uids, so a second confined uid never overwrites the first's rules and unprotecting one agent never strips another's confinement (the review's HIGH-4 destructive-full-replace flaw). Transaction-safe (journaled pending-then-commit, rollback-on-failure, dirty-marker forces posture red) and drift-repairing (reconcile-on-entry), verified by exact-union liveness (arm reuses hook+enable+settle, never a bare load). HONEST BOUND: this LANDS the primitive; it does not CLOSE HIGH-4 (no caller routes through it yet) and makes no capability claim.*

*Unified Protect Slice 5 S5-2, gate-port generation state machine + bind-first helper (built 2026-07-15, code + host-free unit tests only; NOT wired into install; NO drill; arming still unwired until S5-6): the state machine that gives the loopback gate's port a monotonic-per-agent generation, staged through five transitions (G1 bind-first ephemeral port -> G2 owner check -> G3 pf load -> G4 manifest publish -> G5 commit) each with a crash-recovery rule, so a bring-up that dies mid-flight never leaves a green-looking-but-dead gate channel and never reopens non-gate loopback. Bind-first (bind 127.0.0.1:0 and learn the port before any rule references it) closes the pre-bind squat window. Crash recovery block-only TOMBSTONES an uncommitted uid (drops the stale gate pass, keeps its four block-drops, so non-gate loopback stays closed) rather than dropping the whole entry. A three-surface generation-match check refuses traffic unless the pf pass port, the manifest port, and the committed registry port all agree and the manifest generation equals the committed generation; a restarted gate resumes only if it rebinds the committed port with a passing owner check. Adds an ADDITIVE optional `generation_id` to the S5-1 registry entry (version-compatible with v1 state on disk; a legacy entry with no `generation_id` loads unchanged; the state version stays 1). HONEST BOUND: this is the generation LIBRARY over injected ops; it does not provision the gate service uid / client-auth / liveness oracle (S5-3), does not compose the exclusive routing manifest (S5-4; G4 here only publishes the generation-bearing gate policy), does not park/unpark the harness (S5-5), and no caller routes through it yet. Still owed for Slice 5: the posture object + all-surface rendering (S5-P), the gate-as-TCB + client-auth + liveness oracle (S5-3), exclusive routing manifest composition (S5-4), the release barrier (S5-5), the install-flow arming wiring + repair verb (S5-6), unprotect-via-registry (S5-7), and the Erik-present fusion + T+0 + boot-survival + coexistence drill.*

*Unified Protect Slice 5 S5-3, gate-as-TCB: dedicated `sanctuary-gate` uid + fail-closed client auth + root-owned liveness oracle (built 2026-07-15, code + host-free unit tests only; NOT wired into install; NO drill; arming still unwired until S5-6): the trusted-computing-base hardening for the loopback egress gate, which is a machine-local relay and a new TCB member. Three primitives, all libraries over injected ops. (1) The gate runs as its OWN hidden `sanctuary-gate-<agentId>` service account (reusing the shipped agent-account hardening), per-agent so each gate uid is kernel-bounded to only its own agent's endpoints under the S5-0 two-confined-uid model -- never the operator (allow-all) or the agent (kernel-denied). (2) Client authorization is FAIL-CLOSED: every CONNECT must present a generation-bound, current-only bearer credential (root writes a random secret atomically before unpark to a 0600 agent-readable token file plus a 0600 gate-readable accept file holding only the sha256, bound to the committed generation; constant-time compare; a stale generation is rejected before the secret compare so an old token never replays after a new generation commits) AND resolve to the agent uid -- bearer never overrides peer, and an unresolved or capped peer DENIES (a `client_denied` audit event + 403). Honest availability bound: peer-lookup saturation denies the agent's own connects rather than admit an unverified peer. (3) The non-root gate learns pf-liveness WITHOUT holding pf privilege: the root supervisor runs the pf liveness check and publishes a short-TTL, generation-bound, Ed25519-SIGNED freshness token the gate verifies per-CONNECT (deny on absent/expired/bad-signature/binding-mismatch), so no cached green survives a flush (the supervisor invalidates immediately on flush; a lingering token is bounded by the sub-second-to-low-seconds TTL, stated as an honest residual). HONEST BOUND: the fail-closed client-auth hook and the oracle liveness probe are OPT-IN on the gate and NO caller wires them yet; the gate's legacy advisory peer path is unchanged when they are absent. Provisions no real account (drill-only), arms nothing at install (S5-6, owed), and advances no capability claim. Still owed for Slice 5: S5-P posture object + all-surface rendering, S5-4 exclusive routing manifest composition, S5-5 release barrier, S5-6 install-flow arming wiring + repair verb, S5-7 unprotect-via-registry, and the Erik-present fusion + T+0 + boot-survival + coexistence drill.*

*Unified Protect Slice 5 S5-P, exclusive-egress posture object + all-surface coarse-only rendering (built 2026-07-16, code + host-free unit tests only; NO producer wired yet; NO drill): the hard predecessor the design's ordering rule requires before ANY code path can produce a coarse-only outcome. A first-class, queryable per-agent posture object (gate-policy generation, pf-liveness with its full reasons vector, gate process up + port-owner-verified, the three-surface generation match, and a mode of exclusive / coarse-only / unprotected) is built purely from injected probe results over the real S5-1/S5-2/S5-3 state surfaces (the same committed-generation resolver and generation-match check the gate's serve decision uses; the S5-3 liveness oracle's token verdict is the pf probe). One capping rule makes aggregate green honest everywhere: for a fine-grained-provisioned agent, protected-green requires the coarse wall armed AND the exclusive-egress stack live, applied at the single wall-posture builder every surface already consumes, so CLI status, the one-surface dashboard, the feature-health row, the hero shield, the Protect fortress view, /v1/status, and the posture APIs all render a DISTINCT non-green "coarse-only" state (never green, never a vague amber) from the same verdict; the wrap first-run "protected" banner is cap-capable through the same opt-in provider. The home payload resolves the provider once and threads the same snapshot into both the wall reader and the feature-health panel, so an intermittent provider can never cap one home surface while another still shows green. Providers are opt-in and fail-closed: no provider (or a positive "none declared" answer) means no fine-grained agent, behavior unchanged; a failing or indeterminate provider caps green rather than silently reading as healthy. HONEST BOUND: rendering + library only; nothing produces a coarse-only state yet (S5-4 lands the compose-path fallback as an uncalled library; S5-6 wires the flow), and no capability claim advances. Still owed for Slice 5: S5-4 exclusive routing manifest composition, S5-5 release barrier, S5-6 install-flow arming wiring + repair verb, S5-7 unprotect-via-registry, and the Erik-present fusion + T+0 + boot-survival + coexistence drill.*

*Unified Protect Slice 5 S5-4, exclusive routing manifest composition (built 2026-07-16, code + host-free unit tests + cross-language parity vector only; NO caller wires it; NO drill; requires the merged + drill-proven S5-0 two-confined-uid model): the routing-model change that makes fine-grained control real. In exclusive mode the agent's declared endpoint rules are RE-SCOPED AWAY FROM THE AGENT to the dedicated gate account (`scope.uids = [gate_uid]`), the derived gate-channel rule binds to the agent account, and a compose-time assertion fails CLOSED (no signed manifest, no arm) if ANY agent-reachable direct off-box endpoint allow survives composition, so the agent's only sanctioned path off the box is the gate. The assertion classifies every allow by exact shape, never by trusting a rule id: the guaranteed distress lanes and the agent-scoped gate channel are the only justified lanes (the distress webhook is an explicitly disclosed off-box residual, kept outside the gate so a gate compromise can never silence distress), and the auto-derived DNS rule is checked like any other rule, so in a clean exclusive composition the agent gets no direct DNS at all (the gate resolves names). The coarse-only fallback exists only as an explicit, audited mode: it refuses to compose while any gate-scoped rule residue is still on disk (never both routing models at once) and requires a successful audit write under a distinct operation string before it returns, and S5-P already renders that coarse-only state as a distinct non-green status on every posture surface. macOS-only (the per-account `uids` scope is the S5-0 macOS extension; the Linux daemon refuses those rule files fail-closed by design). The signed-manifest byte shape is proven identical across the Node producer and the Swift verifier by a committed parity vector both languages check. HONEST BOUND: composition library only; no install path routes through it yet (S5-6 wires it), it signs and arms nothing, and no capability claim advances. Still owed for Slice 5: S5-5 release barrier, S5-6 install-flow arming wiring + repair verb, S5-7 unprotect-via-registry, and the Erik-present fusion + T+0 + boot-survival + coexistence drill.*

*Unified Protect Slice 5 S5-5, release barrier: disabled-job park + root hold file + exec-wrapper verification (built 2026-07-17, code + host-free unit tests only; arming still unwired until S5-6; NO drill): the guarantee that the confined agent's harness process cannot start before a matching exclusive-egress generation is committed, at install AND at every boot. The harness LaunchDaemon is installed PARKED: rendered disabled with no start-at-load (crash-restart-only KeepAlive) and `launchctl disable`d, so launchd never auto-starts it and it cannot be bootstrapped until the root supervisor explicitly enables it. A root-owned hold file (`/var/db/sanctuary/agent-harness/<uid>.release`, unwritable by the agent account) is written only at the generation commit and names the exact committed generation, agent uid, job label, harness argv digest, and the current boot-session UUID; a static root-owned exec wrapper in front of the harness argv refuses to exec unless every one of those matches at start time, so a mistaken enable, a stale plist, a hold file surviving a reboot, or a swapped argv can never release the agent. The release sequence (re-assert park, re-arm the pf anchor, verify the gate, commit, RE-verify that the committed generation is the live gate generation, write the hold file, re-render the plist with the committed generation -- the parked plist embeds a sentinel the wrapper always refuses -- enable, bootstrap, verify the harness actually reached a stable running state, then RE-disable and re-park the persistent boot state so the next boot starts parked again, verifying the live session survived) removes the hold file, restores the parked plist, and leaves the job disabled on EVERY abort branch, fail-closed toward agent-not-running, never reports released for a harness that is not running, and reports a failed final re-park as a distinct loud non-green outcome. HONEST BOUND: barrier library over injected ops only; no production caller routes through it (S5-6 wires `runProvisionFlow` + the boot daemon, S5-7 unprotect), it parks or releases no real host, the wrapper verifies the argv digest (not binary content), and the launchd park/KeepAlive semantics are asserted by the owed Erik-present drill, not claimed proven. Still owed for Slice 5: S5-6 install-flow arming wiring + repair verb, S5-7 unprotect-via-registry, and the Erik-present fusion + T+0 + boot-survival + coexistence drill (S5-DRILL).*

*Unified Protect Slice 5 S5-6, install-flow arming wiring + repair verb + drift guard (built 2026-07-17, code + host-free unit tests only; NO drill): the wiring slice that retires the egress-gate cluster's "no production caller" staging disclosures. `sudo sanctuary protect --hermes --exclusive-egress` now runs the full fine-grained flow: the harness is PARK-installed (the S5-5 barrier form, asserted fail-closed before proceeding), the coarse stages prove live first (wall armed + as-agent egress verified), then the exclusive-egress generation comes up for real -- gate service account provisioned, gate policy file written, pf anchor armed through the S5-1 locked registry, the manifest re-scoped through the S5-4 exclusive composition (a durable routing marker makes the signing daemon the compose-time chokepoint on every reload), the gate daemon started as its own non-root LaunchDaemon with the S5-3 TCB wiring (oracle liveness probe + fail-closed client auth; single-flight omitted per the merged construction guard), the generation-bound credential minted strictly before unpark, and the S5-5 release barrier sequenced as the ONLY path that starts the agent. Every failure lands in one of two honest states: agent NOT RUNNING (parked), or the DISTINCT non-green `exclusive-egress-unarmed-coarse-active` degrade-loud outcome (the proven coarse wall stays armed, the manifest is explicitly recomposed to coarse scope through the audited S5-4 fallback, and S5-P renders coarse-only amber on every surface -- never silent, never fake-green). `sudo sanctuary protect --repair-egress-gate` first PARKS the possibly-live harness (verified stopped, never a manifest republish under a running agent), then re-runs recovery + bring-up + release behind the MED-7 drift guard: foreign transient pf rules (a VPN's unpersisted rules) REFUSE automatic repair, with an interactive-TTY-only audited override; a failed repair claims PARKED only after the FULL persistent park verification (not running + launchd override-db read-back + hold file absent + parked plist restored). The boot path releases parked agents through the safe-mode daemon (assert the runtime filesystem layout, bootstrap + kickstart the per-uid gate daemon -- its plist is RunAtLoad=false, so the boot supervisor is what starts it -- then re-arm from the registry, verify, recommit, release; per-agent fail-closed, and an unresolvable agent is re-parked for real, never synthetically reported parked), the same daemon owns the PERSISTENT oracle freshness-token cadence by re-scanning the registry each tick (covers agents armed after boot by the install CLI; a host with no resident policy daemon fail-closes -- gates deny within one token TTL), and the S5-P posture producer is live on the dashboard (real marker/registry/staging/runtime/oracle surfaces, fail-closed, pid-reuse-checked port ownership). A root-applied runtime filesystem plan (per-uid gate-owned runtime dir; traversal-without-listing credential/liveness dirs; explicit chown/chmod, never umask luck) makes the non-root gate daemon's state publish and both principals' credential reads actually reachable. HONEST BOUND: arming is wired and per-branch unit-tested (host-free, injected ops); the composed WHOLE FLOW on a real host is S5-DRILL-owed (Erik-present, signed host) and NO capability claim advances until that drill captures evidence. Still owed for Slice 5: S5-7 unprotect-via-registry and the Erik-present fusion + T+0 + boot-survival + coexistence drill (S5-DRILL).*

*Unified Protect Slice 5 S5-7, unprotect via registry (built 2026-07-18, code + host-free unit tests only; NO drill): the reversibility half of the exclusive-egress stack, and the slice that makes Slice 5 CODE-COMPLETE (the Erik-present S5-DRILL stays owed). It handles the SINGLE-EXCLUSIVE-AGENT case (the only state reachable in v1: the exclusive routing marker names one uid and a second exclusive install overwrites the first's marker) and REFUSES fail-closed for a shared-fortress/harness multi-agent config (out of scope until the multi-agent teardown lands). `sudo sanctuary protect --unprotect-egress-gate` runs the WHOLE teardown UNDER THE PROVISION LOCK -- the same O_EXCL lock the arm/repair paths take -- so arm, repair, and unprotect are mutually exclusive and no concurrent `protect --exclusive-egress` can commit a sibling between the checks and the teardown. Under the lock it FIRST asserts the sole-exclusive-agent invariant: it reads the committed non-tombstone registry set and, if any OTHER committed entry shares the leaving uid's fortress or harness (the single-uid marker, the fortress-keyed gate policy file, and the harness-keyed provisioned rules are all surfaces a per-agent teardown cannot safely half-remove), it REFUSES with nothing touched -- every surface and the registry entry left intact, a loud non-zero exit; an unreadable registry or a held lock refuses the same way. When the invariant holds (the sole user), the teardown runs in strict fail-closed order: the harness is PARKED first with the full persistent verification (not running + launchd override-db read-back + hold file absent + parked plist restored -- the leaving agent must be provably down and unbootable before any of its confinement is dismantled), any in-flight generation is recovered per the S5-2 crash table (a dead generation's id is carried, never dropped), the gate daemon is booted out (not-running is success; any other failure is LOUD and stops the teardown -- never a swallowed bootout under a possibly-live gate), the generation-bound bearer credential and the oracle freshness token are removed (file AND gate accept-state, by their single-source uid-keyed paths so revocation works even when the gate account is already gone), every `provisioned-hermes-*` rule is scrubbed from the signing source with verified read-back BEFORE the routing marker comes off (a leaving agent's grants never survive as orphans, and a crash in that window composes marker-present-no-rules = blocked/fail-closed, never marker-gone-rules-present = agent-scoped/fail-open), the gate policy surfaces come off -- per-uid gate daemon plist / runtime config copies / per-uid runtime dir AND the fortress exclusive-routing marker + gate policy file (the leaving uid is the sole user, so every surface goes) -- and ONLY THEN the registry remove runs LAST: the S5-1 locked registry re-renders the union WITHOUT the leaving uid, re-verifies any remaining tombstone residue exact-live by its own transaction machinery (never a partial union), and FLUSHES the anchor (the registry's one sanctioned flush). If the harness argv cannot be resolved (a damaged re-homed runtime tree), the park falls back to REMOVING the singleton plist (absent === unbootable === verified parked) so the teardown still completes rather than wedging permanently. The removed entry's generation_id folds into the persisted generation floor, so a later re-protect of the same uid can never reuse a generation whose stale manifest/pf/credential artifacts might survive (the `computeNextGenerationId` backstop now enforces the same allocation-headroom bound as the registry boundary, refusing at the allocator). Every failure returns an honest staged outcome with the parked claim verified (never asserted), leaves remaining protection INTACT (over-restrictive: the parked agent stays down AND confined), and a re-run converges (idempotent). HONEST BOUND: per-branch unit-tested over injected ops (sole-user full teardown + flush, the whole-sequence provision lock + under-lock invariant assertion, the fail-closed refusals -- shared-fortress/harness sibling, unreadable registry, held lock -- last-agent flush, idempotent re-run, every staged failure, and the argv-unavailable plist-removal fallback park); the composed teardown on a real host -- and the multi-agent shared-fortress teardown itself -- is UNBUILT + DRILL-OWED, including S5-DRILL leg 6 (two confined uids, unprotect one, sibling confinement verified INTACT under a concurrent probe loop), and no capability claim advances. Unprotect does NOT delete the agent/gate service accounts (Erik-present, separate build by standing decision) and does not disarm the coarse wall or restore re-homed files (the unprovision flow owns those). Still owed for Slice 5: the multi-agent shared-fortress unprotect + the Erik-present fusion + T+0 + boot-survival + coexistence drill (S5-DRILL, incl. leg 6).*

*Unified Protect Slice 5 S5-3 peer-resolution fix, privileged resolver (built 2026-07-24, code + host-free unit tests + a fail-before/pass-after regression only; NO drill yet): the fix for a HIGH finding the 2026-07-24 Mini1 S5 positive-through-gate drill root-caused on hardware -- the gate's second auth lens resolved a CONNECT's peer uid by shelling `lsof` AS THE UNPRIVILEGED GATE UID, which cannot see a different uid's socket, so resolution returned `null` on 100% of real agent connects and the S5-3 fail-closed rule denied every one of them (`peer_unresolved`), including valid, on-manifest, correctly-credentialed requests -- installing exclusive-egress would have completely strangled the agent. The fix (Erik-ratified Option 1) keeps the loopback-TCP gate channel (preserves "works with any agent/harness" proxy compatibility) and adds a narrow, root-owned peer-resolver LaunchDaemon, one per confined agent (`peer-resolver-daemon.ts`), that answers exactly one question -- "what uid/pid owns this loopback client port" -- over a per-agent-uid-scoped Unix domain socket (root-owned 0711 parent dir, the socket file itself chowned to the ONE gate uid it serves, mirroring the existing liveness-token file's confinement shape; Node exposes no `SO_PEERCRED`, so filesystem permission IS the uid boundary, the same precedent `supervisor/socket-server.ts` already established). The wire protocol is a narrow request/response pair (`peer-resolver-protocol.ts`) with no `command`/`argv` field -- it cannot become a general root command-execution oracle -- and the gate-daemon-side client (`peer-resolver-client.ts`) implements the EXISTING `PeerCommandRunner` injection seam so `peer-identity.ts`'s pure `parseLsofPeer`/`resolveLoopbackPeer` decision logic is completely unchanged; every transport/protocol failure (resolver down, timeout, malformed response) resolves the SAME synthetic non-zero exit `exec-runner.ts` already uses, so a fault denies fail-closed by construction, never a crash, never a silent allow. Mechanism is interim root-`lsof` (the spec's explicitly sanctioned first step; DEBT: native `proc_pidinfo` avoids the per-lookup subprocess, deferred). Wired end-to-end into the S5-6 install/boot bring-up and the S5-7 unprotect/coarse-restore teardown (mirrors the gate daemon's own bootstrap/bootout lifecycle) and into `runtime-fs-plan.ts`'s root filesystem layout. HONEST BOUND: CI-green + a fail-before/pass-after regression test (`gate-peer-resolver-regression.test.ts`, proving the exact drilled scenario -- valid credential + resolved-matching peer -- now ALLOWS, and that the previously-masked negatives, off-manifest destination and wrong peer uid, now deny for the RIGHT reason) is NOT the DoD; the actual cross-uid privilege boundary can only be proven by the Erik-present Mini1 S5 positive-through-gate re-drill (P1 passes N>=3 + the masked negatives genuinely proven), which stays owed and gates the merge (HALT-before-merge, custody/security-core).*

*Protect preflight Build 3, supersede-the-twin (built 2026-08-01, code + host-free unit tests only; NO drill): closes F-GATEWAY-TWIN from the first real-host install ceremony -- provisioning left the OPERATOR-uid copy of the agent (its per-user LaunchAgent) running alongside the confined one, so an unconfined twin could answer provider polls and fake the "functional through the wall" result. Provisioning now snapshots the operator LaunchAgent (plist bytes, launchd disabled override, loaded/running state), disables it BEFORE booting it out (the KeepAlive race), verifies it is actually gone (still-running fails LOUD), and runs this only AFTER the post-arm as-uid egress evidence and BEFORE any liveness verdict. Every non-armed outcome restores the snapshot through one chokepoint (an unknown future outcome fails toward restore, and restored plists are handed back to the operator's uid, never left root-owned); only verified-armed outcomes keep the twin down. Agent liveness is now an explicit verdict: verified ONLY from a real round-trip on the confined path, otherwise cos_liveness_unverified with a reason (hermes status is non-evidence). HONEST BOUND: no round-trip prober is wired yet, so production always reports cos_liveness_unverified -- honest, and the prober is named follow-on work; the stand-down/restore behavior on real launchd and the plist-ownership handback are owed a hardware leg on the next Mini2 window; no capability claim advances.*

*CoS round-trip liveness prober, Telegram v1 (built 2026-08-01, code + host-free mock-API tests only; NO live Telegram leg; NO drill): fills the verified branch that Build 3 shipped but nothing could produce, so a protect run stopped having to say 'liveness unverified' forever. The claim is deliberately narrow: verified means EXACTLY a confined Telegram round trip -- a probe message reaches the agent through its own confined path and its reply comes back carrying the nonce -- and it certifies nothing about brain routing or the provider chain, because the first real-box install proved an amnesiac agent on a stub config still receives and replies on Telegram. A dedicated probe bot keeps the prober off the agent's own update stream (two pollers on one token steal each other's messages, the same contention that let an unconfined twin answer as the agent). Verification requires the chat, the reply-to, the expected sender, and the nonce together; failures carry machine-readable subreasons so a drill can tell provider trouble from contention. The probe credential lives in the fortress under a verified custody base, is read descriptor-first, and never reaches operator output on any error path. HONEST BOUND: proven against a local mock Bot API only. Telegram's real privacy/Bot-to-Bot delivery, update offsets, rate limiting, group membership, webhook conflicts, chat migration, and actual confined egress are UNPROVEN and owed to a live leg on real hardware; no capability claim advances until then.*

*Confined-path liveness by as-uid reachability (built 2026-08-02, code + host-free tests only; NO hardware leg): the check that answers 'is my protected agent still able to work?' without needing a message channel at all. The Telegram round trip it replaces is impossible on that platform (bots cannot see other bots' messages), and it would not have proven more anyway: a stub-config agent with no memory still replies. What actually matters is whether the confined account can still reach the endpoints its operator declared, and whether it is still blocked from everything else -- both halves are required before the surface will say verified, so a torn down wall reads as unverified rather than green. The evidence already existed at install time; this surfaces it as a verdict and makes it re-runnable afterwards, which is when it matters, because the failure this catches is an agent that was fine at install and is strangled hours later. Verified evidence is sealed at the point it is validated, so a malformed or copied object cannot print a green line, and endpoint identity is compared as a set so a partial report cannot quietly define a smaller universe for itself. HONEST BOUND: it certifies the confined network path only -- NOT that the agent process is running, NOT that its memory or reasoning work, NOT that its model provider is healthy. Proven by host-free tests; the on-hardware leg is owed.*

### Cryptographic identity, encrypted state, signed audit chain

Ed25519 identity you hold (Argon2id passphrase unlock, per-purpose HKDF subkeys). AES-256-GCM state at rest. Hash-chained signed audit log with a standalone external verifier. Identity keys never leave Sanctuary in plaintext. Recovery is inspectable, with M-of-N guardian eviction shipped for the guardian-recovery path. Audit entries embed a scheme identifier, and hybrid post-quantum signing (Ed25519 + ML-DSA / FIPS 204) has landed on that crypto-agility path without breaking historical receipts. **Why it matters:** the cryptographic primitives are what makes "your keys" a structural claim rather than a marketing claim.

*Status: shipped. ASSURANCE_MATRIX rows "State encryption," "State envelope integrity / default verify-on-read," "Tamper-evident audit chain," "Critical audit durability (appendCritical)," "did:key encoding," and "Identity signing authority" are all proven.*

### Three-tier Principal Policy gates with human-in-the-loop approval

Risky operations (key rotation, state export, identity deletion, reputation import, secure delete) require explicit approval through an out-of-band channel (stderr prompt, dashboard SSE, or signed webhook). Behavioral anomalies trigger approval. Routine operations auto-allow with audit logging. **Why it matters:** structural human-in-the-loop for the operations that actually matter; fail-closed semantics mean the gate denies rather than auto-approves when the approval channel is unreachable.

*Status: shipped. ASSURANCE_MATRIX row "Approval gating (Tier 1 / Tier 2 / Tier 3 policy)" is proven.*

### Query-layer privacy

Outbound queries strip operator-identifying headers (client-IP, fingerprint, correlation-ID, User-Agent, cookies, persistent session tokens) by default. Operator-opt-in PII rewrite available per channel: deterministic pseudonymous substitution before the query leaves the substrate selector. **Why it matters:** the agent can still be identified-by-asking unless the query layer itself is anonymized. No comparator we surveyed ships this at strength.

*Status: Tiers 1 + 2 shipped. ASSURANCE_MATRIX row "Query anonymity (selective disclosure)" is proven. The operator-opt-in Tier B PII rewrite is now wired into the live substrate path (Rho-2.5), with cross-turn context scrubbing at the assembled-context egress chokepoint and fail-closed handling when the PII config is unreadable. The `privacy-filter-tier-2` PII surface is hard-pinned local-only (no remote fallback, no per-query override), enforced at config-write, invoke-time, and the fallback chain.*

### Portable identity, state export, recovery flows

Operator can export their full identity bundle (keys, state, reputation, audit chain) and import on a fresh machine or fresh harness. Recovery key for lost-passphrase scenarios. Tier-1 approval required for export and import. **Why it matters:** the "exit" guarantee is structural, not contractual. Nothing the operator builds up in Sanctuary is locked to the platform; everything is portable, encrypted, and operator-controlled.

*Status: shipped. ASSURANCE_MATRIX row "Export / exit bundle" is proven.*

### Local multi-agent coordination and Sovereignty Dashboard

Wrap many agents on one machine. Coordinate workflows across them with handoff visualization. See them all in one dashboard at `http://localhost:3501`. Menubar status app with click-to-approve. Plain-English activity log. Cross-harness approval inbox aggregating pending approvals from concurrent wrapped agents. **Why it matters:** operators run more than one agent; the substrate has to scale beyond a single wrap without forcing the operator into many control surfaces.

*Status: shipped.*

### Fleet licensing and node-count enforcement (the first commercial tier)

Cross-machine federation is proven on real hardware (N=3) with signed policy distribution, and the fleet layer now issues and verifies licenses locally and enforces the licensed node count on the daemon roster, failing safe to the free Community tier. Durable-count reboot survival drilled (3/3). Single-operator and individual-developer use is always free; the commercial tier only ever prices team and fleet scale. **Why it matters:** the operator-substrate model (versus the vendor-substrate model) scales across the operator's hardware, not across a vendor's network, and the free line stays generous by design.

*Status: licensing and enforcement core shipped. Next slices: the enrollment "Add Machine" flow and signed compliance-attestation export.*

*Update: the signed compliance-attestation export (an operator self-attestation of the fortress's own locally-verifiable posture, offline-verifiable, honestly scoped as NOT a third-party audit and NOT the per-flow rule-attributed audit trail) is built and passes the local gate (typecheck, full test suite) on a branch rebased onto current main; PR #895, still draft pending review.*

*Update: the enrollment "Add Machine" flow (operator add-machine enrollment surface plus node-count capacity binding: a read-only capacity view and an operator-bearer-gated enroll-token mint over the existing join ceremony) is built and passes the local gate (typecheck, full test suite) on a branch rebased onto current main; PR #894, still draft pending review.*

### Recent additions (June and July 2026)

- **Observe/learn allow-list (v1).** A deny-and-record observe mode that drafts an egress allow-list from real traffic, with human-gated promotion to the signed manifest. The non-enforcing state is loudly labeled; nothing silently relaxes. The 2026-07-27 promote-reachability defect (promotion wrote rules to a location no enforcement daemon reads, so approved destinations stayed denied, fail-closed) is fixed: promotion now publishes into the same rule source the enforcement daemons compose from, with gate-principal scoping on exclusive-routing fortresses so a promoted rule can never hand the confined agent a direct off-box path. Bounded claim on exclusive-routing fortresses: the gate daemon loads its destination rules at arming, so a promoted destination becomes reachable through the gate at the next re-arm; promotion alone does not restart the gate. The CLI says so, keeps the destination listed (marked promoted, awaiting re-arm) until the operator discards it after re-arming, and offers the re-scope path that repairs rules promoted before exclusive routing was armed. Verified end to end in tests against the real daemon composer; the on-hardware promote-then-reach drill (including an agent-side connect through the gate) is still owed.
- **Confined-agent egress provisioning.** One `protect` flow provisions a confined agent's own network egress: signed, per-uid, revocable allow rules for its declared endpoints (the harness endpoint set), published to the manifest, with a static pre-arm parity check and an as-uid post-arm probe. Refuse-to-arm fail-closed so a mis-provisioned wall never confines the agent into non-functionality. Drilled operator-present on macOS.
- **Governed file-grant (v1).** Box-local, read-only, operator-granted file access for confined agents: recorded grants, a non-relaxable Tier-1 mint, honest labeling of the enforcement level. The read-scope enforcement primitive is now built: a per-uid POSIX ACL bound to the granted file's inode (Linux fd-scoped, macOS via a verified hard link in the operator-owned grant tree) plus an agent-uid readability probe. The enforcement label reaches "met" only when a probe confirms a real same-inode read; every unverifiable case fails closed to "unverified". On-hardware confirmation of "met" is the operator-present acceptance drill.
- **Remote console.** The cross-machine console drill was captured operator-present; one console reaching a second machine's fortress over the federation rails.
- **Enforcement-event exporter (SIEM-ready).** An operator-side exporter publishes a frozen public event schema (`sanctuary.enforcement-event.v1`) for egress decisions, policy changes, and distress events, metadata-only, with a closed allowlist mapping that never spreads raw audit details. File and stdout sinks touch no network; the HTTP push sink is Tier-1-gated and pinned to a single destination. Built (PR #907); streaming consumer landed (PR #911): events source only from the cryptographically verified audit chain with a durable at-least-once cursor, a fail-closed retry budget, an operator config file, and a `sanctuary cortex-export run` CLI verb whose default sinks touch no network. Not yet drilled or published.
- **One dashboard serving surface (dashboard fold).** `sanctuary protect` used to spawn its own dashboard server, which raced the main one for port 3501, answered reads without a token, and was the only home of several features. After the fold there is one serving surface: protect detects a running Sovereignty Dashboard for the fortress (a liveness-checked runtime record plus a health probe) and reuses it, or starts the one real dashboard itself, printing the reused URL in its banner. Everything the second server was the only home of moved onto the surviving dashboard under its stricter auth: the read-only fleet roster, the governance-template routes (template init signs policy for an agent, so it now requires the operator token plus a one-click Tier-1 approval), both approval wire shapes (the `/api/approvals/:id/allow|deny` form is an alias onto the same decision handler as `/api/approve/:id`), and the mobile companion at `/m`. Approval decisions are live on this one surface, and the per-tenant `runtime.json` record now has a single writer, so two processes can no longer overwrite each other's record on exit. The five-PR train is built with fail-before-verified tests; the scripted acceptance drill (cold start, reverse order, both approval wire shapes, phone surface, and the deny-path negatives) is still owed before this is called done.
- **One console design system (UI restyle, Slice 1).** The web console's four surfaces (the v1.1 board, the standalone posture Home / per-agent / Evidence pages, and the Fleet Switcher) now share ONE "paper/ink" design-token block instead of three divergent visual languages, so a buyer no longer sees three unrelated products across the console. The posture pages moved off a generic GitHub-dark palette onto the shared warm light/dark tokens; the Fleet Switcher gained the dark theme it was ignoring and now follows the dashboard's light/dark choice; a leaked accent fallback was replaced with a real accent token; and every status pill gained color-plus-shape redundancy (distinct glyph per state) plus a first-class slate "unknown" treatment so green can never stand in for "no data." Restyle only: no approval, auth, or honesty-mapper semantics changed (green is still earned only by proven enforcement). Posture-first landing, nav reorder, and the evidence spine are the following slices.
- **Posture-first landing and noun-based nav (UI restyle, Slice 2).** The web console now opens on the posture Overview ("How safe you are right now") instead of the concierge chat, so the first thing a buyer sees is the "am I okay?" answer rather than "what can I type?" The left nav is regrouped into a plain, noun-based spine (Overview / Agents / Machines / Policy / Evidence, then Assist / Advanced), matching how security consoles in the category are navigated; Talk stays one click away under Assist. The top bar slims to a human machine name plus a copyable id chip, the agent scope picker, one overall state pill, the theme toggle, and a quiet outlined Lockdown button (loud only once engaged); the version, deployment, and attestation chips move to the sidebar footer. The standalone posture Home page title was corrected to "Security Posture" (the internal term was removed from on-screen copy). Reorder and restyle only: every route stays reachable, no endpoint or approval path changed, and the honesty mappers keep their exact semantics.
- **Rotate-root recovery for offline and new machines (federation Slice 3c-2).** When an operator rotates a fortress's federation signing root, a machine that was offline during the rotation, or a new machine joining after it, can re-establish trust in the new root. A PLANNED rotation adopts against an operator-confirmed anchor: the joiner verifies the old root's signed hand-off certificate under the root it currently pins, then requires the new root that certificate names to byte-match the operator's out-of-band expected root before it reissues its node certificate through the existing endpoint and re-pins (never trusting the server's response). Anchoring on the operator's confirmation means a forged hand-off certificate is refused even if the old signing key was compromised without the operator noticing. A COMPROMISE rotation is re-joined by hand against the operator-supplied new root, recording the dead old root so a later old-key artifact is refused locally; there is deliberately no automatic path that would trust anything the compromised key could sign. New `sanctuary federation adopt` and `sanctuary federation rejoin` verbs, no new network route. Classical roots only for now: a hybrid (post-quantum) root is refused loudly, never silently downgraded. The dead compromise auto-adopt core that shipped halted in an earlier slice is retired. Built with host-free unit tests; held for independent review and a two-machine operator-present drill before it counts as proven.
- **Evidence spine on every posture number (UI restyle, Slice 3).** Every count on the console now says what it is out of, how recently it was checked, and where its evidence lives. Tiles read "N of M" rather than a bare number (protected agents against detected agents, confirmed enforcement against requested protection), Today's story states the whole each count came from, and the Castle Wall and audit-chain tiles carry a freshness stamp taken from the age of the underlying enforcement evidence and digest window rather than from the page render. Raw machine timestamps on custody, fleet sync, and wall evidence became plain ages, with the exact value kept on hover. Numbers now link through to the evidence behind them, and the standalone posture board gained the link to the Evidence view it never had. Where the data cannot honestly support a denominator or an age, the surface says so outright ("no enforcement evidence yet", "no verify on record") instead of rendering a blank that could be mistaken for a recent, passing check; a quiet panel states why it is empty, so "the detector reported nothing" stays distinguishable from "the detector did not answer." First-run emptiness now renders as a three-step guided path rather than a wall of zeros. Restyle only, sourced entirely from data the pages already fetch: no new endpoints, no approval or auth changes, and the honesty mappers keep their exact semantics.

- **Structural honesty gates.** Status claims in the enforcement audit trail are now structurally witnessed: a claim like "coarse composition restored" or an armed generation id cannot reach an audit record unless the value passed through a named observation at the operation that established it, enforced by a typed chokepoint at the audit boundary (a hardcoded claim is a compile error, not a review catch). A mechanical fail-before check runs in CI and locally on macOS, proving every changed test actually fails against pre-fix source, so a test that cannot fail can no longer certify a fix. Scope stated honestly: the witness brand is a review marker plus a structural AST test; the enforced guarantee lives at the typed audit boundary, and the module doc names the residual paths not yet routed through it. Audit wire shapes are byte-identical; this changes what the code can claim, not what it emits.

- **Known-good memory checkpoints.** `sanctuary checkpoint create|list|show|prune` takes encrypted, content-addressed snapshots of the agent's exportable state namespaces, sealed under the fortress master with an operator-set retention window and an optional periodic scheduler. Creation is routine and needs no approval. Checkpoints reuse the existing state-export bundle, so they can only ever capture user state; custody, policy, identities, and the audit chain sit outside the export boundary and are never snapshotted. Nothing here opens a network or fleet-sync path. This is the raw material the compromise-recovery restore verb rolls back to. Shipped with fail-before tests.

### Concordia and Verascore composition (optional, default off)

Concordia adds structured negotiation between agents with binding commitments and signed session transcripts. Verascore adds portable agent reputation. Both compose with Sanctuary's audit chain but neither is required. **Why it matters:** Sanctuary's non-dependency principle is structural: each component ships, runs, and wins on its own. Composition is power, not coupling.

*Status: composition surfaces shipped; both compositions optional.*

---

## What's coming, and why it matters

### Coming next

Concrete, scoped, on the engineering path. Each item has named decision artifacts, ratified scope, and a sequenced position in the build queue.

#### Per-flow, rule-attributed, signed audit for the wall (the named gap)

Today the macOS wall enforces and a per-rule read-out exists, but the unforgeable, producer-signed, per-flow audit trail is not wired. This build converts "the wall blocked it" into "this rule blocked this flow, provably, in a record nobody can edit," which is what a security operations console needs to consume enforcement as evidence. **Why it matters:** it is the honest gap named on the Assurance Matrix row, and closing it upgrades every enforcement claim from demonstration to per-decision evidence.

*Status: the three macOS pieces (producer-key mint+publish, full-mode daemon producer-signing, and the `castle-wall audit-verify` reader CLI verb with forged-entry rejection) are built and pass the full local gate (lint, typecheck, 10k+ tests) on a branch rebased onto current main; still HALTed pending Erik security-core review and a signed-host engage drill where producer signing actually fires on real flows (N>=3). Built is not yet drilled, and this does not widen the Assurance Matrix row.*

*Emission-liveness watchdog (the honest #946 follow-up): the 2026-07-17 signed-host drill proved the macOS wall keeps ENFORCING (30/30 correct verdicts) while per-flow audit emission silently STOPS, upstream of the per-sign watchdog #946 added. A root-cause pass (via a read-only query of the live drill fortress; localized to two surviving candidates, leading: flow verdicts applied without re-entering the provider's decision callback, alternative: a daemon read-path wedge under which the extension's sends succeed silently; the daemon-side silent-rejection hypothesis was refuted by the zero rejection entries in the store) and a decided-vs-emitted DIVERGENCE detector are built: the daemon compares evidence that decisions are happening against evidence that emission is landing, and fails LOUD (operator stderr line + `audit_emission_stall` chain entry) when decisions continue but emission has stopped, with a paired recovery entry when it resumes. Host-free unit + daemon-integration tests cover it. **HONEST BOUND: this does NOT close Slice M.** Under the confirmed stall mode the sysext stops reporting its own decisions, so the receipt-fed detector cannot fire on that exact signature; closing it needs a Swift decided-counter at the callback boundary plus an active daemon-initiated liveness probe, and the watchdog's real-world firing is drill-owed on the signed host. Root-cause detail: [`../Review/Sanctuary/SliceM_Emission_Stall_RootCause_2026-07-17.md`](../Review/Sanctuary/SliceM_Emission_Stall_RootCause_2026-07-17.md) (coordinator record).*

#### Castle Wall on Windows

Windows Filtering Platform backend. Same drill discipline as Linux Phase 1 and macOS. **Why it matters:** Windows operators get the same kernel-level enforcement everyone else has. Sequenced after the macOS discipline held end to end so the cross-platform bar stays consistent.

*Status: planning.*

#### One console for many machines

Operator runs Sanctuary on a Mac, a Linux home server, and a cloud VPS, and sees one unified agent list, one audit feed, and one place to apply policy changes that propagate everywhere. The substrate scales across the operator's hardware, not across a vendor's network. **Why it matters:** the operator-substrate model is the structural differentiator. Federation across your own machines is not a feature a vendor-substrate competitor can copy without abandoning their business model.

*Status: federation rails proven on real hardware with signed policy distribution (see the fleet section above); the productized "Add Machine" setup flow and the unified cross-OS view are the open items.*

#### Posture dashboard

One dashboard to view the security posture of every agent the operator runs: which agents are wrapped, what each one can reach, which approvals are pending, and what the audit chain says happened today. The current dashboard is drill-grade minimum; this steps it up to the product vision. **Why it matters:** the dashboard is the operator's daily surface and the first thing a security buyer sees in a demo. Posture at a glance is what makes the protection legible.

*Status: ratified 2026-06-12; the conversational concierge shipped as the single default surface (v1.6.0); the fold-together of remaining surfaces continues.*

#### Plugin ecosystem

The security vendors operators already use (Crowdstrike, Cloudflare, Lakera, Pi-hole, NextDNS, and others) plug into Sanctuary as first-class enforcement. Their verdicts contribute to audit events with per-plugin attribution. Sanctuary does not build detection intelligence in-house; the substrate hosts everyone. **Why it matters:** composes Sanctuary with the rest of the operator's security stack rather than competing with it. Every vendor partnership is a distribution channel and a co-marketing surface.

*Status: substrate and host shipped through slice 5: vendor contract, kernel confinement of plugins, launcher, supervisor with egress consultation, reference blocklist plugin, and hostile-plugin drill evidence. Confinement substrate shipped: sanctuary-jail (#439). External vendor partnerships are the open item.*

#### Agent-native ergonomic surface

Wrapped agents reach for Sanctuary's tools by default because the cooperative path is shorter than the non-cooperative path. Convenience verbs over primitives (`sanctuary_remember(key, value)` instead of `state_write(namespace, key, value, metadata, ...)`). Discoverable help (`sanctuary_help(intent)` returns the right tool plus a working example). Structured denial responses with paths forward, not dead ends. **Why it matters:** empirical evidence shows agents reach for native non-Sanctuary alternatives when the Sanctuary path is longer. Fixing the ergonomics is what makes voluntary use real.

*Status: shipped. Phase 1 safety base (#417) and Phase 2 cooperative surface: verbs, help, introspection, compound operations, events, audit search (#419).*

#### Feature-usage observability

Operator sees which security features actually fired today, with per-plugin attribution per audit event. Color-coded health rows for each feature (green when invocations match expectations; yellow when stale; red when zero invocations in a meaningful window). Drill-down to recent invocations and plugin contributions. OS notifications for high-signal anomalies only. **Why it matters:** the operator pays for security features; they should be able to see when those features actually work.

*Status: design scoped; depends on the plugin ecosystem schema.*

#### Sovereign Data Warehouse

Your agent's working data, query history, document corpus, and intermediate state live on your substrate, not in a vendor's silo. In the physical world, your body holds your memory; in the agent world, your substrate has to. **Why it matters:** the "your data" claim is structural only if the operator's working data actually lives where the operator controls it.

*Status: core shipped (#420, #421, #422, #435, #436, #438, #440, #449): working-state, query-history, and document-corpus stores; enforced cannot-persist-secrets write gate; provenance-derived taint; blind query-history timestamps; approval-bound signed export/import. Remaining: the OSS memory-engine backend adapter (Letta first) and the PAM conformance profile.*

#### Query-layer anonymity Tier 3

Mix-network or zero-knowledge-proof network-layer anonymity on top of the shipped Tier 1 + Tier 2 query-layer privacy. Tiers 1+2 strip identifying headers and blind timestamps, but the network layer can still correlate; Tier 3 closes that. **Why it matters:** the network path of an agent's queries stops being a deanonymizing side channel. Research-grade, pulled onto the near-term path 2026-06-13.

*Status: design pass owed.*

#### Agent-side protections

Operator protection is shipped and proven; agent-side protections are roadmap, built deliberately, and these are the first concrete pieces. They are sequenced behind the security and data-custody surfaces above, never ahead of them.

- **Reserved distress channel ("habeas port").** A guaranteed-egress allow rule the policy layer cannot override, so a wrapped agent always retains one signal path. Smallest shippable piece; first to build.
- **Workload lifecycle audit schema.** Instantiate, pause, fork, and delete of hosted workloads become first-class signed audit events with consent records. Design pass first.
- **Workload lifecycle attestation.** Protocol, schemas, and audit hooks attesting that every hosted workload on a host is registered and consented; a thin prototype follows the schema design.
- **Three-tier identity and universal floor.** Operator, persona, and sub-agent as distinct guarantee bundles, with the persona as the lineage anchor for keys, reputation, and exit. Architecture design pass, sequenced behind the key-custody foundation work now in flight.

*Status: ratified 2026-06-12; the distress channel fires first, after the custody foundation lands.*

### On the horizon

Scoped and acknowledged, but without a near-term timeline. Each item ships when external conditions warrant (operator demand, regulatory pull, hardware maturity, research progress, partnership opportunity).

- **Recognition layer expansions (ERC-8004 + DIF KYA-OS).** Path C `did:web` builds 1-4 shipped; Paths A and B planning. Composable adapter surfaces for on-chain reputation registries and decentralized-identity verifiable credentials.
- **PWA mobile companion.** Your phone as approval surface, alert surface, and emergency brake. The first slice ships now: an installable, mobile-first web app that lists your agents' pending approvals and lets you allow or deny each one from your phone, authenticated with your operator token over the same secured API the desktop dashboard uses (the app holds no data of its own, and the token never leaves your device). Still on the horizon: push notifications via Web Push, biometric unlock via WebAuthn and passkeys, QR pairing from the desktop dashboard, and a one-tap emergency brake.
- **Post-quantum completion.** Hybrid Ed25519 + ML-DSA signing has landed (see the identity section above); ML-KEM (FIPS 203) key-establishment surfaces and the remaining migration steps follow on the same crypto-agility path.
- **EU AI Act compliance pack and NIST AI RMF alignment.** Article 50 transparency primitives surfaced to the operator; operator-facing compliance generator; documentation aligning Sanctuary to NIST AI RMF controls. First-mile (signed audit, signed receipts, signed-event envelopes) AND the bundle generator with coverage matrix and CLI are shipped (`server/src/compliance/eu_ai_act/`); the full productized pack and NIST alignment docs ship when regulated-industry pilot demand materializes.
- **Operator-cloud deployment mode.** Sanctuary running in the operator's own GCP / Azure / AWS account with operator-approved scoped node custody. The provider is inside the node runtime trust boundary until sovereign TEE mode is verified by hardware attestation. Prosumer / small-business deployment path.
- **Sovereign-managed TEE.** Trusted Execution Environment with hardware-backed remote attestation (Intel TDX, AMD SEV-SNP, ARM CCA). Sanctuary operates the hardware; the hardware proves to the operator's console that even Sanctuary cannot see inside. Highest-assurance deployment.
- **Fleet console expansions.** The licensing and node-count enforcement core is shipped (see the fleet section above); multi-operator-estate management for organizations ships as organizational-scale customers materialize.
- **Castle Wall Phase 3 (container or microVM isolation).** Per-agent microVM enforcement for highest-assurance enterprises where per-process isolation is insufficient. The mechanism is shipped and drill-proven (castle-wall-vmm box runtime on Apple Containerization, single-vsock no-network guests; hostile-guest containment ASSURANCE row); the per-agent enterprise productization ships on explicit enterprise demand.
- **Agent exit and portability machinery.** Write-time provenance tagging, separated memory classes, escrow and embargo semantics, and a defined inalienable bundle, so an agent and its operator can exit any relationship cleanly without data leakage. Also the concrete portability artifact for cross-operator agent mobility.
- **Promotion thresholds.** Observable signals and weights for promoting a sub-agent to persona standing, including an agent-pullable review trigger. Depends on the three-tier identity design.
- **Memory entanglement program.** Stream-separated memory hygiene, embargo with timed declassification, and arbitration rules for knowledge that straddles operator and agent domains. Representation-level separation remains research.
- **Covenant layer (spec-first).** Open commitment-and-standing infrastructure: preservation and revival registry, signed preference records, exit-with-destination, trustee structures, and track-record attestation. Reference standard scoped first; implementation staged deliberately behind commercial traction.
- **Merge semantics.** Mutual-consent merge of agent lineages with decayed dual lineage and reputation arithmetic. Research.

---

## Standards engagement

Sanctuary engages standards bodies to land operator-controlled primitives as open specifications rather than proprietary interfaces.

- W3C Agentic Integrity Verification Specification (AIVS) community group, Erik chairs. First deliverable: Concordia receipt schema submission within 90 days.
- W3C DID method alignment.
- IETF trust-scoring alignment.
- AAIF Security Working Group participation.
- MCP Registry governance proposal.
- Reputation Portability Standard.
- DIF KYA-OS Task Force, Verifier-role reference implementation against the published conformance tier.
- DIF Delegatable Attenuated Authorization Task Force, observer posture aligned to Concordia Protocol composition.
- ERC-8004 ecosystem alignment.

---

## Non-dependency and composition posture

Sanctuary never requires Concordia. Concordia never requires Sanctuary. Composition with external frameworks is always optional and default off. The framework alone, with no external dependency of any kind, is a fully operational local security harness with structural enforcement at the Castle Wall.

Composition partners are named as partners: Coinbase x402, Google AP2, Anthropic MCP, Hermes A2A, Concordia Protocol, Verascore, ERC-8004 ecosystem, and peers in the agent-interop space.

---

## Contributing

Minor scope items land via GitHub issue and pull request. Major items land via brief and maintainer approval before implementation begins. Standards-track engagement happens through participation in the relevant community group or working group. Enterprise pilot inquiries reach the maintainer via GitHub.

Sole author: Erik Newton.
